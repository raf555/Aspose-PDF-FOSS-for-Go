# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## License

This codebase is MIT-licensed (see `LICENSE` at the repo root). Every `.go` file carries an `// SPDX-License-Identifier: MIT` header above its `package` declaration. When adding new `.go` files, preserve this convention — the header on its own line followed by a blank line, then the `package` line.

## API design

When building new functionality, always shape the public API to mirror **Aspose.PDF for .NET**. Its class/method names, types, and call patterns are the reference model: prefer the same concepts (e.g. `Document`, `Page`, `TextFragment`, `Table`/`Row`/`Cell`, `OutlineItemCollection`, `BorderInfo`/`MarginInfo`), the same method names, and the same overall workflow, adapting only where Go idioms require it (error returns instead of exceptions, `io.Reader`/`io.Writer` stream variants, exported funcs over constructors). A Go developer who knows Aspose.PDF for .NET should recognize this API immediately. When a new feature has a counterpart in Aspose.PDF for .NET, check that API first and align with it before inventing a new shape; the existing surface (noted as "Mirrors Aspose.PDF for .NET's …" throughout the Public API section below) shows the pattern to follow.

## Commands

```bash
# Run all tests
go test ./...

# Run a single test
go test -run TestDocumentSplit ./...

# Run tests with verbose output
go test -v ./...

# Build (no binary — library only)
go build ./...
```

## Architecture

Pure Go library. No external dependencies. All PDF functionality is in the root package `asposepdf`; AI-powered operations live in the `ai` subpackage (stdlib-only too — `net/http` against OpenAI-compatible endpoints), so the root package stays free of AI and network code.

### Public API

**`document.go`** — mutable Document API; operations mutate the receiver in place
- `Open(path)` — opens a PDF file and returns a `*Document`; returns `ErrEncrypted` if the file is password-protected
- `OpenStream(r io.Reader)` — opens a PDF from an `io.Reader` and returns a `*Document`; returns `ErrEncrypted` if the file is password-protected
- `OpenWithPassword(path, password)` — opens an encrypted PDF, trying the password as both user and owner password; works on plain PDFs too
- `OpenStreamWithPassword(r io.Reader, password)` — same as `OpenWithPassword` but reads from any `io.Reader`
- `ErrEncrypted` — sentinel error returned by `Open`/`OpenStream` when the file is encrypted; check via `errors.Is(err, asposepdf.ErrEncrypted)`
- `(*Document).PageCount()` — current page count
- `(*Document).Pages()` — returns `[]*Page` live views of all pages
- `(*Document).Page(n)` — returns a `*Page` live view of page n (1-based)
- `(*Document).Rotate(angle, pageNums...) error` — rotates selected pages; rotation accumulates
- `(*Document).SetRotation(angle, pageNums...) error` — sets selected pages to exactly angle, replacing any existing rotation
- `(*Document).Reorder(order) error` — rearranges pages in place; pages may be repeated or omitted
- `(*Document).Append(others...)` — appends all pages from others into this document; nil arguments are skipped
- `(*Document).SetPassword(userPassword, ownerPassword)` — configures encryption; applied on Save/WriteTo
- `(*Document).SetPermissions(p Permissions)` — configures viewer-enforced permissions (print, copy, modify, etc.) for encrypted documents; applied on Save/WriteTo
- `(*Document).SetEncryption(opts EncryptionOptions)` — unified options-pattern API that replaces any prior encryption configuration (passwords + permissions) in one call
- `(*Document).Permissions() (Permissions, bool)` — returns the viewer permissions configured on the document; bool indicates whether the document is encrypted at all
- `(*Document).RemoveEncryption()` — clears any configured passwords and permissions so the next Save produces a plaintext PDF
- `(*Document).ChangePassword(newUserPassword, newOwnerPassword) error` — re-encrypts on the next Save with new passwords while **keeping the current algorithm and permissions** (reads them from the parsed `/Encrypt` via `preserved`, clears it, builds a fresh `encryptConfig`); the document must already be opened+decrypted (`OpenWithPassword`), empty owner defaults to the user password, errors on a plaintext document. Mirrors Aspose.PDF for .NET's `Document.ChangePasswords`
- `(*Document).WriteTo(w) (int64, error)` — writes the document to an `io.Writer` (implements `io.WriterTo`)
- `(*Document).Save(outputPath) error` — writes the document to a file
- `(*Document).Info() (DocumentInfo, error)` — returns Info-dictionary metadata read from live in-memory state; mirrors Aspose.PDF for .NET's `Document.Info`
- `(*Document).ExtractText(opts ...TextExtractOptions) ([]string, error)` — returns text for all pages (one entry per page); `TextExtractOptions{Mode}` selects `TextExtractReading` (visual order, default), `TextExtractRaw` (content-stream emission order) or `TextExtractFlatten` (fixed-pitch grid, positions preserved); mirrors Aspose.PDF for .NET's `TextExtractionOptions.TextFormattingMode`
- `(*Document).ExtractTextWithLayout() ([][]TextLine, error)` — returns structured text lines for each page
- `(*Document).SearchText(query, opts...) ([]TextMatch, error)` — finds occurrences of query across all pages (each match carries its page); mirrors Aspose.PDF for .NET's `TextFragmentAbsorber`
- `(*Document).ReplaceText(old, replacement, opts...) (int, error)` / `(*Page).ReplaceText(...)` — replaces every occurrence and returns the count; redraws the replacement at the same baseline/size/colour in a metric-compatible Standard-14 face (so any text renders even over an embedded subset font); no line re-flow; `ReplaceOptions{CaseInsensitive, Regex}`; mirrors the find-and-replace idiom of Aspose.PDF for .NET's `TextFragmentAbsorber` + `TextFragment.Text` (`replace.go`)

**`document_pages.go`** — page delete/split/extract operations
- `(*Document).DeletePage(n) error` — removes page n (1-based) in place; mirrors Aspose.PDF for .NET's `Document.Pages.Delete(int)`
- `(*Document).DeletePages(pageNums...) error` — removes the given 1-based pages in place; numbers are de-duplicated and validated before any removal (atomic on error); errors on no numbers or on removing every page; mirrors `Document.Pages.Delete(int[])`
- `(*Document).Split() ([]*Document, error)` — returns each page as a separate `*Document`
- `(*Document).Extract(ranges...) (*Document, error)` — returns a new `*Document` with the selected page ranges

**`form_xobject.go`** — reusable Form XObjects (ISO 32000-1 §8.10): draw content once, place it on any number of pages/positions with a single `Do`. Mirrors the intent of Aspose.PDF for .NET's `XForm`
- `(*Document).CreateForm(width, height) *XForm` — an empty Form XObject of the given size. `(*XForm).Canvas() *Page` returns the **drawing surface** — a detached page (not in `doc.pages`) on which the full `*Page` drawing API works (`AddText`, `Draw*`, `AddImage`, `AddTable`, …); `(*XForm).Size()` returns its dimensions
- `(*Page).AddForm(form *XForm, rect) error` — places the form so its `/BBox` maps onto `rect` (per-axis scale + translate via a `cm`). The `/Form` XObject is **built once on the first AddForm (frozen, `ensureBuilt`)** and reused for every later placement — one object, many `Do` invocations across pages — so finish drawing before placing. Errors on nil/cross-document form or an empty rect
- `(*Page).Forms() []*XForm` — the Form XObjects already referenced by the page (parsed from a loaded PDF, or placed earlier): each carries its existing object number + `/BBox`, so it re-places on any page of the same document with `AddForm`. `(*Document).ImportForm(form *XForm) (*XForm, error)` — copies a form **and its whole resource graph** (fonts, images, nested forms) from another document via `importGraph` (the imposition copy machinery: `collectValueDepsDoc` + `rewriteRefs`), returning a placeable XForm; a same-document form is returned unchanged
- Mechanism: `Page` gained an optional `obj *pdfObject` override so `pageObj()` can target a detached canvas dict without polluting `doc.pages`; `CreateForm` makes such a canvas, drawing appends to its `/Contents` + `/Resources` like any page, and `build()` harvests them into `makeFormXObjectWithResources`. Placement reuses `registerFormXObject` (`/Fm0`, `/Fm1`, … in the page `/Resources/XObject`); `bboxFromForm` reads a parsed form's `/BBox`. Out of scope: in-place editing of a parsed form's content stream

**`imposition.go`** — N-up / booklet page imposition (returns a new `*Document`; receiver untouched). A **production helper, not an ISO 32000 feature** — built from spec primitives (each source page wrapped in a Form XObject §8.10, placed via a `cm` transform), so the output is an ordinary, spec-compliant PDF. Mirrors the *intent* of Aspose.PDF for .NET's legacy `PdfFileEditor.MakeNUp`/`MakeBooklet`, but follows this library's `Document`-method paradigm rather than the file-path facade shape
- `(*Document).NUp(opts NUpOptions) (*Document, error)` — imposes pages onto larger sheets in a `Rows`×`Cols` grid; each source page is scaled to fit its cell preserving aspect ratio and centered; sheets fill until all pages placed (last sheet may have empty cells)
- `NUpOptions` struct — `Rows, Cols int` (≥1), `PageSize PageFormat` (zero → A4), `Margin float64`, `Gutter float64`, `Order NUpOrder`, `DrawBorder bool`
- `NUpOrder` enum — `NUpRowMajor` (default), `NUpColumnMajor`
- `(*Document).Booklet(opts BookletOptions) (*Document, error)` — imposes pages two-up reordered for saddle-stitch (print double-sided, fold, read in order); page count padded up to a multiple of 4 with blank halves
- `BookletOptions` struct — `PageSize PageFormat` (zero → `2·W × H` of the first page, i.e. source pages at 100%), `Binding BookletBinding`
- `BookletBinding` enum — `BindingLeft` (default, LTR), `BindingRight` (RTL, mirrors each spread)
- Internals: `importPageAsXObject` wraps a page (content + `/Resources`) as a `/Subtype /Form` XObject with `/BBox` = MediaBox; `importGraph` copies each page's resource graph into the result once (shared objects deduped via a single `idMap`, reusing `collectValueDepsDoc` + `rewriteRefs`); `makePlacement` computes the fit-inside `cm`; `addImposedSheet` emits `q cm /Xn Do Q` per cell. Known limits (v1): inherited `/Resources` from the page-tree parent not resolved (uses the page dict's own `/Resources`); no creep/shingling

**`page.go`** — `RotationAngle` type and constants (`Rotate0`, `Rotate90`, `Rotate180`, `Rotate270`)

**`page.go`** — Page and PageSize types
- `PageSizes(inputPath)` — returns dimensions of every page in a PDF file
- `(*Page).Number()` — 1-based page number within the document
- `(*Page).Size() (PageSize, error)` — page dimensions (width/height) from MediaBox (with inheritance from page tree)
- `(*Page).Rotation()` — effective rotation in degrees (0, 90, 180, 270); reflects Document.Rotate patches
- `(*Page).MediaBox() (Rectangle, error)` — full page rectangle (with inheritance); mirrors Aspose.PDF for .NET's `Page.MediaBox`
- `(*Page).CropBox() (Rectangle, error)` — visible region as a `Rectangle`; falls back to MediaBox if not set
- `(*Page).TrimBox() (Rectangle, error)` — intended trim dimensions; falls back to CropBox then MediaBox
- `(*Page).BleedBox() (Rectangle, error)` — production bleed region; falls back to CropBox then MediaBox
- `(*Page).ArtBox() (Rectangle, error)` — meaningful content extent; falls back to CropBox then MediaBox
- `(*Page).SetMediaBox/SetCropBox/SetTrimBox/SetBleedBox/SetArtBox(Rectangle) error` — set the respective box directly on the page (validated, overrides inherited/referenced values); mirror Aspose.PDF for .NET's `Page.*Box` setters
- `(*Page).SetPageSize(width, height float64) error` — resize the page by setting its MediaBox to `[0 0 width height]` (content is not scaled); mirrors `Page.SetPageSize`
- `(*Page).ExtractText(opts ...TextExtractOptions) (string, error)` — returns the text content of a page in visual reading order (default), raw emission order (`TextExtractRaw`) or laid out on a fixed-pitch grid (`TextExtractFlatten`); unknown font characters become U+FFFD. `TextExtractionMode` enum: `TextExtractReading`, `TextExtractRaw`, `TextExtractFlatten`
- **Flattened layout** (`buildFlattenedTextFromFragments`, `text_layout.go`, `pdf-go-r30a`): each fragment starts at the column its x position maps to, the space between fragments is padding, and a vertical gap wider than 1.5 line heights becomes a blank line — so the output reads like the page in a monospace font, columns stay columns without a table detector, and two revisions of a page diff cleanly. The cell width is the **median** advance per character on the page (the median, not the minimum, so one narrow superscript cannot stretch the page into ribbons); the origin is the leftmost text, so the left column starts at 0 regardless of the page margin. A fragment whose column falls behind text already placed is separated by a single space instead of overwriting it. Mirrors Aspose.PDF for .NET's `TextFormattingMode.Flatten`
- `(*Page).ExtractTextWithLayout() ([]TextLine, error)` — returns structured text lines in visual reading order with coordinates and font info
- `(*Page).Paragraphs() (PageMarkup, error)` / `(*Document).Paragraphs() ([]PageMarkup, error)` (`paragraph.go`) — **structural extraction**: groups a page's text into columns (`MarkupSection`) and paragraphs (`MarkupParagraph{Text, Rectangle, Lines}`). Built on the `ExtractTextWithLayout` pipeline: fragments are clustered into columns by a horizontal occupancy histogram (a wide vertical gutter with text on both sides splits columns), then each column's lines are grouped into paragraphs by the vertical gap between baselines and font-size changes. Heuristic (figures/tables may split/merge); recovers prose paragraph/column structure well. Mirrors Aspose.PDF for .NET's `ParagraphAbsorber` (`PageMarkup → MarkupSection → MarkupParagraph`)
- `(*Page).SearchText(query string, opts ...SearchOptions) ([]TextMatch, error)` — finds occurrences of query on the page in reading order, returning a bounding `Rectangle` per match (`text_search.go`); built on the layout pipeline, matches are located within a single line; literal by default, with optional case-insensitive and RE2-regex modes. Match rectangles use the per-glyph start positions recorded during extraction (`textFragment.runeX` → unexported `TextFragment.runeX`), so sub-fragment boxes are accurate rather than interpolated (right edge approximate only when a match ends a fragment's last glyph). Vertically a match rectangle is the **text box**, descender to ascender (`matchRect`: `Y + descent` .. `+ Height`, with the unexported `TextFragment.descent` carried from extraction), the convention viewers use for highlight and redaction quads (`pdf-go-7lkk`); non-embedded Standard-14 fonts, which have no `/FontDescriptor`, take their ascender/descender from the AFM (`standard14VerticalMetrics`, aliases like Arial included), so their fragment `Height` is the real text box rather than the whole em
- `SearchOptions` struct — CaseInsensitive bool, Regex bool, Rectangle *Rectangle (non-nil limits results to matches whose box intersects the region, per page; mirrors Aspose's `TextSearchOptions.Rectangle`), SearchInAnnotations bool (also matches annotation `/Contents` text — sticky notes, free text, markup — each match's `Rect` is the annotation rectangle; mirrors Aspose's `TextSearchOptions.SearchInAnnotations`). Zero value = case-sensitive literal, whole-page, page-content only, matching Aspose.PDF for .NET's default
- `TextMatch` struct — Text string, PageNumber int (1-based), Rect Rectangle (PDF user space)
- `PageSize` struct — Width, Height in points (1/72 inch)
- `Color` struct — R, G, B, A float64 (values in [0, 1])
- `TextLine` struct — Text, Y, Fragments []TextFragment
- `TextFragment` struct — Text, X, Y, Width, FontName, FontSize, Height, Bold, Italic, CharSpacing, Color Color, IsSubscript, IsSuperscript, Rotation (baseline angle in degrees CCW from the combined text matrix; rotated fragments are isolated into their own TextLines, never merged into horizontal lines; HTML export emits transform:rotate, Markdown export drops all-rotated paragraphs as decoration)
- `(*Page).ExtractImages() ([]Image, error)` — returns all images found on the page
- `(*Document).ExtractImages() ([][]Image, error)` — returns images for all pages (one slice per page)
- `Image` struct — Data, Format, Width, Height, BPC, ColorSpace, X, Y, PageWidth, PageHeight, Inline
- `ImageFormat` — ImageFormatPNG, ImageFormatJPEG
- `ImageColorSpace` — ColorSpaceDeviceRGB, ColorSpaceDeviceGray, ColorSpaceDeviceCMYK, ColorSpaceIndexed, ColorSpaceICCBased
- `(*Image).Save(path) error` — writes the image data to a file
- `(*Image).WriteTo(w) (int64, error)` — writes the image data to a writer
- `ImageInfo` struct — Width, Height, BPC, ColorSpace, Format, X, Y, PageWidth, PageHeight, Inline, Name
- `(*ImageInfo).Extract() (*Image, error)` — decodes the image and returns the full Image with pixel data
- `(*Page).ImageInfos() ([]ImageInfo, error)` — returns metadata for all images without decoding
- `(*Document).ImageInfos() ([][]ImageInfo, error)` — returns image metadata for all pages without decoding
- `(*ImageInfo).Replace(path) error` — replaces image data from a file; format detected by magic bytes (JPEG, PNG); position unchanged
- `(*ImageInfo).ReplaceFromStream(r) error` — replaces image data from an io.Reader
- `(*ImageInfo).Remove() error` — removes image from page (resources + content stream); XObject stays in doc objects
- `Rectangle` struct — LLX, LLY, URX, URY (PDF rectangle in points)
- `(*Page).AddImage(path, rect) error` — adds an image from a file to the page; format detected by magic bytes (JPEG, PNG)
- `(*Page).AddImageFromStream(r, rect) error` — adds an image from an io.Reader to the page
- `(*Page).AddInlineImage(path, rect) error` / `(*Page).AddInlineImageFromStream(r, rect) error` — draws a small image **inline** in the content stream (`BI … ID … EI`, ISO 32000-1 §8.9.7) instead of as an Image XObject; for tiny one-off pictures (icons, rules). PNG and JPEG inputs; both are decoded to 8-bpc samples and re-stored as Flate, then ASCIIHex-wrapped (`/F [/AHx /Fl]`) so the `EI` boundary is unambiguous (the `>` EOD never recurs in hex — a binary or ASCII85 stream can contain a spurious `>EI`). PNG alpha is flattened over white (inline images cannot carry a soft mask). `AddImage` (an XObject) stays the recommended default for anything but tiny images (`image_inline_write.go`)
- `ImageToDocument(path, opts...) (*Document, error)` — creates a single-page PDF from an image file; DPI-aware page sizing
- `ImageToDocumentFromStream(r, opts...) (*Document, error)` — creates a single-page PDF from an image reader
- `ImageToDocumentOptions` struct — PageWidth, PageHeight, MarginLeft, MarginRight, MarginTop, MarginBottom
- `(*Document).RemoveUnusedObjects() int` — removes objects not reachable from any page or from the document catalog (`/Root` is a GC root too, so `/Metadata`, `/OutputIntents`, `/AcroForm`, `/StructTreeRoot`, `/Names`, etc. are never dropped just because no page also references them); entries the writer rebuilds are not followed (`catalogKeyRebuiltByWriter`: `/Pages` always, `/Outlines` once the tree is loaded via `Outlines()`), nor is a page's own `/Parent` (`collectReachableIDs`) — on a reopened file the old page-tree number is free and the next new object takes it, which would otherwise keep that object alive forever; returns count of removed objects
- `OptimizeImageOptions` struct — MaxDPI, JPEGQuality, ConvertPNGToJPEG
- `(*Document).OptimizeImages(opts) (int, error)` — optimizes images to reduce file size; downscales above MaxDPI, converts opaque PNG to JPEG
- `(*Document).Optimize(OptimizationOptions) (OptimizationResult, error)` (`optimize.go`) — unified size-reduction pass mirroring Aspose.PDF for .NET's `Document.OptimizeResources(OptimizationOptions)`. Folds `RemoveUnusedObjects`/`OptimizeImages`/`SubsetFonts` and adds three lossless transforms, run in order images→fonts→compress→dedupe→remove-unused→compress-objects. `OptimizationOptions{RemoveUnusedObjects, SubsetFonts, CompressStreams, RemoveDuplicateStreams, CompressObjects, Images *OptimizeImageOptions}`; `DefaultOptimizationOptions()` is the safe lossless preset (all on, including object-stream packing, except image recompression, which is opt-in via `Images` because it can be lossy). `OptimizationResult` reports per-op counts, including `CompressedObjects` (objects eligible for packing; the packing itself happens on Save/WriteTo). **`compressUncompressedStreams`**: marks source streams stored without a `/Filter` (`Decoded=false`) as decoded so the writer Flate-compresses them (skips `/Type /Metadata` — PDF/A wants it raw — image XObjects, and streams < 48 bytes). **`removeDuplicateStreams`**: hashes each stream (`streamSignature` = sha256 of `canonicalDict` sans `/Length` + `decodedStreamData`), merges byte-identical stream objects into the lowest-numbered keeper, and repoints every reference — including refs embedded in content-stream bytes — via `rewriteRefs`; limitation: dicts must be literally equal (a producer that cloned a subtree with fresh object numbers isn't deduped). **`CompressObjects`** (in the default preset, `objstm_write.go`, `pdf-go-o0wy`): `Optimize` sets `Document.compressObjects`, and `buildDocumentPDF` branches to `buildObjectStreamPDF`, which writes ineligible objects as ordinary indirect objects, every other object in Flate-compressed `/ObjStm` streams of up to 100 (`objStmCapacity`), and a `/Type /XRef` cross-reference stream instead of the classic table (header raised to at least `%PDF-1.5`). Never packed (`objectPackable`): streams, the encryption dictionary, signature dictionaries (their placeholders are patched by byte offset). A packed object is written without per-object encryption — the object stream is encrypted as a stream under its own number — and the cross-reference stream is never encrypted. `appendRevision` writes a cross-reference stream section (`encodeRevisionXRef`, `/Index` subsections + `/Prev`) when the file's last section is one, so incremental signing and `AddValidationInfo` keep the file's kind. The flag lives on the in-memory document only (reopening a compressed file and saving writes classic unless `Optimize` runs again) and `SaveLinearized` ignores it. **PDF/A-1 guard**: PDF/A-1 is PDF 1.4-based, where neither object nor cross-reference streams are allowed, so `buildDocumentPDF` takes the object-stream branch only when the document's own XMP does not identify it as PDF/A-1 (`isPDFA1`, `validate_pdfa.go` — `pdfaid:part` read via the same regex `ValidatePDFA` uses). Checking at write time, not just the flag, covers every call order — `ConvertToPDFA(PDFA1B)` followed by `Optimize(DefaultOptimizationOptions())` still writes classic — and a document opened from an existing PDF/A-1 file whose XMP was never touched by `ConvertToPDFA` at all. `ConvertToPDFA` also clears `compressObjects` for PDF/A-1 formats as a convenience, but the writer-side check is what actually enforces it. Corpus: 1003 documents resaved compressed, qpdf's syntax check found no problems introduced by the packing (16 problems found were pre-existing in the source files), 21.6% smaller than the classic save. `UnembedFonts` is the remaining follow-up
- `(*Document).ConvertToGrayscale() error` (`grayscale.go`) — converts the whole document to grayscale in place by mapping every colour to its luminance grey (`0.3R+0.59G+0.11B`; CMYK via CMYK→RGB→luminance). Mirrors the intent of Aspose.PDF for .NET's `RgbToDeviceGrayConversionStrategy`. Coverage: content-stream device colours (`rg`/`RG`/`k`/`K`→`g`/`G`, and `sc`/`scn` when the active `cs`/`CS` space — tracked + resolved from `/Resources/ColorSpace` — is DeviceRGB/Gray/CMYK or ICCBased N=1/3/4, converted to equal-channel grey in place); raster images (decoded via the existing image pipeline, re-encoded as 8-bpc `/DeviceGray`, `/SMask` preserved, `/ImageMask` stencils left to the greyed fill colour); axial/radial (type 2/3) shadings + shading patterns (greys the function `/C0`/`/C1` and `/Background`, sets `/ColorSpace /DeviceGray`); tiling patterns (recursed); and annotation `/C`/`/IC`/`/MK` colours + `/AP` appearance streams — recursing through Form XObjects (each content/image stream processed once via visited-sets). Best-effort / left unconverted (documented): Separation/DeviceN tints, Pattern-name fills, Indexed palettes, and type 0/4 shading functions. Content is rewritten with `serializeContentOps` into a single fresh `/Contents` stream
- `(*Document).FlattenTransparency(opts ...FlattenTransparencyOptions) (int, error)` (`flatten_transparency.go`, `pdf-go-bqxs`) — eliminates transparency in place; needed because PDF/A-1 (ISO 19005-1) forbids it entirely (`ValidatePDFA`'s `TRANSPARENCY` issue, PDF/A-1 only — `pdfaCheckTransparency`/`pdfaDictHasTransparency` in validate_pdfa.go, reused here as-is). Mirrors Aspose.PDF for .NET's `Document.FlattenTransparency()`. Scope (v1): **whole-page rasterization** — a page whose `/Resources` graph (walked recursively through Form XObjects/Patterns via `collectValueDepsDoc`, same as imposition's resource-copy machinery) or own `/Group` carries a transparency group, `/SMask`, a non-Normal blend mode, or `/ca`/`/CA` < 1 is rendered to an opaque raster and its `/Contents` replaced with a single full-page image; a page without any of those is left completely untouched (still vector, still searchable). Not a sub-region approach (render only the transparent bit, splice it in, leave the rest of the page as-is) — rejected because nothing in the renderer separates a transparency region's ops from surrounding vector ops in z-order; see the design doc for the full reasoning. Out of scope: annotation-level transparency (own opacity, or transparency inside `/AP`) — detection only walks page content + `/Resources`, not `/Annots`, so annotations keep rendering live unchanged; call `Annotation.Flatten()`/`Form.Flatten()` first to bake those into page content (where this pass then does catch them) if that's also needed. `FlattenTransparencyOptions{DPI, Pages}` (DPI 0 → `DefaultDPI`; Pages empty → every page). Mechanics: `renderContentForFlatten` rasterizes content only (a new `skipAnnotations` renderer flag, alongside `suppressText`/`hideFormWidgets`) in the page's own **unrotated** coordinate space — `RenderImage` bakes `/Rotate` into its output for direct display, but a raster placed back via `cm`+`Do` must not have rotation baked in twice, since the page's untouched `/Rotate` still applies once at display time (regression-tested, `TestFlattenTransparencyPreservesRotation`: rotation applied exactly once, not zero or twice — a small tolerance, not exact byte equality, since `deviceMatrix` rounds the embed-time pixel dimensions and the image is then rescaled to the box's exact point dimensions on redisplay, causing a fraction of a percent of ordinary sub-pixel resampling noise at content edges, same as any rasterize-then-redisplay pipeline). The image is PNG-encoded (always opaque by construction, so Go's encoder drops the alpha channel and no `/SMask` is reintroduced) and its registration into a fresh `/Resources` is fully prepared — including via `createImageXObject`, the same helper `AddImage` uses — before the page dict is touched at all, so a failure partway through never leaves `/Resources` wiped with `/Contents` still pointing at names that no longer exist; `replacePageContents` (shared with the redaction pipeline) then swaps in one new `/Contents` stream, and any page-level `/Group` is deleted (its content has been rasterized away — code review caught an earlier version that detected a page-level group but never cleared it, so `ValidatePDFA` kept flagging `TRANSPARENCY` and a second `FlattenTransparency` call kept re-flattening the same page forever; `TestFlattenTransparencyClearsPageGroup` regression-tests both). Calls `RemoveUnusedObjects` once at the end when it flattened at least one page — found necessary during testing: `pdfaCheckTransparency` scans every object in the document, not just reachable ones, so an orphaned ExtGState left behind by a flattened page would still trip `TRANSPARENCY` otherwise. Design: `docs/superpowers/specs/2026-09-23-flatten-transparency-design.md`
- `PageFormat` struct — Width, Height in points; predefined: `PageFormatA3`, `PageFormatA4`, `PageFormatLetter`, `PageFormatLegal`
- `(PageFormat).Landscape()` — returns the format with width and height swapped
- `NewDocument(width, height) *Document` — creates a single-page blank document with given dimensions
- `NewDocumentFromFormat(format) *Document` — creates a single-page blank document from a predefined page format
- `(*Document).AddBlankPage(width, height) error` — appends a blank page with given dimensions
- `(*Document).AddBlankPageFromFormat(format) error` — appends a blank page from a page format
- `(*Document).InsertBlankPage(position, width, height) error` — inserts a blank page at a 1-based position
- `(*Document).InsertBlankPageFromFormat(position, format) error` — inserts a blank page from a page format at a position
- `Font` — interface implemented by standard 14 fonts and embedded TTF fonts; has `BaseFont()` and `IsEmbedded()` methods
- Standard 14 PDF fonts as package-level `Font` vars: `FontHelvetica`, `FontHelveticaBold`, `FontHelveticaOblique`, `FontHelveticaBoldOblique`, `FontTimesRoman`, `FontTimesBold`, `FontTimesItalic`, `FontTimesBoldItalic`, `FontCourier`, `FontCourierBold`, `FontCourierOblique`, `FontCourierBoldOblique`, `FontSymbol`, `FontZapfDingbats`
- `FindFont(name) (Font, error)` — returns a standard 14 `Font` by PostScript name (case-insensitive); error for unknown names
- `(*Document).LoadFont(path) (Font, error)` — reads a TrueType (`.ttf`) **or** OpenType-CFF (`.otf`) file, embeds it into the document, returns a `Font` usable in `TextStyle.Font`. TTF embeds as `CIDFontType2` + `/FontFile2`; OTF embeds as `CIDFontType0` + `/FontFile3` (Subtype `OpenType`, full sfnt) with no `/CIDToGIDMap` — for a non-CID-keyed CFF the Identity-H glyph IDs are used directly as the CID/GID (ISO 32000-1 §9.7.4.2). CID-keyed CFF (carrying a ROS) is rejected (`embedCFFFont`/`buildFontFile3Stream` in `font_embed.go`). `SubsetFonts` only subsets TrueType — embedded OTF keeps the full `/FontFile3`
- `(*Document).LoadFontFromStream(r) (Font, error)` — like `LoadFont` but reads from an `io.Reader`
- `(*Document).LoadFontByName(family string, bold, italic bool) (Font, error)` — resolves a font by **family name** (not a path) through the `FontRepository` — folders/files registered via `AddFontFolder`/`AddFontFile` first, then the OS font directories — and embeds the matched face, returning a `Font` for `TextStyle.Font` (`font_byname.go`). The by-name counterpart to `LoadFont`, mirroring Aspose.PDF for .NET's `FontRepository.FindFont`. Resolution (`resolveNamedRef`): exact PostScript name, then the **literal family** at the requested style/regular, then the Standard-14 alias expansion (Helvetica→Arial, …) — the literal family is tried first so "DejaVu Sans" doesn't resolve to Arial via the "sans" alias. A `.ttc` collection sub-font is re-wrapped as a standalone sfnt before embedding (`standaloneSFNT` copies the sub-font's table directory through `assembleSFNT`, since a collection can't be one `/FontFile2`); single-font `.ttf`/`.otf` embed their bytes verbatim through the normal `loadFontFromBytes` pipeline (so `SubsetFonts` still trims a by-name TTF). Returns an error if no matching font is registered or installed. Unlike the renderer's opt-in system substitution, an explicit by-name request always consults the OS fonts
- `(*Document).SubsetFonts() (int, error)` — rebuilds every embedded TTF (`LoadFont`) to keep only the glyphs actually drawn, returning the number of fonts subsetted. Call once, after all text is added and before Save/WriteTo (later text could reference dropped glyphs). Typical reduction ~300-700 KB/font → a few KB. Implementation (`font_subset.go`): tracks used glyph IDs as the text encoders emit them, takes the transitive closure over composite-glyph components, renumbers the kept glyphs into a compact range, rebuilds `glyf`/`loca`/`hmtx`/`maxp`/`head`/`hhea` + a synthetic `post` 3.0 (drops `cmap`/`OS/2`/`name`/hinting), and reassembles the sfnt with correct checksums. Original glyph IDs are preserved as CIDs via a generated `/CIDToGIDMap` stream (was `/Identity`), so no content stream is rewritten; `/W` and `/ToUnicode` are trimmed to the used glyphs. Mirrors the post-processing shape of `OptimizeImages`
- `(*Document).CreateType3Font() *Type3Font` (`type3_font.go`) — **Type3 font authoring** (epic `pdf-go-nul`; a capability Aspose.PDF for .NET does not expose): define custom glyphs with the full vector drawing API and use the font in `TextStyle.Font`. `(*Type3Font).AddGlyph(r rune, width float64) (*Page, error)` returns a detached glyph canvas in glyph space (1000 units/em, baseline y=0, MediaBox [0,-300..1000,1000]; the XForm detached-canvas mechanism) — draw with any `Draw*`/`AddImage` ops; `width` is the advance in 1/1000 em. On first use the font freezes: canvases harvest into `/CharProcs` streams (`d0` + content), canvas resources merge into the font `/Resources`, `/Encoding /Differences` carries **uniXXXX glyph names** and a generated `/ToUnicode` bfchar CMap maps the single-byte codes (1..255, max 255 glyphs) — so authored text extracts and searches; the built-in renderer draws it through the existing Type3 path. Unmapped runes are skipped when drawing. Read side (extraction+rendering of existing Type3 fonts) shipped earlier; corpus survey: only 2 documents use Type3, both extract with 0% replacement runes
- `HAlign` — `HAlignLeft`, `HAlignCenter`, `HAlignRight`
- `VAlign` — `VAlignTop`, `VAlignMiddle`, `VAlignBottom`
- `TextStyle` struct — Font, Size, Color, Background, HAlign, VAlign, LineSpacing, Underline, Strikethrough, Rotation, Skew (synthetic oblique — per-line sheared text matrix, faux italic for fonts without an italic face), Behind, Invisible (text rendering mode `3 Tr`, ISO 32000-1 §9.3.6 — glyphs paint nothing but stay selectable/searchable/extractable; Underline/Strikethrough decorations are suppressed with it; the primitive under `ai.OcrCopilot.MakeSearchable`'s hidden OCR layer)
- `(*Page).AddText(text, style, rect) error` — draws text inside a rectangle with word wrap, alignment, clipping, optional underline/strikethrough, rotation, and behind-content mode. **RTL / bidirectional** (`bidi.go`, epic `pdf-go-emak` phase 1): when the text is `TextStyle.RTL` or contains any strong right-to-left character, each wrapped line is reordered into visual order by a pragmatic Unicode Bidi Algorithm (UAX #9: classes W1–W7 / N1–N2 / I1–I2 over a single line, L2 reordering, L4 mirroring) before glyph emission, and an RTL base paragraph defaults to right alignment. This runs entirely in the layout layer — the encoder and renderer are untouched. **Arabic contextual shaping** (`arabic_shape.go`, phase 2) runs first, on the logical string: `shapeArabic` maps each letter to its Unicode Presentation Forms-B glyph (isolated / initial / medial / final by joining context) and forms the mandatory lam-alef ligatures, so a font that covers Forms-B (e.g. DejaVu Sans) renders connected Arabic. Hebrew and Arabic both render correctly (verified visually: connected Arabic, right-aligned, with numbers kept LTR). Out of scope for the bidi layer: explicit embedding/isolate codes, the bracket-pair rule N0, the L1 trailing-whitespace reset. **OpenType shaping** (`opentype.go` / `shape.go` / `text_shape.go` / `combining.go` / `arabic_joining.go` / `normalize_data.go`, phase 3, epic `pdf-go-26u4`; spec `docs/superpowers/specs/2026-09-10-opentype-shaping-design.md`): an embedded font (`LoadFont`) carrying GSUB/GPOS is shaped by a pure-Go OpenType engine. The reader (`parseOTLayout`, lazily cached as `ttfFont.layout()`) reads GDEF (glyph classes, mark attachment classes, mark glyph sets), GSUB types 1–8 and GPOS types 1–9 (extensions resolved at parse time) defensively — a malformed subtable is dropped, never fatal; lookup types/subtable counts/features/scripts match fontTools on DejaVu, Amiri, Noto Naskh and Noto Sans Arabic. The engine (`shapeRun`) follows HarfBuzz: a logical-order buffer, per-glyph feature masks, lookups applied in lookup-list order, LookupFlag + GDEF + mark-filtering-set skipping (`otMatcher`), one context/chain-context matcher for GSUB 5/6 and GPOS 7/8 with nested-lookup position bookkeeping (`applyNested`), ligature component tracking for mark-to-ligature, and cursive/mark attachment recorded as chains then resolved into absolute offsets (`resolveAttachments`, direction-aware advance compensation). Per run: `normalizeForFont` decomposes characters the font lacks, puts marks in canonical order using HarfBuzz's modified Hebrew/Arabic classes (dagesh before vowels, shadda before fatha) and recomposes what the font has (UCD tables generated into `combining.go` / `normalize_data.go`); Arabic joining masks come from the UCD joining types (`arabic_joining.go`, so Persian/Urdu/Syriac/N'Ko letters join); feature plans are Arabic `ccmp locl isol init medi fina med2 fin2 fin3 rlig rclt calt liga mset` + `curs kern dist abvm blwm mark mkmk`, default `ccmp locl liga clig calt rclt rlig` + `kern dist abvm blwm mark mkmk`; default-ignorable characters end as zero-advance space glyphs. **Glyph-for-glyph identical to HarfBuzz** (uharfbuzz) on a 54-string Arabic/Persian/Urdu/Hebrew/Latin differential suite across those four fonts. Wiring in `renderTextInBuilder`: wrapping stays rune-based; each wrapped line is split into bidi level runs (`bidiLevelRuns`, RTL runs mirrored), each run shaped in logical order and reversed, and `shapedLine.showOps` emits a plain `Tj` when the shaped positions agree with the font's rounded `/W` widths (sub-thousandth residuals are carried, so unkerned text is byte-identical to the old path), `TJ` kerning numbers where they differ, and `Ts` text rise around off-baseline marks. The Forms-B pass is skipped when the font's GSUB shapes Arabic itself (`fontShapesArabic`). **Shaped text stays extractable**: a substituted glyph claims a `/ToUnicode` entry for its text (`embeddedFont.claimGlyphText` — ligatures and contextual forms, allowed to override presentation-form code points such as U+FB03; folded in before serialization by `refreshShapedText` in `assemble` and by `SubsetFonts`), while glyphs that cannot carry their text through `/ToUnicode` — a glyph already nominal for another character (Noto Naskh draws zain as its reh glyph plus a dot), decomposition pieces with no character of their own, hidden joiners — are wrapped in `/Span <</ActualText …>> BDC … EMC`. MuPDF renders of Amiri/Noto/DejaVu samples match ours, and the showcase's RTL block is pixel-identical before and after. Fonts without layout tables, Standard-14 and Type3 fonts keep the rune-by-rune path. Out of scope: the Indic/Khmer/Myanmar/Hangul reordering shapers, vertical text, alternate-glyph selection, device/variation tables
- `(*Document).AddTextWatermark(text, style, pageNums...) error` — applies a text watermark to all or selected pages using full-page rectangle from MediaBox

**`stamp.go`** — stamp/overlay family; mirrors Aspose.PDF for .NET's `Aspose.Pdf.Stamp` hierarchy
- `Stamp` — interface implemented by `TextStamp`, `ImageStamp`, `PageNumberStamp`, `PdfPageStamp` (unexported `applyToPage`, so only these types satisfy it)
- Shared (embedded `stampBase`) exported fields mirroring Aspose `Stamp`: `Rect Rectangle` (zero = whole page MediaBox), `HAlign`/`VAlign` (placement within `Rect`), `Opacity` (0..1, zero treated as 1), `RotateAngle` (degrees CCW, **pivots about the `Rect` centre**), `Background bool` (draw behind page content — Aspose `Stamp.Background`)
- `TextStamp` — `Value string`, `TextStyle`; `NewTextStamp(text, style) *TextStamp`. Reuses `AddText` (wrap/align/font); opacity folds into the colour alpha, rotation is centre-pivoted via `centerPivotRect`
- `ImageStamp` — PNG/JPEG stretched to `Rect`; `NewImageStamp(path) (*ImageStamp, error)`, `NewImageStampFromStream(r) (*ImageStamp, error)`. `(*Page).drawImageStamp` emits a centre-pivoted, opacity-wrapped (`ensureExtGState`) image draw, prepended when `Background`
- `PageNumberStamp` — `Format` (`"{0}"`=current page, `"{1}"`=total; empty = `"{0}"`), `StartingNumber` (page 1 shows it; page n shows `StartingNumber-1+n`), `TextStyle`; `NewPageNumberStamp(format, style) *PageNumberStamp`. Renders the correct number per page
- `PdfPageStamp` (`stamp_pdfpage.go`) — stamps a **page from another PDF document** as an overlay/underlay; `NewPdfPageStamp(srcDoc, srcPageNum) (*PdfPageStamp, error)`. The source page is imported once as a Form XObject (reusing imposition's `importPageAsXObject` + `importGraph`) into the target document, registered on the page, and drawn into `Rect` scaled to **fit preserving aspect**, positioned by `HAlign`/`VAlign`, rotated about the rect centre (`RotateAngle`), at `Opacity`, in front or behind (`Background`). `drawFormStamp` composes the fit + centre-pivot rotation with `matMul`/`translateMatrix`. Mirrors Aspose.PDF for .NET's `PdfPageStamp`
- `(*Page).AddStamp(s Stamp) error` — mirrors `Page.AddStamp`; `(*Document).AddStamp(s Stamp, pageNums...) error` — apply to all/selected pages (headers/footers/page numbers/watermarks)
- `(*Document).Form() *Form` — returns the document's AcroForm (always non-nil; empty form for documents without /AcroForm)
- `(*Page).Annotations() *AnnotationCollection` — returns the page's annotation collection (always non-nil; empty for pages with no /Annots)
- `(*Document).ApplyRedactions() error` — destructively removes content (text glyphs, image XObjects, paths) inside every `/Redact` annotation's regions, draws overlay text/fill, then deletes the redact annotations
- `(*Document).ValidateRedactions() error` — pre-flight parseability check on redact-bearing pages; recommended before `ApplyRedactions`

**`vector.go` / `vector_draw.go`**
- `LineCap` enum — `LineCapButt` (default), `LineCapRound`, `LineCapSquare`. PDF operator J. (Shared with `appearance_builder.go` annotation drawing.)
- `LineJoin` enum — `LineJoinMiter` (default), `LineJoinRound`, `LineJoinBevel`. PDF operator j.
- `LineStyle` struct — `Color *Color`, `Width float64`, `DashPattern []float64`, `DashPhase float64`, `Cap`, `Join`, `MiterLimit float64`. Width ≤ 0 → no stroke. Mirrors Aspose.PDF for .NET's GraphInfo stroke fields.
- `ShapeStyle` struct — embeds `LineStyle` + adds `FillColor *Color`, `FillPattern string` (internal SVG pattern-name hook), `FillGradient Gradient` (public linear/radial gradient fill), and `FillTiling *TilingPattern` (public repeating tiling fill). Precedence on fill: `FillTiling` → `FillGradient` → `FillPattern` → `FillColor`. Either stroke or fill (or both) may be configured; if neither, draw call is a no-op (`paintOp` counts a tiling/gradient/pattern fill as a fill).
- `Path` — opaque fluent builder. `NewPath().MoveTo(x, y).LineTo(x, y).CurveTo(c1x, c1y, c2x, c2y, x, y).QuadTo(cx, cy, x, y).Arc(cx, cy, r, startAngle, sweepAngle).Close()`. Arc decomposes into ≤4 cubic Beziers per the Goldapp formula (k = (4/3)·tan(θ/4)).
- `(*Page).DrawLine(from, to Point, style LineStyle) error` — single line segment.
- `(*Page).DrawRectangle(rect Rectangle, style ShapeStyle) error` — axis-aligned rect, stroke and/or fill.
- `(*Page).DrawRoundedRectangle(rect Rectangle, radius float64, style ShapeStyle) error` — radius auto-clamped to half-shorter-side.
- `(*Page).DrawCircle(center Point, radius float64, style ShapeStyle) error` — 4-Bezier approximation (kappa = 0.5522847498).
- `(*Page).DrawEllipse(center Point, rx, ry float64, style ShapeStyle) error` — axis-aligned ellipse.
- `(*Page).DrawPolyline(points []Point, style LineStyle) error` — open path, stroke-only. Errors if len(points) < 2.
- `(*Page).DrawPolygon(points []Point, style ShapeStyle) error` — closed path, stroke and/or fill. Errors if len(points) < 3.
- `(*Page).DrawPath(path *Path, style ShapeStyle) error` — arbitrary path. Errors on nil path.
- `TilingPattern` (`vector_tiling.go`) — public repeating tiling fill (PatternType 1, colored/PaintType 1). `(*Document).CreateTilingPattern(width, height) *TilingPattern` → draw the cell via `(*TilingPattern).Canvas() *Page` (the full drawing API, reusing the XForm detached-canvas mechanism), optionally `(*TilingPattern).SetStep(xstep, ystep)` for gaps/overlap (default = cell size), then fill any shape by setting `ShapeStyle.FillTiling`. `(*Page).resolveShapeTiling` (called from `resolveShapeGradient`, so every Draw* site picks it up) builds the `/Pattern` stream once (`ensureBuilt`), registers it in the page `/Resources/Pattern` (`registerPatternRef` → `/P0`, `/P1`, …) and stores the name in `FillPattern`, so the existing `/Pattern cs … scn` emission paints it. The renderer (`render_tiling.go`) already executes the cell per tile. Limitation: PaintType 2 (uncolored, fill-colour-inheriting) tiles not exposed
- `Gradient` interface (`LinearGradient` / `RadialGradient`) + `GradientStop{Offset, Color}` (`vector_gradient.go`) — public gradient fills usable as `ShapeStyle.FillGradient` on every fill-capable Draw call. Constructors `NewLinearGradient(x1,y1,x2,y2, stops...)` and `NewRadialGradient(cx,cy,r, stops...)` (focal = centre); set `RadialGradient.FX/FY` for an off-centre highlight. Coordinates are PDF user space. Reuses the SVG shading machinery: `(*Page).resolveShapeGradient` adapts the public type to an internal `svgGradient` and calls `ensurePatternResource` (Type 2 axial / Type 3 stitched / Type 3 radial shading pattern) with an identity matrix; the resulting `/Pattern` resource name is stored in `FillPattern`, so the existing `/Pattern cs … scn` emission paints it. Limitations (mirror SVG Phase 3a): spread = pad, per-stop alpha not rendered (DeviceRGB shadings).
- Alpha (`Color.A < 1`) for stroke and fill is rendered via the existing `ensureExtGState` (now sets both `/CA` stroke alpha and `/ca` fill alpha). Distinct stroke vs. fill alpha values in the same shape: takes the more-restrictive value (single ExtGState per draw call). For per-property precision, use separate draw calls.
- Coordinates are PDF user space (Y up, origin at page bottom-left). Drawing outside the page is allowed; PDF viewers clip to MediaBox.

**`svg.go` / `svg_parse.go` / `svg_render.go` / `svg_path.go` / `svg_transform.go` / `svg_viewbox.go` / `svg_attrs.go` / `svg_types.go` / `svg_named_colors.go` / `vector_emit.go`**
- `(*Page).AddSVG(path, rect)` — reads an SVG file and renders it into the given rectangle on the page; unsupported elements are skipped silently
- `(*Page).AddSVGFromStream(r io.Reader, rect)` — io.Reader variant
- `(*Page).AddSVGObject(svg *SVG, rect)` — renders a pre-parsed `*SVG`
- `(*Document).LoadSVG(path) (*SVG, error)` — parse once, reuse on many pages
- `(*Document).LoadSVGFromStream(r io.Reader) (*SVG, error)` — io.Reader variant
- `(*Document).AddSVGWatermark(path string, pageNums ...int) error` — watermark on all (when pageNums empty) or selected pages; uses each page's full MediaBox honoring SVG `preserveAspectRatio`
- `(*Document).AddSVGWatermarkFromStream(r io.Reader, pageNums ...int) error` — io.Reader variant
- `(*Document).AddSVGObjectWatermark(svg *SVG, pageNums ...int) error` — pre-parsed watermark
- `SVG` — opaque pre-parsed type returned by `LoadSVG` / `LoadSVGFromStream`
- `(*SVG).ViewBox() (x, y, w, h float64)` — viewBox attribute or `(0, 0, intrinsicW, intrinsicH)` fallback
- `(*SVG).Size() (width, height float64)` — intrinsic dimensions from `<svg width=... height=...>` attrs
- **Supported in Phase 2**: basic shapes (`<rect>`/`<circle>`/`<ellipse>`/`<line>`/`<polyline>`/`<polygon>`/`<path>`); full SVG 1.1 path syntax (M/L/H/V/C/S/Q/T/A/Z + lowercase relatives) with elliptical-arc decomposition into cubic Béziers; transforms (`translate`/`rotate`/`scale`/`matrix`/`skewX`/`skewY`); `viewBox` + all 10 `preserveAspectRatio` modes with Y-flip; presentation attrs + inline `style="..."`; hex (3/6/8-digit), `rgb()`/`rgba()`, 147 CSS named colors, `none`/`transparent`/`currentColor`; absolute length units (px/pt/pc/mm/cm/in); group inheritance cascade (resolved at parse time)
- **Added in Phase 3a**: `<linearGradient>` and `<radialGradient>` rendering via PDF Type 2 (axial) / Type 3 (radial) shading patterns. Supports `<stop>` (offset numeric/percent, stop-color, stop-opacity), `gradientUnits` (both `userSpaceOnUse` and `objectBoundingBox`), `gradientTransform` (full matrix), `spreadMethod=pad`. Multi-stop gradients use Type 3 stitching combining Type 2 exponential interpolations. `fill="url(#id)"` and `stroke="url(#id)"` resolved at render time; missing refs fall back to no fill (best-effort).
- **Added in Phase 3b**: `<text>` and `<tspan>` rendering with mixed content (CharData + `<tspan>` + CharData), cursor-based positioning, `dx`/`dy` offsets, absolute `x`/`y` override on `<tspan>`, `text-anchor` (start/middle/end), `font-family`/`font-size`/`font-weight`/`font-style` attributes. Font matching: built-in heuristic mapping Standard 14 keywords (Arial/Helvetica → FontHelvetica, Times → FontTimesRoman, Courier → FontCourier, plus bold/italic variants); pluggable `SVGFontResolver` callback via `(*Document).SetSVGFontResolver` for embedded TTF fonts (Cyrillic etc.). Gradient fills (Phase 3a) work on text via the same `/Pattern cs` mechanism.
- `SVGFontResolver` — `func(family string, bold, italic bool) Font` — callback signature for font resolution
- `(*Document).SetSVGFontResolver(fn SVGFontResolver)` — register a custom resolver; the renderer queries it before falling back to the heuristic; pass `nil` to revert
- **Added in Phase 3c**: `<image>` (data:image/png and data:image/jpeg base64 inline — external URLs silently skipped); `<defs>`/`<use>`/`<symbol>` (reusable elements with parse-end deep-clone resolution; forward refs supported; cycle detection); `<clipPath>` (children = shape elements; `clipPathUnits` userSpaceOnUse + objectBoundingBox; multi-child union; maps to PDF `W`/`W*` operators); `clip-path="url(#id)"` presentation attribute on any shape/text/image.
- **Added in Phase 3d**: practical SVG completion — `<mask>` via PDF soft masks (Form XObject `/Group /S /Transparency` + ExtGState `/SMask`, supporting `maskUnits` and `maskContentUnits`); CSS `<style>` blocks with `.class`/`#id`/element selectors (specificity: inline > id > class > type); `<filter>` with `feDropShadow` emulated as offset+alpha bbox duplicate (no blur — PDF has no native Gaussian blur, other filter primitives silently skipped); `<marker>` (`marker-start`/`marker-mid`/`marker-end`) with `orient=auto` rotation along path tangent, `refX`/`refY` anchor, `markerUnits=strokeWidth`+`userSpaceOnUse`.
- **Out of scope (Phase 3e)**: `<textPath>`, vertical writing modes, `xml:space="preserve"`, em/ex/% length units, `spreadMethod=reflect/repeat`, CSS descendant/pseudo/attribute selectors, real Gaussian blur (requires rasterizer), external `href` in `<image>`, `data:image/svg+xml`
- **Best-effort error policy**: unsupported elements skipped silently; only XML parse failures and invalid numeric attrs surface as errors
- `vector_emit.go` — internal `emit*ToBuf` helpers extracted from Phase 1 `(*Page).Draw*` methods so SVG renderer can reuse the exact byte-emission code (PDF output is byte-identical to hand-written Phase 1 calls)

**`page_labels.go`** — page label support
- `(*Page).Label()` — formatted page label from the document's `/PageLabels` number tree; falls back to decimal page number if absent
- Supported styles: `/D` decimal, `/r`/`/R` roman, `/a`/`/A` alphabetic; optional `/P` prefix and `/St` start value
- `(*Document).SetPageLabels([]PageLabelRange) error` / `(*Document).ClearPageLabels()` — author the `/PageLabels` number tree so viewers show logical labels (front matter i, ii, then body 1, 2, …); ranges must be sorted ascending with the first at page 1; round-trips through Save and is read back by `Label()`. Mirrors Aspose.PDF for .NET's `Document.PageLabels` / `PageLabel`
- `PageLabelRange` struct — `StartPage` (1-based), `Style PageLabelStyle`, `Prefix string`, `StartNum int`. `PageLabelStyle` enum — `PageLabelStyleNone`, `PageLabelDecimal`, `PageLabelRomanLower`, `PageLabelRomanUpper`, `PageLabelAlphabeticLower`, `PageLabelAlphabeticUpper`

**`page_range.go`**
- `PageRange` struct — From, To (1-based, inclusive)

**`metadata.go`** — Info-dictionary metadata. Naming note: this is the PDF `/Info` dictionary and mirrors Aspose.PDF for .NET's `Document.Info` (`DocumentInfo`). In Aspose.PDF for .NET, `Document.Metadata` is the *XMP* store — which here is `(*Document).XMP` (see `xmp.go`), not this type. We deliberately name the Info surface `Info`/`DocumentInfo` (not `Metadata`) to avoid that collision.
- `(*Document).Info() (DocumentInfo, error)` — returns the Info-dictionary metadata read from live in-memory state
- `(*Document).SetInfo(info)` — replaces the Info dictionary in memory; full replacement, empty fields omitted
- `(*Document).ClearInfo()` — removes the Info dictionary; applied on Save/WriteTo
- `DocumentInfo` struct — Title, Author, Subject, Keywords, Creator, Producer, CreationDate, ModDate, Custom map[string]string

**`xmp.go`** — XMP metadata (the `/Catalog/Metadata` RDF/XML packet, ISO 32000-1 §14.3.2)
- `(*Document).XMP() (XMPMetadata, error)` — parse the XMP packet; zero value (`IsEmpty()`) when absent
- `(*Document).SetXMP(meta XMPMetadata) error` — serialise + store a standard packet (uncompressed `/Type /Metadata /Subtype /XML`); reuses the existing object on update
- `(*Document).ClearXMP()` — remove `/Catalog/Metadata` (orphans the stream; `RemoveUnusedObjects` reclaims it)
- `(*Document).XMPRaw() ([]byte, error)` / `(*Document).SetXMPRaw([]byte) error` — raw packet bytes escape hatch
- `(*Document).SyncInfoToXMP() error` — build an XMP packet from the current `/Info` dict so both metadata stores agree (PDF date → ISO 8601)
- `XMPMetadata` struct — Title (dc:title), Authors []string (dc:creator Seq), Description (dc:description), Keywords []string (dc:subject Bag), CreatorTool (xmp:CreatorTool), Producer (pdf:Producer), CreateDate/ModifyDate/MetadataDate (xmp:*, ISO 8601 strings), Custom []XMPProperty
- `XMPProperty` struct — Namespace, Prefix, Name, Value for arbitrary simple namespaced properties. Parser handles both attribute and element forms and rdf:Alt/Seq/Bag containers; `encoding/xml` does not surface the original prefix so a custom property's Prefix is synthesised on read (Namespace/Name/Value round-trip)

**`encrypt.go` / `decrypt.go` / `encrypt_aes.go` / `decrypt_aes.go` / `encrypt_aes256.go` / `decrypt_aes256.go`**
- `Encrypt(inputPath, outputPath, userPassword, ownerPassword)` — top-level helper writes RC4-128-protected PDF (PDF 1.4 Standard Security Handler V=2 R=3). For AES, use `(*Document).SetEncryption(EncryptionOptions{...})`
- `ErrEncrypted` — sentinel error from `Open`/`OpenStream` on encrypted input
- Decryption pipeline: `OpenWithPassword`/`OpenStreamWithPassword` parse `/Encrypt`, dispatch by `/V` (V=2 R=3 → RC4 path; V=4 R=4 → AES-128 path via `/CFM /AESV2`; V=5 R=6 → AES-256 path via `/CFM /AESV3` per ISO 32000-2). All paths share PKCS#7 helpers. For V≤4 password handling reuses Algorithms 2/5/7 (MD5-based); for V=5 R=6 password handling uses Algorithm 2.B (iterated SHA-256/384/512 hash chain). Per-object decryption uses Algorithm 1 (RC4) or 1.A (AES-128, with `"sAlT"` literal suffix in MD5 input); AES-256 uses the FEK directly (no per-object derivation). Stream `/Filter` chains are re-applied after decryption per PDF spec ordering (encrypt-after-filter)
- `Permissions` struct — eight bool flags (AllowPrint, AllowModify, AllowCopy, AllowAnnotations, AllowFormFill, AllowAccessibility, AllowAssembly, AllowPrintHighRes); zero value denies everything. Adobe-convention bit packing per ISO 32000-1 §7.6.3.2 Table 22 with reserved bits 7-8 and 13-32 set high
- `EncryptionOptions` struct — unified encryption configuration: UserPassword, OwnerPassword (empty → defaults to UserPassword), Permissions *Permissions (nil → grant all), Algorithm EncryptionAlgorithm (zero value → AES-128). Consumed by `(*Document).SetEncryption`
- `EncryptionAlgorithm` enum — `EncryptionAlgAES128` (default, AES-128 V=4 R=4 `/CFM /AESV2` per ISO 32000-1 §7.6.3.2), `EncryptionAlgRC4_128` (legacy V=2 R=3), `EncryptionAlgRC4_40` (the original handler V=1 R=2, `/Length 40`, `pdf-go-aqux` — for old readers and archives only: a 40-bit key falls to brute force in minutes; the revision-2 forms of Algorithms 2/3/4 were already implemented for *reading* such files, so writing them reuses them), `EncryptionAlgAES256` (AES-256 V=5 R=6 `/CFM /AESV3` per ISO 32000-2 §7.6.4; bumps PDF header to `%PDF-2.0` and includes /U /O /UE /OE /Perms entries with tamper-detection)
- AES-128 specifics: per-object key via `MD5(docKey || objNum_LE_3 || gen_LE_2 || "sAlT")[:16]` (Algorithm 1.A); AES-128-CBC with PKCS#7 padding and random 16-byte IV prepended to each encrypted string/stream. Single document-wide StdCF crypt filter; `/StmF` and `/StrF` both point to it
- **No `usePdf20` toggle.** Aspose.PDF for .NET's `Encrypt(..., bool usePdf20)` chooses between revision 5 (the deprecated Adobe extension) and revision 6 (ISO 32000-2). This library always writes R=6 for AES-256, so the flag could only select the weaker, withdrawn revision — it is deliberately absent (`pdf-go-aqux`)
- AES-256 specifics: random 256-bit File Encryption Key (FEK) is encrypted into /UE under user-derived key and /OE under owner-derived key; passwords are validated against /U / /O hashes computed by Algorithm 2.B; /Perms is an AES-256-ECB encrypted permissions block under FEK providing tamper-detection of /P. Per-object encryption uses FEK directly with AES-256-CBC + PKCS#7 + random 16-byte IV. PDF header bumped to `%PDF-2.0` per ISO 32000-2 requirement

**`pubsec.go`** — public-key security handler (ISO 32000-1 §7.6.4, `/Filter /Adobe.PubSec`; epic `pdf-go-dn5`): encrypt for X.509 recipients instead of a shared password. Mirrors the intent of Aspose.PDF for .NET's certificate encryption, in this library's options idiom
- `EncryptionOptions.Recipients []Recipient` (`Recipient{Certificate *x509.Certificate, Permissions *Permissions}`) switches `SetEncryption` to the public-key handler and defaults the algorithm to AES-256; `OpenWithCertificate(path, cert, key)` / `OpenStreamWithCertificate(r, cert, key)` open such a file, taking any `crypto.Decrypter` (an `*rsa.PrivateKey` satisfies it — no `.p12` needed), the read-side counterpart of `Sign`'s `crypto.Signer`
- Mechanics: a random 20-byte seed plus that recipient's 4 permission bytes (`pubsecPermissionBits` — bit 1 set, bits 7-8 clear, 13-32 set, the Acrobat/PDFBox convention) are sealed per recipient into a CMS EnvelopedData (`buildRecipientEnvelope`: RSA PKCS#1 v1.5 key transport + AES-CBC content encryption, hand-rolled on `encoding/asn1` next to the SignedData code, reusing `algorithmIdentifier`/`issuerAndSerial`/`contentInfo`). The **file key** is the digest of the seed followed by every recipient blob in `/Recipients` order — SHA-256 for AESV3, SHA-1 for AESV2 (`pubsecFileKey`) — so any recipient who recovers the seed derives the same key. Per-object encryption/decryption is then the unchanged standard-handler machinery, which is why the whole read/write pipeline is shared
- `/Encrypt` layout: `/Filter /Adobe.PubSec`, `/SubFilter /adbe.pkcs7.s5`, `/V 4` (AESV2) or `/V 5` (AESV3), no `/O` `/U` `/P` (permissions live in the envelopes), and `/Recipients` inside the `/DefaultCryptFilter` crypt filter; the reader also accepts `/Recipients` on the encryption dict itself and any crypt-filter name
- **Per-recipient permissions** are a genuine capability gain over the password handler; `Permissions()` reports the ones from the caller's own envelope. Opening a certificate-encrypted file re-saves for the same recipients (the parsed state is preserved verbatim), so edit-in-place works
- Validated: **OpenSSL** (`cms -decrypt -recip cert.pem -inkey key.pem`) recovers the payload byte-for-byte from our envelope; unit tests cover AES-128/AES-256 round-trip, multi-recipient, non-recipient and wrong-key rejection, per-recipient permissions and re-save. Out of scope: RC4 sub-filters s3/s4, EC recipients (key agreement), and RSAES-OAEP key transport

**`sign.go` / `pkcs7_sign.go` / `pkcs7_timestamp.go` / `sign_incremental.go` / `sign_verify.go` / `pkcs7_verify.go` / `appearance_sign.go`** — digital signatures (ISO 32000-1 §12.8; epic `pdf-go-bm9`). PKCS#7-detached signatures (`/adbe.pkcs7.detached` or PAdES `/ETSI.CAdES.detached`), SHA-2 or SHA-3 digests, RSA or ECDSA keys, over plain **or encrypted** documents; invisible by default or a visible appearance block; optional certification (DocMDP), RFC 3161 trusted timestamp, and **multiple signatures** via incremental update. Mirrors the intent of Aspose.PDF for .NET's `PdfFileSignature` + `SignatureCustomAppearance`
- `(*Document).Sign(opts SignOptions) error` — configures a signature applied on the next Save/WriteTo (like `SetEncryption`). `SignOptions` — `Certificate *x509.Certificate`, `PrivateKey crypto.Signer` (RSA/ECDSA — no `.p12` file needed; caller loads the key however) or `SignHash` for a key the process never sees, `Chain []*x509.Certificate`, `Reason`/`Location`/`ContactInfo`/`Name`, `SigningTime`. Signing an encrypted document is rejected
- `SignOptions.SignHash func(digest []byte, hash crypto.Hash) ([]byte, error)` (`pdf-go-7p2n`) — sign without holding the key: the callback is handed the digest and the hash that produced it and returns the raw signature (PKCS#1 v1.5 for an RSA certificate, ASN.1 ECDSA for an EC one), which is the shape a hardware module or a remote signing service offers. Internally it is wrapped in a `crypto.Signer` (`callbackSigner`, `Public()` answering from the certificate), so the CMS path stays single. Exactly one of `PrivateKey` and `SignHash` must be set and `Certificate` is required either way — the `SignerInfo` is built from it. A callback error surfaces from `Save`/`WriteTo` rather than producing a broken file. Mirrors the intent of Aspose.PDF for .NET's `CustomSignHash`
- `SignOptions.Digest DigestAlgorithm` — the hash for the message digest and the signature: `DigestSHA256` (zero value, what every earlier release wrote), `DigestSHA384`, `DigestSHA512`, `DigestSHA3_256`, `DigestSHA3_384`, `DigestSHA3_512`. Mirrors Aspose.PDF for .NET's `DigestHashAlgorithm`. SHA-3 comes from the standard library (`crypto/sha3`, blank-imported so the hashes register); the OID tables live in `pkcs7_sign.go` — digests under the NIST hashAlgs arc, and for the signature algorithm bare `rsaEncryption` with a SHA-2 digest (the long-proven form) but the explicit `id-rsassa-pkcs1-v1_5-with-sha3-*` / `id-ecdsa-with-sha3-*` identifiers for SHA-3, which not every validator recognises in the rsaEncryption pairing. ECDSA now names its digest correctly too (`ecdsa-with-SHA384/512`), where it used to write SHA-256 whatever the hash. The verifier reads the algorithm from the `SignerInfo` rather than assuming, so a SHA-3 signature round-trips. Validated with **OpenSSL** (`cms -verify` accepts our SHA3-256 signature) (`pdf-go-blch`)
- `SignOptions.PAdES bool` — emit an ETSI.CAdES.detached (PAdES baseline) signature: `/SubFilter /ETSI.CAdES.detached` + the ESS signing-certificate-v2 signed attribute (RFC 5035, `oidAttrSigningCertV2`)
- `SignOptions.Certify CertifyPermission` (`NotCertified`/`CertifyNoChanges`/`CertifyFillForms`/`CertifyAnnotations`, mirroring Aspose `DocMDPAccessPermissions`) — a certification (author/DocMDP) signature: the `/Sig` dict gets a `/Reference` with `/TransformMethod /DocMDP` + `/TransformParams /P`, and the catalog `/Perms/DocMDP` points at the signature. pyHanko reports `docmdp_ok`
- `SignOptions.TimestampURL string` — fetch an RFC 3161 timestamp from that TSA over the signature value and embed it as the signature-time-stamp **unsigned** attribute ([1] IMPLICIT SET), anchoring the time to a trusted clock (`pkcs7_timestamp.go`: `buildTimestampRequest` → POST `application/timestamp-query` → parse `TimeStampResp` → `marshalTimestampUnsignedAttr`). Validated end-to-end against DigiCert's production TSA; pyHanko reports `TIMESTAMP_TOKEN<INTACT:TRUSTED>`. Requires network at sign time
- `SignOptions.Incremental bool` + **multiple signatures** (`sign_incremental.go`) — signing writes an incremental update (the original bytes verbatim + an appended revision: new/modified objects, a classic xref section with `/Prev` → the previous one, a fresh trailer carrying the original `/ID`) instead of rewriting, so any earlier signature stays valid. Auto-enabled when the document already has a signature; requires a document opened from an existing PDF. Capture at open (`document.go`): `catalogNum` (`/Root`), `docID` (`/ID`), `origSize` (`/Size`), `encryptObjNum` (`/Encrypt`) — new objects start at `origSize` (the stripped `/Pages`/`/Catalog` numbers still occupy slots in the file). `buildIncrementalSignedPDF` adds the Sig dict + Sig field/widget (`uniqueSignatureFieldName` → `Signature1`, `Signature2`, …), modifies `/AcroForm/Fields` + the page `/Annots` + (for DocMDP/new AcroForm) the catalog in place by original number, then `applySignature` patches the placeholders. Validated with pyHanko: first signature `ENTIRE_REVISION` + `ACCEPTABLE_MODIFICATIONS`, second `ENTIRE_FILE` `UNTOUCHED`
- **Signing encrypted documents** — an encrypted document is always signed incrementally: the appended signature objects are encrypted with the document's own scheme (RC4-128 / AES-128 / AES-256) while the signature `/Contents` and `/ByteRange` stay plaintext (written as `pdfRaw`, per ISO 32000-1 §7.6.2). The reader side exempts a signature dict's `/Contents` from decryption (`isSignatureDict` in `decrypt.go`, applied across the RC4/AES-128/AES-256 decrypt walkers). A freshly-built encrypted document (no source) is signed by `buildFreshEncryptedSignedPDF`: serialize the encrypted PDF, reopen it (so it has source + preserved encryption), then sign incrementally — the only form pyHanko/Acrobat accept (full-rewrite-then-sign confuses revision-aware validators). The incremental trailer repeats `/Encrypt`. Validated with pyHanko across all three algorithms: `INTACT:TRUSTED,UNTOUCHED`, `ENTIRE_FILE`
- Visible signature: `SignOptions.Visible` + `Rect Rectangle` + `Page int` (mirrors Aspose `Sign(..., visible, rect, page)`) draw a generated appearance block ("Digitally signed by …", date, optional reason/location) via `generateSignatureAppearance` (`appearance_sign.go`) → an `/AP/N` Form XObject drawn with a Standard-14 font (so the built-in renderer shows it). `SignOptions.Appearance *SignatureAppearance` customizes content/style (`ShowName`/`ShowDate`/`ShowReason`/`ShowLocation`/`Font`/`FontSize`/`Color`; nil = full default block) — mirrors Aspose's `SignatureCustomAppearance`. The appearance does not affect the CMS (it is part of the signed bytes, written before the signature is computed)
- `(*Document).VerifySignatures() ([]SignatureVerification, error)` — verifies every `/Sig` field against the document's source bytes (stored on Open in `d.source`). `SignatureVerification` — `FieldName`, `SignerName`, `Reason`, `Location`, `SigningTime`, `Valid`, `IntegrityOK`, `CoversWholeDocument`, `Certificate *x509.Certificate`, `Chain []*x509.Certificate` (the other certificates the signer embedded — the intermediates needed to build a path; the signer is not repeated), `Err`. Trust-chain validation is the caller's (the certificates are returned)
- `(*Document).SignatureNames() []string` and `(*Document).VerifySignature(fieldName string) (SignatureVerification, error)` (`pdf-go-w3i5`) — mirror Aspose.PDF for .NET's `GetSignatureNames` and `VerifySignature(sigName)`. The error reports a missing field or missing source bytes; a signature that is present but invalid is not an error — it comes back in the result with the reason in `Err`, which is also why Aspose's throwing/non-throwing `VerifySignature`/`TryVerifySignature` pair collapses into one method here
- **Revocation and LTV** (`ocsp.go` / `crl.go` / `revocation.go` / `dss.go`, epic `pdf-go-x2s6`; design `docs/superpowers/specs/2026-09-16-signature-revocation-ltv-design.md`): `VerifySignatures`/`VerifySignature` take `ValidationOptions{Revocation, Fetcher, Timeout}` — `RevocationNone` (zero value) checks nothing, `RevocationOffline` uses only material the document carries (its `/DSS`), `RevocationOnline` also fetches from the certificate's OCSP responders and CRL distribution points. The result gains `Revocation *RevocationStatus{Status, Source, RevokedAt, Reason, ProducedAt, Err}` and `LTVEnabled bool` (the `/DSS` carries certificates plus a verifying revocation answer for this signer; mirrors Aspose's `IsLtvEnabled`). **`Valid` keeps its cryptographic meaning** — a revoked certificate never flips it. OCSP is hand-rolled on `encoding/asn1` (`buildOCSPRequest`, `parseOCSPResponse`, `parseOCSPAny` — which also accepts the bare `BasicOCSPResponse` a `/DSS /OCSPs` entry holds —, `verifyOCSPResponse`): a response counts only when signed by the issuer or by a delegated responder the issuer signed carrying `id-kp-OCSPSigning`, and an expired/future-dated answer or one about another certificate is never a "good". OpenSSL (`ocsp -reqin -req_text`) parses our requests. CRLs go through `x509.ParseRevocationList` plus issuer-signature and freshness checks (`checkCRL`). Fetching is behind `RevocationFetcher` (built-in transport: `DefaultRevocationFetcher`, 10 MB response cap), which is how tests stay offline. The issuer is found among the signature's embedded certificates (`issuerOf`). `(*Document).AddValidationInfo(opts...)` writes `/DSS` (`/Certs` `/OCSPs` `/CRLs` + `/VRI` keyed by the uppercase SHA-1 of the signature's `/Contents` octets) and `/Extensions /ADBE /ExtensionLevel 5` as **a new revision** through `appendRevision` — the revision writer now shared with incremental signing — and marks the document `revisionAppended`, so `WriteTo` emits those bytes verbatim instead of rebuilding. `SignOptions.LTV` signs, reopens and runs `AddValidationInfo` with `RevocationOnline` (`buildSignedWithLTV`). No independent `/DSS` validator is available locally (pyHanko absent). Out of scope: building a path to a trust anchor, document timestamps (PAdES B-LTA), OCSP nonces, delta/indirect CRLs
- The byte-range dance (`sign.go`): the writer emits fixed-width `pdfRaw` placeholders for `/Contents` (`<` + 16384 zero hex + `>`) and `/ByteRange` (`[0 0000000000 …]`); `buildSignatureObjects` adds the `/Sig` dict + invisible Sig field/widget (`Rect [0 0 0 0]`) into `/AcroForm/Fields` with `/SigFlags 3`; after serialization `applySignature` finds the placeholders, fills the real `/ByteRange` (the excluded gap is the whole `/Contents` value **including the `<>` brackets** — the convention validators expect), hashes the covered bytes, builds the PKCS#7, and splices the hex in-place without shifting offsets
- CMS is hand-rolled on `encoding/asn1` (stdlib has no PKCS#7): `buildPKCS7Detached` (SignedData with contentType/messageDigest/signingTime signed attributes; signature over the SET-OF-re-tagged attrs) and `verifyPKCS7Detached` (parse, recover signer cert by issuer+serial, check messageDigest against the hash the SignerInfo names, verify RSA PKCS#1v1.5 / ECDSA). Validated against **OpenSSL** (`cms -verify`) and **pyHanko** (`INTACT:TRUSTED,UNTOUCHED`, coverage `ENTIRE_FILE`) — invisible, visible, PAdES, DocMDP, RFC 3161-timestamped, multi-signature, and encrypted-document alike. Out of scope: image/logo in the appearance
- Test certs are generated in-memory (`crypto/x509`); **no private key is ever committed** (avoids secret-scanner findings)

**`outline.go` / `outline_parse.go` / `outline_write.go` / `outline_destination.go`**
- `(*Document).Outlines() *OutlineItemCollection` — root outline collection. Always non-nil; empty for documents without `/Outlines`. Lazy-parsed on first call. Mirrors Aspose.PDF for .NET's `Document.Outlines`
- `OutlineItemCollection` — recursive tree node (entry + children collection). Mirrors Aspose .NET's `OutlineItemCollection : IList<OutlineItemCollection>`
- Constructor: `NewOutlineItemCollection(doc *Document) *OutlineItemCollection` — unattached entry, must be added via `Add`/`Insert` to take effect. .NET equivalent: `new OutlineItemCollection(doc.Outlines)`
- Style accessors: `Title()/SetTitle`, `Bold()/SetBold` (`/F` bit 2), `Italic()/SetItalic` (`/F` bit 1), `Color()/SetColor` (`*pdf.Color`), `IsExpanded()/SetIsExpanded` (sign of `/Count`)
- Target accessors: `Action()/SetAction` (reuses `Action` from annotations), `Destination()/SetDestination`. Both may be set; per ISO 32000-1 §12.3.3 viewers honor `/Dest` first
- Tree: `Add(child) error`, `Insert(index, child) error`, `Remove(child) bool`, `RemoveAt(index) error`, `At(index) *OutlineItemCollection`, `Count() int`, `All() []*OutlineItemCollection`, `Parent() *OutlineItemCollection`. Errors on nil child, cross-document, cycle, or already-attached
- `Destination` interface + 8 concrete types per ISO 32000-1 §12.3.2.2: `DestinationXYZ`, `DestinationFit`, `DestinationFitH`, `DestinationFitV`, `DestinationFitR`, `DestinationFitB`, `DestinationFitBH`, `DestinationFitBV`. Each has `NewDestinationXxx(page *Page, ...)` constructor; XYZ/FitH/FitV/FitBH/FitBV also have `NewDestinationXxxUnchanged` variants that encode `/null` for "leave as-is"
- Pages in destinations referenced by `*Page` (resolved to underlying object number at write time). Lazy dict-backed reads with copy-on-mutate: parsed items read from their PDF dict directly; first `SetXxx` call materializes all values into struct fields
- Encryption-safe: outlines roundtrip cleanly under AES-128, AES-256, and RC4-128

**`named_destinations.go` / `named_destinations_parse.go` / `named_destinations_write.go`**
- `(*Document).NamedDestinations() *NamedDestinations` — name-to-destination collection. Always non-nil; empty for documents without `/Catalog/Names/Dests` or `/Catalog/Dests`. Lazy-parsed on first call. Mirrors Aspose.PDF for .NET's `Document.NamedDestinations`
- `NamedDestinations` — collection with `Add(name, dest) error`, `Get(name) Destination`, `Has(name) bool`, `Remove(name) bool`, `Count() int`, `Names() []string` (lex-sorted snapshot), `All() map[string]Destination` (snapshot), `Clear()`, `Document()`. Per ISO 32000-1 §12.3.2.3
- `NamedDestination` — 9th concrete `Destination` type wrapping a name reference; `DestinationType()` returns `DestinationTypeNamed`. Constructor `NewNamedDestination(doc, name)`. Lazy `Resolve() Destination` and `Page() *Page` look up in the collection at call time (forward references allowed). Mirrors Aspose .NET's `NamedDestination` subtype
- Read path: `/Catalog/Dests` legacy dict + `/Catalog/Names/Dests` modern name tree merged into one collection; on collision `/Names/Dests` wins. Name tree walker handles arbitrary `/Kids` depth with cycle protection
- Write path: emit `/Catalog/Names/Dests` as a flat single-root tree (valid for any size per ISO 32000-1 §7.9.6). Legacy `/Catalog/Dests` is dropped on save — automatic migration. Sibling `/Catalog/Names` subentries (JavaScript, EmbeddedFiles, etc.) are preserved through round-trip
- Outline integration: `OutlineItemCollection.SetDestination(NewNamedDestination(doc, name))` serializes as `/Dest <name>` PDF string; on parse, `Destination()` returns `*NamedDestination` wrapper. Unregistered names still wrap (preserves the reference) — `Resolve()` returns nil to signal missing

**`javascript.go` / `open_action.go`** — document-level JavaScript + open action; mirror Aspose.PDF for .NET's `Document.JavaScript` and `Document.OpenAction`
- `(*Document).JavaScript() *JavaScriptCollection` — document-level named scripts backed by the `/Catalog/Names/JavaScript` name tree. Always non-nil; lazy-parsed. Methods: `Add(name, script) error` (empty name rejected), `Get(name) string`, `Has(name) bool`, `Remove(name) bool`, `Names() []string` (lex-sorted), `Count() int`, `Clear()`. Each mutation rewrites the `/JavaScript` subentry of `/Catalog/Names` as a flat name-tree leaf via the shared `(*Document).namesDict()` helper, so it coexists with `/Names/Dests` (named destinations) and round-trips through Save. Distinct from action-level `JavaScriptAction` (which fires on an annotation/field event). Same security caveat — scripts run in the recipient's viewer on open
- `(*Document).OpenAction() Action` / `SetOpenAction(Action)` / `RemoveOpenAction()` — the `/Catalog/OpenAction` run when the document opens. Getter resolves a `/GoTo` destination's page ref to a 1-based `PageNum` (returns nil for a bare destination-array open-action); setter binds a `*GoToAction` to the document so its destination is written as a page reference, and `nil` clears the entry. `resolveGoToPage` is the shared GoTo page-resolution helper (also used by `LinkAnnotation.Action`)

**`toc.go`** — table-of-contents generation; loosely mirrors Aspose.PDF for .NET's `TocInfo`
- `(*Document).GenerateTOC(opts ...TOCOptions) (int, error)` — builds a TOC from the document's outline (bookmark) tree and inserts it as new page(s) at the **front**; outline nesting → indent level. Returns pages added (0 when there are no outlines). Page numbers and GoTo links reflect the final page order after insertion (targets captured as stable `*pdfObject`, resolved to numbers post-insert)
- `(*Page).AddTOC(entries []TOCEntry, rect Rectangle, opts ...TOCOptions) (int, error)` — renders a supplied entry list into `rect`; overflow auto-appends continuation pages (same size) and returns the count. Each entry with a non-nil `Page` gets its number + a borderless GoTo link
- `TOCEntry` struct — `Title string`, `Level int` (0 = top), `Page *Page` (nil = no number/link), `Label string` (overrides the displayed number with a logical label, e.g. a `/PageLabels` label or a body number that differs from the physical page index; the link still targets `Page`). The showcase's `addTOC` uses `Page.AddTOC` with `Label` for exactly this
- `TOCOptions` struct — `Title` (heading; empty = none), `TitleStyle`/`EntryStyle TextStyle`, `IndentStep` (per level, default 18), `LineSpacing` (default 1.6), `Margin` (GenerateTOC inserted pages, default 54), and the inverted toggles `NoPageNumbers`/`NoLeader`/`NoLinks` (zero value = numbers + dotted leader + links on). Internals: `drawTOCRow` (indented truncated title + right-aligned number + baseline-aligned dotted leader via `DrawLine`), `paginateTOC` (measure pass mirroring the render loop), `truncateToWidth` (ellipsis), built on `AddText` + `fontWidthAndAscent`/`measureString`

**`form.go` / `form_fields.go`**
- `Form` — AcroForm view; `Fields() []Field`, `Field(name string) Field`, `HasField(name string) bool`, `NeedAppearances() bool`, `SetNeedAppearances(v bool)`
- `Field` interface — `PartialName() string`, `FullName() string`, `Value() string`, `SetValue(s string) error`, `IsReadOnly() bool`, `IsRequired() bool`, `PageIndex() int`, `Rect() Rectangle`
- Concrete types: `TextBoxField`, `CheckboxField`, `RadioButtonField` + `RadioButtonOptionField`, `ComboBoxField`, `ListBoxField`, `ButtonField` (push button)
- `ChoiceOption` — option data for ComboBox / ListBox: `Value`, `Export`
- `FormFieldType` enum + `FieldType(f Field) FormFieldType` convenience helper
- Field values are encoded UTF-16BE-with-BOM when non-ASCII, Latin-1 / PDFDocEncoding otherwise (per ISO 32000-1 §7.9.2.2)
- Widget `/AP/N` is pre-generated on every AddXxx call and re-generated on every value-mutating setter (SetValue / SetChecked / SetSelected / AddOption / RemoveOption), so the form renders identically across Acrobat / Foxit / browser PDF viewers / MuPDF / Poppler without depending on `/AcroForm/NeedAppearances`. The flag defaults to false; `SetNeedAppearances(true)` is opt-in for callers who want viewer-side regeneration (one side-effect: Acrobat marks the document as modified on open). Appearance generators in `appearance_widget.go` cover all six widget types — text/combo/list/push render the current value via the page text engine; checkbox/radio paint vector chrome (rect + tick, ring + dot)
- `(*Form).AddTextField/AddCheckbox/AddRadioGroup/AddComboBox/AddListBox/AddPushButton` — programmatic field creation; auto-creates /AcroForm and /AcroForm/DR/Font/Helv on first call; combined field+widget dict for single-widget fields, parent + kids for radio groups
- **Extra field types** (`form_fields_extra.go`) — `AddPasswordField`, `AddFileSelectField`, `AddRichTextField`, `AddNumberField(…, NumberFormatOptions)`, `AddDateField(…, format)` create the typed variants `PasswordBoxField` / `FileSelectBoxField` / `RichTextBoxField` / `NumberField` / `DateField`. Each is a text field (`/FT /Tx`) that **embeds `TextBoxField`** (so it inherits the whole text surface — `Value`/`SetValue`, `SetMaxLen`, `SetReadOnly`, `SetStyle`, `Flatten`, …) and is distinguished by a `/Ff` flag (Password bit 14 / FileSelect bit 21 / RichText bit 26) or a JavaScript format action in `/AA` (Number → `AFNumber_Format`, Date → `AFDate_FormatEx`). Because that marker persists in the PDF, `fieldFromNode` reclassifies the field to its concrete type on Save+Open (Number/Date detected via the format-JS function name, `nodeHasFormatJS`). `RichTextBoxField.SetRichValue(rv, plain)` writes `/RV` + `/V`; `DateField.Format()` reads the mask back. `FieldType`/`FormFieldType` gain `Password`/`FileSelect`/`RichText`/`Number`/`Date`, and the form-data export/import (`asTextField`) treats all text-family types uniformly. Mirrors Aspose.PDF for .NET's `NumberField`/`DateField`/`PasswordBoxField`/`RichTextBoxField`/`FileSelectBoxField`
- **`BarcodeField`** (`barcode.go` / `barcode_code128.go` / `barcode_qr.go` / `barcode_qr_gf256.go` / `barcode_qr_tables.go` / `appearance_barcode.go`, `pdf-go-nc28`) — draws its value as a barcode symbol instead of text. `(*Form).AddBarcodeField(pageNum, rect, name, symbology BarcodeSymbology, value string) (*BarcodeField, error)`; `BarcodeField` embeds `TextBoxField` like the extra field types above, so `Value`/`SetValue`/`SetReadOnly`/`SetStyle`/`Flatten`/JSON-FDF-XFDF export all just work, distinguished on reopen by a private (non-spec) dict marker rather than a `/Ff` flag or format JS. `(*BarcodeField).Symbology()/SetSymbology(s)` read/change the encoding; `SetValue`/`SetSymbology`/`AddBarcodeField` all validate the value encodes under the (new) symbology before writing anything, leaving the field unchanged on error — including through JSON/FDF/XFDF import, which dispatches `SetValue` through the `Field` interface for this reason rather than through the shared `asTextField` helper. Bar colour and background reuse `FieldStyle.TextColor`/`BackgroundColor`; unlike other field types no default border/fill is drawn (would eat into the barcode's required quiet zone). `BarcodeSymbology` — `BarcodeCode128` (ISO/IEC 15417, Code Set B only: printable ASCII 0x20-0x7E, one symbol per character, no Code Set C digit-pair compaction) and `BarcodeQR` (ISO/IEC 18004, byte mode only — raw UTF-8 bytes, no ECI designator — error-correction level M, version 1-40 chosen automatically). QR's Reed-Solomon and mask selection are fully algorithmic (GF(256) arithmetic + generator-polynomial construction from the single primitive-polynomial constant, no per-version EC table beyond the block-size table ISO 18004 requires); validated by an independent self-decoder in `barcode_qr_test.go` that recomputes function-pattern geometry from the version, extracts and unmasks the format info, reads codewords back in zigzag order, and asserts each block's Reed-Solomon syndromes are all zero. This machinery caught a real generator-polynomial index bug (the `x` and `root` terms swapped between `qrRSGeneratorPoly`'s two accumulators) during development — but a second, more subtle bug (`writeFormatInfo`'s module coordinates fully transposed, row/col swapped, except the two positions a transpose can't move) passed every test at first, because the self-decoder's format-info reader was written by mirroring the writer's code rather than independently: a self-mirroring reader agrees with a wrong writer by construction. Code review caught it; a from-scratch decoder built against a reference implementation's verbatim source (fetched and hand-translated from its `(x, y)` = (column, row) convention to this codebase's `(row, col)` one) confirmed it and pinned the fix, and the self-decoder's format-info extraction was then rewritten from that same source rather than from `writeFormatInfo`'s code, closing the blind spot. Lesson generalized: a round-trip self-test only proves two independently-*derived* implementations agree, not that either matches the spec — mirroring one side's code into the other's "independent" checker defeats the whole point. Mirrors Aspose.PDF for .NET's `BarcodeField`/`Symbology`. **Out of scope**: `BarcodePDF417` (`pdf-go-cyzm`) — a correct PDF417 encoder needs a 2,787-entry (929 values × 3 clusters) codeword-to-bar-pattern lookup table (ISO/IEC 15438 Annex A) that can't be safely hand-transcribed without a reference decoder to validate it against, unlike Code128's ~106-entry table or QR's fully-algorithmic Reed-Solomon
- `(*Form).RemoveField(name) bool` — removes field plus all its widgets from /AcroForm/Fields and per-page /Annots
- `(*Document).Flatten() error` / `(*Form).Flatten() error` — bake every form field's current appearance into its page content stream and remove all fields, widgets, and `/AcroForm` (the result renders identically but is no longer fillable). `Document.Flatten` is a convenience for `Form().Flatten`. Mirrors Aspose.PDF for .NET's `Document.Flatten` / `Form.Flatten` (`flatten.go`)
- `(Field).Flatten() error` — flattens a single field (bakes its widgets, removes just that field), leaving other fields and `/AcroForm` intact. Mirrors `Field.Flatten`
- Per-type structural mutators: `SetReadOnly`, `SetRequired` on every type; `TextBoxField.{SetMaxLen,SetMultiline,SetPassword}`; `ComboBoxField.{SetEditable,AddOption,RemoveOption}`; `ListBoxField.{SetMultiSelect,AddOption,RemoveOption}`
- `RadioItem` struct — `PageNum`, `Rect`, `Export` for cross-page radio groups
- `(Field).SetStyle(FieldStyle) error` / `(Field).Style() FieldStyle` — visual styling on every field type. `FieldStyle` struct: `BorderColor`, `BackgroundColor`, `TextColor *Color`, `BorderWidth float64`, `BorderStyle` (Solid/Dashed/Beveled/Inset/Underline), `DashPattern []float64`, `TextFont Font`, `TextSize float64`, `TextAlign HAlign`. Persisted per ISO 32000-1 as `/MK` (/BC, /BG), `/BS` (/W, /S, /D), `/DA` (font + size + colour), `/Q` (quadding) and re-rendered into the widget `/AP`, so the chosen look is identical across Acrobat / MuPDF / browser viewers. Round-trips through Save+Open. Choice/text alignment honoured (left/centre/right); checkbox uses BorderColor for the box and TextColor for the tick; radio uses BorderColor for the ring and TextColor for the dot. Font styling covers the Standard 14 fully; embedded TTF fonts render with the chosen font in-session and fall back to Helvetica metrics after a Save+Open round-trip (the `/DA` still references the embedded font for viewer-side regeneration). `(*Form).ensureFont(Font)` registers any font in `/AcroForm/DR/Font` and is reused for the default Helvetica
- **Form-data interchange** (`form_json.go` / `form_fdf.go` / `form_xfdf.go`) — export/import field values in three formats over one shared value model (`formValues`/`applyFormValues`: text/combobox → string, checkbox/radio → on-state name, listbox → N strings; push buttons skipped). All importers match by full field name and reuse the existing `SetValue`/`SetChecked`/`SetSelected` (so widget `/AP` regenerates), return the count applied, and skip unknown names / non-applying values rather than erroring. Mirrors Aspose.PDF for .NET's `Document.Form.ExportToJson`/`ImportFromJson` + `Facades.Form.Export/Import{Fdf,Xfdf}`
  - `(*Form).ExportJSON(opts...) ([]byte, error)` / `WriteJSON` / `ImportJSON(data) (int, error)` / `ReadJSON` — **typed JSON** keyed by full name: `{"name": {"type": "...", "value": ...}}` where the value's JSON kind follows the type (text/radio/combobox → string, checkbox → bool, listbox → array). Sorted full snapshot by default; `JSONExportOptions{Indent, OmitEmpty}`. Import dispatches on the **target** field's concrete type (the JSON `type` is advisory)
  - `(*Form).ExportFDF() ([]byte, error)` / `WriteFDF` / `ImportFDF(data) (int, error)` / `ReadFDF` — **FDF** (Forms Data Format, ISO 32000-1 §12.7.7): a PDF-syntax `%FDF-1.2 … /FDF /Fields [ << /T /V >> … ]` file, hand-serialised; import scans indirect objects (`objHeaderRE` + `parseValue`), resolves `/FDF /Fields`, and descends `/Kids` joining partial `/T` names with `.`
  - `(*Form).ExportXFDF() ([]byte, error)` / `WriteXFDF` / `ImportXFDF(data) (int, error)` / `ReadXFDF` — **XFDF** (XML Forms Data Format, ISO 19444-1) via `encoding/xml` (`<xfdf xmlns=…><fields><field name><value>`); nested `<field>` elements flatten to dotted names
- `(*ButtonField).Action() Action` / `SetAction(act Action)` (`form_button.go`) — read/write the push button's activation action (`/A` on its widget): typically `NewSubmitFormAction`/`NewResetFormAction`, but any `Action` works (GoTo, URI, JavaScript); nil clears. GoTo destinations resolve their page number on read. Mirrors Aspose.PDF for .NET's `ButtonField.OnActivated`
- `(*ButtonField).SetAppearance(ButtonAppearance) error` (`form_button.go`) — rich push-button appearance. `ButtonAppearance` struct: `Caption` (/MK/CA), `RolloverText` (/MK/RC), `DownText` (/MK/AC), `IconPath` (PNG/JPEG, baked into /AP), `IconPosition ButtonIconPosition` (/MK/TP: `ButtonCaptionOnly`/`ButtonIconOnly`/`ButtonIconAboveCaption`/`ButtonCaptionOverIcon`), `TextColor`/`FaceColor`/`BorderColor *Color`. Writes the /MK characteristics and bakes three appearance streams — /AP/N (normal), /AP/R (rollover), /AP/D (down, with a depressed-look offset + darkened face) — so the button reacts to hover/press in any viewer. Rollover/down captions fall back to Caption when empty. The icon is embedded as an Image XObject and drawn into each state's Form XObject. Out of scope (follow-up): caption rotation (/MK/R) and exposing the icon as a /MK/I Form XObject for viewer-side regeneration

**`annotation.go` / `annotation_action.go` / `annotation_link.go` / `annotation_markup.go`**
- `Annotation` interface — `AnnotationType()`, `Rect()/SetRect()`, `Color()/SetColor()`, `Title()/SetTitle()`, `Contents()/SetContents()`, `PageIndex()`
- `AnnotationType` enum — `AnnotationTypeUnknown`, `AnnotationTypeLink`, `AnnotationTypeHighlight`, `AnnotationTypeUnderline`, `AnnotationTypeStrikeOut`, `AnnotationTypeSquiggly`, `AnnotationTypeWidget`, `AnnotationTypeSquare`, `AnnotationTypeCircle`, `AnnotationTypeLine`, `AnnotationTypeInk`, `AnnotationTypeText`, `AnnotationTypeFreeText`, `AnnotationTypeStamp`, `AnnotationTypeFileAttachment`, `AnnotationTypeRedact`, `AnnotationTypePolygon`, `AnnotationTypePolyLine`, `AnnotationTypeCaret`
- Concrete types: `LinkAnnotation`, `HighlightAnnotation`, `UnderlineAnnotation`, `StrikeOutAnnotation`, `SquigglyAnnotation`, `WidgetAnnotation` (existing form fields, read-only via this surface), `GenericAnnotation` (catch-all for unsupported subtypes)
- `AnnotationCollection` — `Add(a) error`, `At(i) Annotation`, `Delete(a) bool`, `DeleteAt(i) error`, `Count() int`, `All() []Annotation`. Add panics on nil; idempotent same-page; errors on cross-page re-attach
- `(Annotation).Flatten() error` — bakes the annotation's normal appearance (`/AP/N`, honoring `/AS`) into the page content at its `/Rect`, then removes it (no-appearance annotations are removed without drawing). `(*AnnotationCollection).Flatten() error` — flattens every non-widget annotation on the page (widgets are left for `Form.Flatten`). Shared baking machinery in `flatten.go`; mirrors Aspose.PDF for .NET's `Annotation.Flatten`
- Constructors: `NewLinkAnnotation(page, rect)`, `NewHighlightAnnotation(page, rect)`, `NewUnderlineAnnotation(page, rect)`, `NewStrikeOutAnnotation(page, rect)`, `NewSquigglyAnnotation(page, rect)`
- `LinkAnnotation.Action() Action`, `LinkAnnotation.SetAction(act Action)` — nil clears /A
- `LinkAnnotation.Highlight() LinkHighlightMode`, `LinkAnnotation.SetHighlight(h LinkHighlightMode)` — controls /H click-feedback (None / Invert / Outline / Push)
- `LinkHighlightMode` enum — `LinkHighlightInvert` (default), `LinkHighlightNone`, `LinkHighlightOutline`, `LinkHighlightPush`
- `Action` interface — `ActionType()`; concrete types: `GoToURIAction`, `GoToAction`, `NamedAction`, `SubmitFormAction`, `ResetFormAction`, `JavaScriptAction` (parsed from existing PDFs and constructable; read via `Script() string`)
- Action constructors: `NewGoToURIAction(uri)`, `NewGoToAction(pageNum, top)`, `NewNamedAction(name)`, `NewSubmitFormAction(url, fields, flags)`, `NewResetFormAction(fields)`, `NewJavaScriptAction(script)`
- `ActionType` enum — `ActionTypeUnknown`, `ActionTypeGoToURI`, `ActionTypeGoTo`, `ActionTypeNamed`, `ActionTypeSubmitForm`, `ActionTypeResetForm`, `ActionTypeJavaScript`
- `NamedActionType` enum — `NamedActionFirstPage`, `NamedActionLastPage`, `NamedActionNextPage`, `NamedActionPrevPage`, `NamedActionPrint`
- `SubmitFormFlags` bitfield per ISO 32000-1 Table 237 (`SubmitIncludeNoValueFields`, `SubmitExportFormat`, `SubmitGetMethod`, ...)
- `QuadPoint` struct — `X1 Y1 X2 Y2 X3 Y3 X4 Y4` floats per ISO 32000-1 §12.5.6.10 (UL/UR/LL/LR corners). Used by `SetQuadPoints`/`QuadPoints` on the four markup types
- **Markup appearances** (`appearance_markup.go`, `pdf-go-c4c4`): the four text-markup types share `markupAnnotationBase` (quad accessors + the regenerate hook) and each draws its own `/AP/N` — Highlight a Multiply-blended wash over every quad (the glyphs stay readable through it), Underline a rule along the quad's bottom, StrikeOut one through its middle, Squiggly a zigzag under it; line width and wave amplitude scale with the marked text's height, and the colour is `/C` (highlight defaults to yellow, the rules to black). `SetQuadPoints`/`SetColor`/`SetRect` regenerate, and `RegenerateAppearance()` forces it. A conforming viewer synthesizes such an appearance from `/QuadPoints` on its own, but every `/AP`-only consumer — this library's renderer, and through it the HTML and SVG exporters — paints nothing without the stream, so `synthesizeAnnotationAppearance` (`render_annotations.go`) also builds one on the fly for an `/AP`-less markup annotation parsed from another producer's file. **Quads are treated as text boxes** (the convention viewers write and expect: bottom at the descender, top at the ascender) — the same box `SearchText` reports, so a match rectangle can be used as a quad directly

**`annotation_drawing.go` / `appearance.go` / `appearance_builder.go`**
- `Point` struct — single point in PDF user-space (used for Line endpoints, Ink strokes)
- `BorderStyle` enum — `BorderSolid`, `BorderDashed`, `BorderBeveled`, `BorderInset`, `BorderUnderline` per ISO 32000-1 Table 168
- `LineEndingStyle` enum — 10 styles (`LineEndingNone`, `LineEndingSquare`, `LineEndingCircle`, `LineEndingDiamond`, `LineEndingOpenArrow`, `LineEndingClosedArrow`, `LineEndingButt`, `LineEndingROpenArrow`, `LineEndingRClosedArrow`, `LineEndingSlash`) per ISO 32000-1 Table 176
- `SquareAnnotation` / `CircleAnnotation` — `BorderWidth/SetBorderWidth`, `BorderStyle/SetBorderStyle`, `DashPattern/SetDashPattern`, `Color/SetColor` (stroke), `InteriorColor/SetInteriorColor` (fill), inherited `Rect/SetRect/Title/SetTitle/Contents/SetContents/PageIndex`. Constructors `NewSquareAnnotation(page, rect)` / `NewCircleAnnotation(page, rect)`
- `LineAnnotation` — `Start/SetStart`, `End/SetEnd`, `StartLineEnding/SetStartLineEnding`, `EndLineEnding/SetEndLineEnding`, `LeaderLineLength/SetLeaderLineLength`, `InteriorColor/SetInteriorColor`. Auto-bbox /Rect from endpoints + `9 × BorderWidth` padding. Constructor `NewLineAnnotation(page, start, end)`
- `InkAnnotation` — `Strokes/SetStrokes` (defensive deep copy), `AddStroke`, full border surface. Catmull-Rom smoothed in /AP for 3+ point strokes; raw /InkList stored unchanged. Constructor `NewInkAnnotation(page, strokes)`
- All four types regenerate `/AP/N` on every property setter; an explicit `RegenerateAppearance()` method is also exposed on each type
- `/AP/N` infrastructure: every drawing annotation owns one Form XObject in `doc.objects`. Setters mutate the XObject in place — no leaks across multiple property changes

**`annotation_poly.go` / `annotation_caret.go` / `appearance_poly.go` / `appearance_caret.go`** — vertex-based + caret annotations; mirror Aspose.PDF for .NET's `PolygonAnnotation` / `PolylineAnnotation` / `CaretAnnotation`
- `PolygonAnnotation` — closed polygon through `Vertices()/SetVertices([]Point)` (PDF user space); `InteriorColor()/SetInteriorColor` fill; full border surface (width/style/dash/color) inherited from `drawingAnnotationBase`. `/Rect` auto-computed from vertices + `9 × BorderWidth` padding. `/AP/N`: closed path, filled (`/IC`) and/or stroked (`/C`); nothing drawn when neither colour set (same convention as Square). Constructor `NewPolygonAnnotation(page, vertices)`
- `PolylineAnnotation` — open polyline through the same `Vertices()/SetVertices`; `StartLineEnding/SetStartLineEnding`, `EndLineEnding/SetEndLineEnding` (reuse `LineEndingStyle` + `/LE`), `InteriorColor/SetInteriorColor` (fill for closed endings). `/AP/N`: stroked open path + endings at first/last vertex (start ending points outward from vertex 0). Constructor `NewPolylineAnnotation(page, vertices)`
- Both share the unexported `polyAnnotationBase` (Vertices/`/IC`/`/Rect` recompute + `SetBorderWidth` override) embedding `drawingAnnotationBase`
- `CaretAnnotation` — insertion marker drawn as an upward chevron filling `/Rect`, painted with `/C` (default black). `Symbol()/SetSymbol(CaretSymbol)` writes `/Sy` (`CaretSymbolNone` / `CaretSymbolParagraph` = `/P`). Constructor `NewCaretAnnotation(page, rect)`
- `CaretSymbol` enum — `CaretSymbolNone`, `CaretSymbolParagraph`
- All three regenerate `/AP/N` on every setter; explicit `RegenerateAppearance()` exposed; round-trip through Save+Open and render via the shared `/AP/N` path

**`annotation_text.go` / `annotation_freetext.go` / `annotation_stamp.go` / `appearance_freetext.go` / `appearance_stamp.go`**
- `TextAnnotation` — sticky-note annotation. `Icon()/SetIcon(t)`, `Open()/SetOpen(b)`, inherited `SetRect/SetColor/SetTitle/SetContents`. Constructor `NewTextAnnotation(page, position Point)` — auto-bbox 24×24pt at anchor. No /AP — viewers render the icon themselves
- `TextIcon` enum — `TextIconNote` (default), `TextIconComment`, `TextIconKey`, `TextIconHelp`, `TextIconNewParagraph`, `TextIconParagraph`, `TextIconInsert`, `TextIconUnknown`
- `FreeTextAnnotation` — text drawn directly on the page. `Contents()/SetContents`, `TextStyle()/SetTextStyle` (round-trips through /DA + /Q + /BG). Border via `drawingAnnotationBase` (BorderWidth/BorderStyle/DashPattern). `Intent()/SetIntent` for Plain/Callout/Typewriter modes; `CalloutPoints/EndLineEnding/InnerRect` for callouts; `BorderEffect/BorderEffectIntensity` for cloudy borders. Honors `style.VAlign` (Top/Middle/Bottom) in /AP/N rendering. Constructor `NewFreeTextAnnotation(page, rect, contents, style)`
- `FreeTextIntent` enum — `FreeTextIntentFreeText` (default), `FreeTextIntentCallout`, `FreeTextIntentTypewriter`
- `BorderEffect` enum — `BorderEffectNone` (default), `BorderEffectCloudy` (wavy "cloud" border via /BE/S=/C)
- `StampAnnotation` — rubber-stamp annotation. `Name()/SetName(StampName)`, `RawName()/SetRawName(string)` (escape hatch for non-spec names). Custom image override via `SetCustomImage(path)/SetCustomImageFromStream(r)/ClearCustomImage()`. Border via `drawingAnnotationBase`. Constructor `NewStampAnnotation(page, rect, name)`. Library-default visuals for all 14 predefined names (color-coded: green=positive, red=warning, orange=informational, gray=neutral)
- `StampName` enum — 14 names per ISO 32000-1 §12.5.6.13 Table 184: `StampNameApproved`, `StampNameAsIs`, `StampNameConfidential`, `StampNameDepartmental`, `StampNameDraft`, `StampNameExperimental`, `StampNameExpired`, `StampNameFinal`, `StampNameForComment`, `StampNameForPublicRelease`, `StampNameNotApproved`, `StampNameNotForPublicRelease`, `StampNameSold`, `StampNameTopSecret`, plus `StampNameUnknown` for non-spec names
- All three types regenerate `/AP/N` on every property setter (TextAnnotation has no /AP — `RegenerateAppearance()` is no-op for API symmetry); explicit `RegenerateAppearance()` method exposed on each type

**`embedded_files.go`** — document-level embedded (attached) files: the `/Catalog/Names/EmbeddedFiles` name tree (ISO 32000-1 §7.11.4). Mirrors Aspose.PDF for .NET's `Document.EmbeddedFiles`. The document-level counterpart to `FileAttachmentAnnotation` (page-pinned); both share the `(*Document).buildEmbeddedFilespec` helper that emits the `/EmbeddedFile` stream + `/Filespec` dict
- `(*Document).EmbeddedFiles() *EmbeddedFiles` — the attachment collection (always non-nil; empty for documents without attachments)
- `(*EmbeddedFiles)` — `Add(path) (*EmbeddedFile, error)` (embeds the file under its base name, MIME from extension), `AddFromStream(name, r) (*EmbeddedFile, error)`, `Get(name) *EmbeddedFile`, `Has(name) bool`, `Names() []string` (lex-sorted), `Count() int`, `All() []*EmbeddedFile`, `Remove(name) bool`, `Clear()`. Reads via `walkNameTree` (handles `/Kids` subtrees); writes a flat `/Names` leaf, preserving existing `/Filespec` refs and the sibling `/Names` subentries (Dests, JavaScript)
- `(*EmbeddedFile)` — `Name()`, `Description()/SetDescription()`, `MIMEType()` (from the stream `/Subtype`), `Size()`, `Data() ([]byte, error)`, `WriteTo(w)`, `Save(path)`. Content decoded through `decodedStreamData` (handles a producer's FlateDecode). Round-trips and coexists with named destinations + JavaScript under `/Catalog/Names`

**`collection.go`** — PDF portfolios / collections (ISO 32000-1 §7.11.6 + §12.3.5, epic `pdf-go-vbz`): the presentation layer over `EmbeddedFiles` that makes a viewer show the attachments as a navigable table of custom columns instead of a plain list. Mirrors Aspose.PDF for .NET's `Document.Collection` / `CollectionSchema` / `CollectionField`
- `(*Document).Collection() *Collection` — always non-nil facade over the catalog's `/Collection` dict; `IsPortfolio()` reports whether the document actually is one, and every mutator (`SetView`/`SetInitialFile`/`SetSort`/`Schema().Add`) creates the dict on demand (materializing `d.catalog` too, as `namesDict` does). `Remove()` drops `/Collection` and the attachments stay as an ordinary attachment list; `Files()` delegates to `EmbeddedFiles().All()`
- `SetView(CollectionView)` — `CollectionViewDetails` (`/D`, table), `CollectionViewTiles` (`/T`), `CollectionViewHidden` (`/H`); `SetInitialFile(name)` → `/D` (the attachment presented first); `SetSort(field, ascending)` → `/Sort` (single field; the reader also accepts a producer's array form)
- `Schema()` → `*CollectionSchema`: `Add(key, displayName, CollectionFieldType) *CollectionField` (re-adding a key replaces it, new columns get the next `/O`), `Field`/`Fields`/`Keys` (display order = `/O`, then key), `Remove`, `Count`. `CollectionField`: `DisplayName`/`SetDisplayName` (`/N`), `Type` (`/Subtype`), `Order`/`SetOrder` (`/O`), `IsVisible`/`SetVisible` (`/V`, default true), `IsEditable`/`SetEditable` (`/E`, default false), `IsDerived` (the column's values come from the file specification, so no per-file entry is needed)
- `CollectionFieldType` — `CollectionFieldText` (`/S`), `Date` (`/D`), `Number` (`/N`), plus the **derived** columns whose values come from the file specification itself and need no per-file entry: `Filename` (`/F`), `Description` (`/Desc`), `ModDate`, `CreationDate`, `Size`
- `(*EmbeddedFile).CollectionItem() *CollectionItem` — the per-attachment values (`/Filespec` `/CI`): `SetText`/`SetNumber`/`SetDate` and `Text`/`Number`/`Date` (typed reads, `ok=false` when unset or mistyped), `Fields()`, `Remove(field)`. Reads follow a producer's `/CollectionSubitem` wrapper (`/D`) transparently; writes are direct values. Field keys are user-facing strings (`"Amount"`), mapped to PDF names through `collectionFieldKey`/`collectionKeyFromName`
- Round-trips through Save (the writer copies every catalog key but `/Pages`); verified with pikepdf on a generated three-invoice portfolio (schema order, `/CI` values, `/Sort`, `/D`) and by our own `Validate`. Out of scope (v1): `/Folders` (folder hierarchies), collection subitem prefixes on write, and the Adobe Navigator SWF/`/Navigator` extension

**`associated_files.go` / `einvoice.go` / `einvoice_xmp.go`** — PDF/A-3 associated files and hybrid e-invoices (ZUGFeRD 2.x / Factur-X 1.0; epic `pdf-go-t6s9`; design `docs/superpowers/specs/2026-09-22-einvoice-facturx-design.md`). Mirrors Aspose.PDF for .NET's `FileSpecification.AFRelationship` and `PdfFormat.ZUGFeRD`
- `AFRelationship` (`AFUnspecified`/`AFSource`/`AFData`/`AFAlternative`/`AFSupplement`) — `(*EmbeddedFile).AFRelationship()` / `SetAFRelationship(r)` write `/AFRelationship` on the file specification and list it once in the catalog `/AF` (a direct file-spec dict is promoted to an object first); removing or replacing the attachment drops it from `/AF`. Every embedded file now carries `/Params /ModDate`. `ValidatePDFA` for PDF/A-3 reports `EMBEDDED_FILE_NOT_ASSOCIATED` (ISO 19005-3 §6.8) and `ConvertToPDFA` to PDF/A-3 repairs it (`associatePDFAEmbeddedFiles`: existing relationship kept, missing → Unspecified, `/AF` + `/ModDate`). Converting to PDF/A-1 (1a or 1b), which forbids attachments, drops the `/Names/EmbeddedFiles` tree **and** the catalog `/AF`, then deletes the file specifications and embedded-file streams nothing references any more — detaching only the name tree left the bytes in the saved file. Documented limit: the file specification of a `FileAttachmentAnnotation` gets no `/AFRelationship`, and neither the PDF/A-3 rule nor its repair covers it — use `EmbeddedFiles` for an associated file
- `(*Document).AttachInvoice(xml, InvoiceOptions{FileName, Profile, Format})` — the caller supplies CII XML (generation is out of scope); checked for the `CrossIndustryInvoice` root in `urn:un:unece:uncefact:data:standard:CrossIndustryInvoice:100`, profile read from the guideline ID (`invoiceGuideline` stops at it) unless given; embedded as `factur-x.xml` (`xrechnung.xml` for XRechnung), MIME `text/xml`, relationship `Data` for MINIMUM/BASIC WL and `Alternative` above; `fx:DocumentType/DocumentFileName/Version/ConformanceLevel` written in `urn:factur-x:pdfa:CrossIndustryDocument:invoice:1p0#` plus the PDF/A extension schema declaring them; then `ConvertToPDFA` (PDFA3B default, PDFA3A allowed, anything else an error). A second call replaces the invoice and its metadata
- `(*Document).Invoice() (*Invoice, error)` — `Invoice{XML, FileName, Profile, Standard, Version}`; `(nil, nil)` when absent. Finds the attachment by the XMP `DocumentFileName`, else `factur-x.xml`/`xrechnung.xml`/`zugferd-invoice.xml` (case-insensitive, so ZUGFeRD 1.0's `ZUGFeRD-invoice.xml` too); profile from the XMP `ConformanceLevel` (`invoiceProfileFromLevel`, COMFORT → EN 16931), else the XML; `Standard` from the XMP namespace (Factur-X/ZUGFeRD 2.1+, ZUGFeRD 2.0 `…invoice:2p0#`, ZUGFeRD 1.0 `urn:ferd:…`), else the XML root/file name
- **XMP extension schemas survive `ConvertToPDFA`**: `setPDFAMetadata` rebuilds the packet from parsed fields, which used to drop every `pdfaExtension:schemas` block — ours and any other producer's — though PDF/A requires one for each custom namespace. It now lifts those `rdf:Description` blocks out first (`xmpExtensionBlocks`), drops custom properties in the `http://www.aiim.org/pdfa/ns/` namespaces so flattened schema fields never reappear as loose properties, and re-inserts the blocks (`insertXMPDescriptions`). The extraction is a depth-counting scan of `rdf:Description` elements (not a regex), so nested foreign structures are carried whole
- **XMP prefix handling was hardened** (`xmp.go`) during this work: known namespaces get their preferred prefixes (`fx`, `zf`, `pdfaid`, `xmpMM`), `bindCustomPrefixes` binds exactly one prefix per namespace (a fresh `ns<N>` on collision; the core `dc`/`xmp`/`pdf` prefixes and the `rdf`/`x` prefixes are reserved), and properties of a namespace declared by a kept extension schema are re-serialised under that schema's own declared prefix rather than a generic one
- Validated with the Python `factur-x` library (Akretion): it finds `factur-x.xml`, detects the level, and the MINIMUM sample passes the official XSD. Full PDF/A-3 conformance still needs veraPDF (JRE absent). Out of scope: CII generation, EN 16931 Schematron, writing ZUGFeRD 1.0/2.0, UBL, Order-X

**`optional_content.go`** — optional content / layers authoring (OCGs, ISO 32000-1 §8.11). The write-side companion to the renderer's OCG visibility (`render_oc.go`). Mirrors Aspose.PDF for .NET's `Document.Layers`
- `(*Document).AddLayer(name) *Layer` — creates an OCG (`/Type /OCG /Name`), registers it in `/Catalog/OCProperties/OCGs` + the default config `/D/Order` (creating `/OCProperties` with a `/D` when absent). `(*Document).Layers() []*Layer` — lists existing layers
- `(*Layer)` — `Name()/SetName()`, `IsVisible()/SetVisible(bool)` (membership in `/D/OFF`)
- `(*Page).BeginLayer(layer) error` / `EndLayer() error` — bracket page content in an `/OC /ocN BDC … EMC` marked-content section assigned to the layer; `registerOCGProperty` maps `/ocN → OCG` in the page `/Resources/Properties` (reusing an existing mapping). Everything drawn between the calls (AddText, Draw*, AddImage, …) joins the layer, since `appendToContentStream` concatenates into one content stream. The built-in renderer drops a hidden layer's content (it reads `/D/OFF` via `ocOffSet`), and layers round-trip with names + visibility

**`annotation_fileattachment.go` / `annotation_redact.go` / `appearance_redact.go` / `redact_apply*.go`**
- `FileAttachmentAnnotation` — embedded file annotation. `Icon()/SetIcon(i)`, `SetFile(path)/SetFileFromStream(r, name)`, `HasFile()`, read-only metadata `FileName/FileMIMEType/FileSize/FileBytes/FileDescription/SetFileDescription`. Constructor `NewFileAttachmentAnnotation(page, position Point)` — auto-bbox 24×24pt. No /AP — viewers render the icon themselves
- `FileAttachmentIcon` enum — `FileAttachmentIconPaperclip` (default), `FileAttachmentIconGraph`, `FileAttachmentIconPushPin`, `FileAttachmentIconTag`, `FileAttachmentIconUnknown`. MIME type auto-detected from file extension via `mime.TypeByExtension`; embedded file stored as a `/EmbeddedFile` stream referenced by a `/Filespec` dict
- `RedactAnnotation` — mark + apply redaction. `QuadPoints()/SetQuadPoints`, `InteriorColor()/SetInteriorColor`, `OverlayText()/SetOverlayText`, `RepeatOverlayText()/SetRepeatOverlayText`, `OverlayTextStyle()/SetOverlayTextStyle`. Border via `drawingAnnotationBase`. Mark-mode `/AP/N` renders quad fills + optional overlay preview; destructive content removal via `(*Document).ApplyRedactions()`. Constructor `NewRedactAnnotation(page, rect)`
- `(*Document).ApplyRedactions() error` — destructively removes content inside every `/Redact` annotation's `/QuadPoints` (or `/Rect`) regions: text glyphs (per-glyph filter with TJ kerning gaps to preserve surviving positions), `Do` XObject invocations (drop or clip), and drawing paths (drop or clip). After rewrite, fills each quad with `/IC` color and renders `/OverlayText` (centered or tiled if `/Repeat`); then removes the redact annotations from `/Annots`. Best-effort semantics — partial state on failure
- `(*Document).ValidateRedactions() error` — pre-flight dry-run parseability check on every redact-bearing page; recommended before `ApplyRedactions`
- `NewJavaScriptAction(script string) *JavaScriptAction` — public constructor for `/JavaScript` actions; the action is encoded back on Save (JS actions are also parsed from existing PDFs). Includes documented security warning — embedded JavaScript executes in the recipient's viewer
- Apply pipeline files: `redact_apply.go` orchestrates; `redact_apply_text.go` rewrites Tj/TJ/'/" with per-glyph filtering; `redact_apply_image.go` clips/drops `Do` invocations using even-odd clip paths; `redact_apply_path.go` clips/drops path-construction sequences buffered until a paint terminator

**`table.go` / `table_render.go`**
- `pdf.NewTable() *Table` — builder for a tabular layout drawn onto a Page. Mirrors Aspose.PDF for .NET's `Table` class. After `(*Page).AddTable` renders the table, the `*Table` is not held by the document
- `Table` — `SetColumnWidths([]float64) *Table` (in points), `SetBorder(BorderInfo) *Table` (outer), `SetDefaultCellBorder(BorderInfo) *Table`, `SetDefaultCellMargin(MarginInfo) *Table` (per-cell padding default), `SetDefaultCellStyle(TextStyle) *Table`, `AddRow() *Row`, `Rows() []*Row`, `RowCount() int`. Getters for each setter
- `Row` — `AddCell(text) *Cell`, `AddCells(texts ...string) []*Cell`, `Cells() []*Cell`, `CellCount() int`, `SetHeight(float64) *Row` (0 = auto-fit), `Height() float64`, `Table() *Table`
- `Cell` — `SetText`, `SetTextStyle`, `SetBackground(*Color)`, `SetBorder(BorderInfo)`, `SetMargin(MarginInfo)`, `SetHAlign(HAlign)`, `SetVAlign(VAlign)` — all chainable, all paired with getters. Per-cell setters override the table default. `Background()/Border()/Margin()` return nil when the cell inherits the table default
- `BorderSide` enum — `BorderSideNone`, `BorderSideTop`, `BorderSideRight`, `BorderSideBottom`, `BorderSideLeft`, `BorderSideAll` (bitwise OR of all four)
- `BorderInfo` struct — `Sides BorderSide`, `Width float64`, `Color *Color` (nil = black). Zero value = no border. Width 0 also = no border regardless of Sides. Mirrors Aspose.PDF for .NET's `BorderInfo`
- `MarginInfo` struct — `Top`/`Right`/`Bottom`/`Left` in points. Inside a Cell represents the padding between border and content. Mirrors Aspose.PDF for .NET's `MarginInfo`
- `(*Page).AddTable(t *Table, rect Rectangle) (int, error)` — renders the table inside the rectangle. Cell content is drawn via the existing `AddText` machinery (inherits its word-wrap, alignment, font embedding, Unicode handling, clipping). When rows don't fit, continuation pages are auto-appended to the document and the return value reports how many were added (0 if the table fits in `rect`). Errors on nil table, bad rect, mismatched cell count, or non-positive column width
- Cell text style resolution: zero `TextStyle` → table.DefaultCellStyle overlay → cell.TextStyle overlay → explicit HAlign/VAlign overrides
- Border layering: cell backgrounds first, then cell text, then cell borders (so borders appear on top of clipped text edges), then table outer border last (so outer border appears on top of cell-edge overlaps)
- `Cell.SetColSpan(n) / ColSpan() int` — cell occupies n consecutive columns; default 1. When set, the caller does not add cells for the columns covered by the span — the row simply has fewer cells. Mirrors Aspose.PDF for .NET's `Cell.ColSpan`
- `Cell.SetRowSpan(n) / RowSpan() int` — cell occupies n consecutive rows; default 1. Covered positions in subsequent rows are skipped by the caller. Mirrors Aspose.PDF for .NET's `Cell.RowSpan`
- `Table.SetRepeatingRowsCount(n) / RepeatingRowsCount() int` — marks the first n rows as headers that repeat at the top of every continuation page (default 0). Mirrors Aspose.PDF for .NET's `Table.RepeatingRowsCount`
- `Table.SetOverflowMargins(top, bottom) / OverflowMargins()` — top/bottom margins (points) for the continuation rect on auto-appended pages; defaults 50pt each. Same LLX/URX as the original rect; Y range = [bottom, pageHeight - top]
- `(*Page).AddTable(t, rect) (pagesAdded int, err error)` — now returns the number of continuation pages auto-appended (0 if the table fits in rect). Validation also rejects: ColSpan/RowSpan out of bounds, merge overlaps, rowspan crossing the header/body boundary, header height exceeding rect height, or any spanning group too tall for a continuation page
- Spanning groups: rows linked by rowspan are atomic — a group never breaks across pages. Each group is the smallest contiguous range [s, e] such that no rowspan in [s, e] extends past e. Page-break decisions operate on groups, not individual rows
- `Cell.SetImage(path) / SetImageFromStream(r) / Image() (path, hasImage)` — cell renders an image instead of text (image wins over text if both set). Auto-fits cell interior width preserving aspect ratio; HAlign/VAlign positions it. PNG and JPEG supported. Mirrors Aspose.PDF for .NET's `Cell.Image`
- `Row.SetBackground(*Color) / Background() *Color` — row-level background; cells inherit unless they call SetBackground themselves
- `Row.SetTextStyle(TextStyle) / TextStyle() *TextStyle` — row-level text style overlay between table.DefaultCellStyle and cell.TextStyle in the inheritance chain
- `Row.SetBorder(BorderInfo) / Border() *BorderInfo` — row-level border default; cells inherit unless overridden
- `Row.SetMargin(MarginInfo) / Margin() *MarginInfo` — row-level cell padding default
- `Table.AddRows([][]string) []*Row` — batch row constructor; one row per inner slice, one cell per string. Returns the rows for further per-row styling. Spans not supported in batch flow
- Border edge de-duplication: identical-style adjacent border lines (cell-cell shared edges, outer border overlapping cell perimeter edges) emit only once per page. Different styles still render both for caller intent. Per-page edge tracking
- Inheritance chain (4 deep): zero TextStyle/MarginInfo/BorderInfo ← `table.Default*` ← `row.*` ← `cell.*` ← cell.HAlign/VAlign override. Background chain: nil ← `row.Background` ← `cell.Background`
- Out of Phase 3 scope (Phase 4 candidates): auto-fit column widths (content-driven), dash patterns on borders, per-side border width/color, rowspan splitting across page breaks, image cells with explicit pixel sizing

**`table_detect.go` / `table_detect_rules.go` / `table_detect_lattice.go` / `table_detect_stream.go`** — table detection (epic `pdf-go-w4ht`, complete: lattice + stream); mirrors Aspose.PDF for .NET's `TableAbsorber` (whose default engine is lattice-only; its borderless recognition lives behind the opt-in `UseFlowEngine`, which returns no text styles — ours carries shading and styled runs into every consumer)
- `NewTableAbsorber()` → `(*TableAbsorber).Visit(page) error` (replaces the list) → `TableList() []*AbsorbedTable`; `AbsorbedTable{Rect, PageNumber}` → `RowList() []*AbsorbedRow` → `CellList() []*AbsorbedCell`; `AbsorbedCell{Rect, RowSpan, ColSpan, Covered}` + `TextFragments()`/`Text()` (fragments grouped into visual lines, gap-synthesized spaces)
- **`visitPaths`** (`table_detect_rules.go`) — the reusable CTM-aware path-geometry visitor: walks content ops delivering `pathVisit{segs (page-space straight segments), rects (axis-aligned re), hasCurve, bbox, paint (stroke/fill/both/none), strokeW (CTM-scaled), depth}`; optional Form-XObject recursion (`/Matrix` composed, `/Resources` override, cycle guard, depth cap 8). `flow_vector.go`'s cluster pass rides it (recursion off); the redaction rewriter keeps its own op-rewriting walker by design
- **Rule collection** (`pageRules`): stroked axis-aligned segments (±0.5pt), stroked-rect edges, thin filled rects (≤2.5pt — how Word/LaTeX/Excel draw borders) → H/V rulings; larger filled rects → fillBoxes (cell shading, unused so far). `normalizeRules`: snap parallel rules within 2pt to the length-weighted mean, join collinear pieces with gaps ≤3pt (dashes, per-cell drawing), drop <8pt
- **Lattice** (`table_detect_lattice.go`): H×V intersections (tol 2.5pt) → tabula-java corner search (nearest right + nearest down with continuously-ruled edges → smallest closable rectangle; missing inner rules yield bigger cells = **spans for free**) → union-find grouping by shared edges (6pt) → components ≥4 cells and ≥2×2 map onto the logical grid (boundary snap → `RowSpan`/`ColSpan`, spanned positions `Covered`, unruled gaps become empty cells); text assigned by fragment baseline midpoint
- Validated: `AddTable` round-trip (3×3 texts + colspan/rowspan grid), `PdfWithTable.pdf` 41×3, showcase bill 12×4; corpus sweep 2 945 pages ≈ 4 s, 312 tables/141 docs, zero panics
- **Consumers (all four flow exporters)**: DOCX `DocEnhancedFlow` (default) → real `w:tbl` (gridSpan/vMerge/`w:shd`/per-cell `w:jc`); EPUB + HTML flow → `<table>` with colspan/rowspan/backgrounds/styled runs; Markdown → GFM pipe tables (first row = header, spans flattened, `\|` escaping, right-aligned numeric columns as `---:`). `AbsorbedCell.Shading` carries the cell's background fill (visitor tracks `rg`/`g`/`k`/`sc`/`scn` non-stroking colour, fill boxes map onto cells at ≥60% coverage); `absorbedCellRuns` is the shared cell→styled-runs builder
- **Stream (borderless) detection** (`table_detect_stream.go`, `pdf-go-w4ht.3`): a second pass in `Visit` over the raw layout lines (NOT `Paragraphs()` — its column splitter dissects the whitespace channels the pass observes) on regions not claimed by lattice tables. Rows = lines split into cells at gaps ≥ 2 space widths; runs of ≥3 consecutive multi-cell lines; columns seeded from the modal cell count (Camelot), boundaries at gap midpoints. Conservative guard battery ("false negative beats false positive"): modal count ≥2 in ≥3 rows; non-overlapping column ranges; whitespace channel vertically clear in ≥85% of rows; ≤3 dominant font sizes; dot-leader TOC guard; H-rule structural agreement (rows/(rules−1) ∈ (0.65,1.5) when ≥3 rules); union-of-prose-paragraph coverage ≥80% → reject (kills interleaved two-column body text — index pages, justified columns); first column of list markers → a list; 2-col with sequential bare integers in col 0 → line-numbered listing; 2-col with long col-1 entries ending in a number → TOC/list-of-figures; 2-col ≥80% monospace → code+comment listing; prose-shaped columns (median ≥4 words in every column, or in the value column of a 2-col) → interleaved columns / specimen sheets. Cell words counted via `streamCellText` (gap-aware fragment join — fragment boundaries are not word boundaries). v1 scope: no merged cells (all 1×1), no cross-page stitching, no rotated tables. Corpus: 656 tables/212 docs (vs 312/141 lattice-only), reference docs page-exact in Word (showcase 15/15+5 tables, Binder1 payslip fully tabular 8/8+4); the borderless PDF-spec tables in `Large.pdf` (e.g. TABLE 3.1) now detect. Related extraction fix: `detectColumns` (paragraph.go) counts DISTINCT LINES per histogram bin — a single straddling line (a centered running head over a two-column page) no longer conceals the gutter, so such pages split into proper columns and read in correct order

**`comparison.go` / `comparison_tokens.go` / `comparison_diff.go` / `comparison_markup.go`** — document comparison (epic `pdf-go-175w`, phase 1: text); mirrors the text half of Aspose.PDF for .NET's `Aspose.Pdf.Comparison` namespace (`TextPdfComparer`, `DiffOperation`, `ComparisonOptions`). Design: `docs/superpowers/specs/2026-09-15-pdf-comparison-design.md`
- `ComparePages(p1, p2 *Page, opts ...ComparisonOptions) ([]DiffOperation, error)`, `CompareDocumentsPageByPage(d1, d2 *Document, opts...) (*ComparisonResult, error)`, `CompareFlatDocuments(d1, d2 *Document, opts...) (*ComparisonResult, error)` — mirror `TextPdfComparer.ComparePages` / `CompareDocumentsPageByPage` / `CompareFlatDocuments`. Page-by-page pairs pages by index (the longer document's tail is reported wholesale); flat treats each document as one continuous text, so content that moved across a page break reads as unchanged
- `DiffOperation{Operation, Text, SourcePage, DestPage, SourceRects, DestRects}` — `Operation` (`OperationEqual`/`Insert`/`Delete`) and `Text` mirror Aspose's `DiffOperation`; the page and rectangle fields are ours (Aspose carries coordinates only in the 26.7 side-by-side `EditContainer`). A run is a maximal sequence of adjacent words with one operation, broken at line and page boundaries, so each rectangle is a real box on a real page
- `ComparisonResult` — `HasChanges()`, `Operations()`, `PageOperations(n)`, `Statistics() ComparisonStatistics{EqualWords, InsertedWords, DeletedWords, ChangedPages}`, `SaveMarkup(path, opts...)` / `WriteMarkup(w, opts...)`
- `ComparisonOptions{ExtractionArea, ExcludeAreas1, ExcludeAreas2, ExcludeTables, EditOperationsOrder, IgnoreCase}` — mirrors theirs plus `IgnoreCase`; `ExtractionArea` together with `ExcludeTables`/`ExcludeAreas*` is an error, as in Aspose. `AssembleSourceText`/`AssembleDestinationText` rebuild either side from the operations (their `AssemblySourcePageText`/`AssemblyDestinationPageText`)
- **Tokenization** (`comparison_tokens.go`): one `ExtractTextWithLayout` per page; each line's text is split at whitespace and every word's rectangle comes from `buildLineRuneMap` + `matchRect` — the machinery `SearchText` uses, so right-to-left lines (logical order) and sub-fragment boundaries are handled identically. Filters decide by the rectangle's midpoint, so a word on a boundary belongs to exactly one region; `ExcludeTables` takes the bounding rectangles from `NewTableAbsorber().Visit`
- **Diff** (`comparison_diff.go`): common prefix/suffix trimmed; when trimming empties one side, the answer is immediate (one run of deletions or insertions — no search needed), otherwise Myers O(ND) runs over the word keys with the frontier snapshots walked back into an edit script; adjacent same-kind edits group into runs (broken at page changes, rectangles unioned per line). `maxEditDistance` (2000) caps the search — the cost grows with the square of the edit distance in both time and memory (the frontier snapshots, roughly 64 MB at the cap) — beyond it the comparer reports the whole source deleted and the whole destination inserted rather than grinding through, and allocating, the full square
- **Markup** (`comparison_markup.go`): `SaveMarkup`/`WriteMarkup` copy one side (serialize + reopen, so the caller's documents are untouched, and the output is a new document — signatures on the input do not survive the copy) and annotate it — destination side: insertions as `HighlightAnnotation` over the run's rectangles, deletions as a `CaretAnnotation` anchored between the neighbouring unchanged words with the removed text in `/Contents`; source side: deletions as `StrikeOutAnnotation`, insertions as carets. All carry `/T` (default `"Comparison"`), so a viewer's comments panel is the change list. The annotations draw their own appearance streams (`appearance_markup.go`), so the markup paints in our own renderer as well as in Acrobat. `DiffMarkupOptions{Side, InsertColor, DeleteColor, Title, Flatten}` (named apart from `MarkupSection`/`MarkupParagraph`/`PageMarkup` in `paragraph.go`, an unrelated namespace). Marking up an encrypted document is not supported — a document configured for encryption re-encrypts on the copy's `WriteTo`, so reopening it would fail with a bare `ErrEncrypted`; `copyDocument` detects this via `(*Document).Permissions()` and reports it with a wrapped `ErrEncrypted` instead
- Out of scope in phase 1: graphical (pixel) comparison, side-by-side output, the HTML/JSON/Markdown diff generators and the Aspose-style generated text report, character-level refinement inside a changed word, and de-hyphenation (a word broken across lines compares as two words)

**`flow.go`** — flow / document-generator layout (epic `pdf-go-m9kt`, Tier 1): the **flow counterpart to the Rectangle-based drawing API** — content is laid out top-to-bottom and paginated automatically, instead of positioned absolutely. An additive layer (it computes Rectangles and calls the existing `AddText`/`AddImage`/`AddTable`/`drawList`), so it coexists with the Rectangle paradigm. Mirrors the intent of Aspose.PDF for .NET's generator / `Page.Paragraphs` flow model
- `(*Document).NewFlow(opts FlowOptions) *Flow` — `FlowOptions{Format, Margin{Left,Right,Top,Bottom} (zero → 54pt), ParagraphSpacing (zero → 6pt), Tagged *TaggedContent}`. Builders (chainable): `AddParagraph`, `AddHeading(level, …)`, `AddImage(path, w, h)` / `AddImageAlt(…, alt)` (h ≤ 0 → preserve aspect via `image.DecodeConfig`), `AddTable(*Table)`, `AddList(items, ordered, style)`, `AddSpacer(h)`. `Render() (pages int, error)` lays everything out and returns the page span
- Engine: a `flowState` cursor (current page + Y) over a content box (page minus margins). `startPage` reuses the document's initial blank page (resized) when it has exactly one with no content, else appends. Paragraphs/headings **split across pages line-by-line** (`wrapText` → draw the lines that fit, new page, continue); images are atomic (scaled down to one content area if oversized); tables draw inline when they fit, on a fresh page otherwise, and fall back to `AddTable`'s own pagination when taller than a page; lists use the shared `drawList`. When `Tagged` is set, each element is auto-tagged (`/P`, `/H1…/H6`, `/Figure`+`/Alt`, `AddTaggedTable`, tagged `drawList`) so the flow output passes `ValidatePDFUA` — a one-call accessible report generator
- **Floating boxes** (`flow_box.go`, Tier 2): `FloatingBox` is a positioned content container with an optional border/background/padding holding its own content (`NewFloatingBox().AddParagraph/AddHeading/AddImage/AddList/AddSpacer().SetBorder/SetBackground/SetPadding/SetSpacing`). `SetSpacing(gap)` overrides the inter-element gap (default 6pt; `0` for a single-paragraph box so symmetric padding centres it vertically). The border honours `BorderInfo.Sides` — a single side renders as a rule (e.g. `BorderSideLeft` for a block-quote) rather than a full outline (`drawBoxFrame` draws per-side edges, only `BorderSideAll` strokes the whole rect). `(*Page).AddFloatingBox(box, rect)` places it absolutely (paints the frame, flows content within `rect`−padding, clips on overflow); `(*Flow).AddFloatingBox(box)` inserts it in the flow at its **measured** height (`measureFlowElems`; new page if it doesn't fit). The box lays out via a `boxed` `flowState` (`newPage` → `errBoxFull` stops instead of paginating) reusing all element drawing; when the document is tagged the box is a `/Div` (content tagged under it via the new `flowState.parent`) and its frame an `/Artifact`. Tables are not allowed inside a box (they paginate)
- **Multi-column layout** (Tier 3): `FlowOptions.Columns` (>1) + `ColumnGap` (zero → 18pt) divide the content box into N columns; the engine fills a column top-to-bottom then moves to the next via `flowState.advance` (column → next column → next page), with `colLeft()` giving the current column's X and `contentW` the per-column width. `(*Flow).AddColumnBreak()` forces the following content into the next column (or, from the last column, the next page) — used to put one section per column. Works with tagging (content stays under the structure tree across columns) and pagination; tables/boxes flow within the current column (a wide table in a narrow column overflows — use a single column for those). Boxed (`FloatingBox`) layout stays single-column (`advance` → `errBoxFull`)
- **Text flow-around** (`flow_float.go`, Tier 3): `(*Flow).AddFloatBox(box, FloatLeft|FloatRight, width)` draws a `FloatingBox` pinned to a column edge and registers an `activeFloat` band; the following paragraphs wrap **line by line** around it (`flowText` → `flowTextAround` when `len(floats) > 0`): each line's `[left,right]` comes from `lineBounds(y)` (column minus any active float band + `floatTextGap`), `nextLine` greedily fits words into that width, lines on one page are bracketed into a single tagged `/P` (`flush`), and the cursor returns to full width once it passes the float's `bottom` (`pruneFloats`). A line with no room beside the float drops to `nextFloatBottom`; non-wrapping elements (image/table/list/box) call `dropBelowFloats` to start on a clean line below the float. Floats are cleared on a column/page break (so they don't span pages). Designed for single-column flows; in a multi-column flow the box floats within the current column
- Tier-3 remaining (not yet): keep-with-next / widow-orphan control, image floats (only boxes float), splitting a single paragraph's tagging into one `/P` across pages (currently one `/P` per page-chunk)

**`validate.go`**
- `Validate(inputPath)` — checks a PDF for **structural integrity** (parseable + internally consistent); returns `*ValidationReport` with a `Valid` flag and a list of `ValidationIssue` (code + message). NOT a standards check: it does not validate PDF/A or PDF/UA. This differs from Aspose.PDF for .NET's `Document.Validate`, which checks PDF/A·PDF/UA conformance — the capability here is intentionally narrower
- Issue codes: `INVALID_HEADER`, `XREF_ERROR`, `OBJECT_ERROR`, `PAGE_TREE_ERROR`, `STREAM_ERROR`, `ENCRYPTED`
- Checks performed: header, xref/trailer, all objects readable, page tree traversal, orphaned `/Pages` nodes, `/Page` → `/Parent` refs resolve to `/Pages`, streams without `/Filter` don't contain compressed data, image XObjects declare the required `/ColorSpace` + `/BitsPerComponent` (ISO 32000-1 §8.9.5; image masks and JPXDecode images exempt — `validateImageXObjects`). Rendering stays tolerant where Acrobat refuses; the check is Acrobat-compatibility diagnostics only

**`validate_pdfa.go`** — PDF/A archival-conformance validator (epic `pdf-go-s2n`, phase 1). Mirrors the intent of Aspose.PDF for .NET's `Document.Validate(PdfFormat)` but returns a structured report instead of an XML log
- `(*Document).ValidatePDFA(format PDFAFormat) *PDFAValidationReport` — read-only diagnostic (never mutates); the write-side counterpart is `ConvertToPDFA` (`pdfa_convert.go`, below)
- `PDFAFormat` enum — both conformance levels: `PDFA1B`/`PDFA2B`/`PDFA3B` (basic) and `PDFA1A`/`PDFA2A`/`PDFA3A` (accessible). `part()` → 1/2/3; `conformance()` → `"A"`/`"B"`. The **"a"** levels add the Tagged-PDF requirement: `pdfaCheckTagged` (run only for `conformance()=="A"`) flags `NOT_TAGGED` (`/MarkInfo /Marked`), `NO_STRUCT_TREE` (`/StructTreeRoot`) and `NO_LANG` (`/Catalog/Lang`) — author the structure tree with `(*Document).TaggedContent` (`tagged.go`). The XMP check matches `pdfaid:conformance` against the requested letter
- `PDFAValidationReport` struct — `Format`, `Conformant bool`, `Issues []PDFAIssue`; `PDFAIssue` — `Rule` (stable code) + `Message`
- Rules + codes: `XMP_MISSING`/`XMP_PDFAID_MISSING`/`XMP_PART_MISMATCH`/`XMP_CONFORMANCE_MISMATCH` (XMP packet with matching `pdfaid:part`/`pdfaid:conformance`, regex-read from `XMPRaw`), `FONT_NOT_EMBEDDED` (every Type1/TrueType/Type0 font **a content stream actually selects with `Tf`** carries `/FontFile`/`/FontFile2`/`/FontFile3`; Type3 is glyph-stream-embedded; CIDFont descendants checked via their Type0 parent). Usage comes from `pdfaUsedFonts` (`pdfa_fonts.go`), which walks every content stream — pages, Form XObjects incl. annotation appearances, Type3 glyph procedures — and resolves each `Tf` name through that stream's resources: PDF/A requires embedding for the fonts used to render (ISO 19005-1 §6.3.4), and a face left in a resource dict (every Acrobat form keeps ZapfDingbats in `/AcroForm/DR`) renders nothing. Checking every font object instead raised a violation veraPDF does not — 130 of 1,009 corpus documents were flagged for a font nothing drew with, `ENCRYPTED`, `JAVASCRIPT`/`LAUNCH_ACTION` (scan `/Names/JavaScript`, `/OpenAction`, and every action dict's `/S`), `COLOR_NO_OUTPUT_INTENT` (a page content stream uses a device-colour operator `rg`/`RG`/`g`/`G`/`k`/`K` but the catalog has no `/OutputIntents` with a `/DestOutputProfile`), `TRANSPARENCY` (PDF/A-1 only: transparency group, `/SMask` ≠ `/None`, non-Normal `/BM`, or `/ca`/`/CA` < 1), `ANNOTATION_FLAGS`/`ANNOTATION_NO_AP` (Print flag set, Hidden/NoView clear, non-Link visible annots have `/AP`), `METADATA_COMPRESSED` (the `/Metadata` stream is unfiltered), `XMP_MALFORMED` (the packet does not parse as XML), `LZW_FILTER` (PDF/A-1 only), `EMBEDDED_FILES` (PDF/A-1 only — a `/Names/EmbeddedFiles` entry **or** a non-empty catalog `/AF`, since an associated file reaches its bytes through either). Validated on the live in-memory document (Standard-14 fonts flagged, embedded TTF not flagged)

**`pdfa_convert.go`** — PDF/A conformance writer (epic `pdf-go-s2n`, phase 2). Mirrors the intent of Aspose.PDF for .NET's `Document.Convert(PdfFormat)`
- `(*Document).ConvertToPDFA(format PDFAFormat) (*PDFAValidationReport, error)` — applies the structural/metadata fixes the library can make safely in place (mutates the document; `Save`/`WriteTo` writes it) and returns a fresh `ValidatePDFA` report of whatever still fails. When `Conformant` is true the document satisfies the validator's checks
- Transforms: `RemoveEncryption`; strip JavaScript/Launch actions (catalog `/OpenAction`/`/AA`, `/Names/JavaScript`, per-annotation `/A`/`/AA`, then delete the now-orphaned action objects); remove `/Names/EmbeddedFiles` for PDF/A-1; set annotation `/F` (Print on, Hidden/NoView off); **draw missing annotation appearances** (`generatePDFAAppearances`: form-field widgets through `regenerateWidgetAppearance`, drawing/markup annotations through their own `RegenerateAppearance`; a conforming reader may not synthesize one, so a widget with no `/AP` would vanish) and clear `/AcroForm/NeedAppearances`; **auto-embed the simple fonts the document draws with** (`embedStandardFonts`/`buildEmbeddedSimpleFont`, see below); **embed composite (CJK) fonts as a subset** (`embedCompositeFonts`, `pdfa_fonts.go`); add an sRGB ICC **OutputIntent** (`/S /GTS_PDFA1`, `/DestOutputProfile` → ICC stream); write a `pdfaid` **XMP** packet synced from `/Info` (uncompressed, so it also clears `METADATA_COMPRESSED`)
- **Standard-14 / non-embedded simple-font embedding**: every non-embedded `Type1`/`TrueType`/`MMType1` font is rewritten in place (same object number, so content streams and resource refs are untouched) into an embedded `/Subtype /TrueType` font backed by the **bundled metric-compatible substitute** (`fallbackFontFor`: Arimo↔Helvetica/Arial, Tinos↔Times, Cousine↔Courier, …, the same clones the renderer uses). The original `/Encoding` is preserved and `/Widths` (`/FirstChar` 0 `/LastChar` 255) are computed from the substitute's *own* glyph advances (`ttfFont.glyphID` → `glyphWidths`, scaled to 1000/`unitsPerEm`) so the dictionary and the embedded program agree, as PDF/A requires. Because the clones are metric-compatible, layout and word-wrap are unchanged and text still extracts/renders correctly. `Symbol`/`ZapfDingbats` have no Latin substitute (`fallbackFontFor` returns nil) and stay a reported violation; composite (Type0/CJK) and Type3 fonts are out of scope
- **Accessible ("a") levels**: `ConvertToPDFA(PDFA1A/2A/3A)` runs the same transforms and writes `pdfaid:conformance "A"`, but does not build the structure tree (that is the caller's job via `TaggedContent` — auto-tagging arbitrary content is out of scope). The returned report flags `NOT_TAGGED`/`NO_STRUCT_TREE`/`NO_LANG` when the document was not tagged first. A document authored with `TaggedContent` then converted with `PDFA1A` is fully conformant
- `srgbICCProfile()` builds a minimal but valid **ICC v2.1 RGB display profile** in pure Go (128-byte header with `acsp`/`mntr`/`RGB `/D50 illuminant, tag table, `desc`/`wtpt`/`r,g,bXYZ` sRGB D50-adapted primaries/`r,g,bTRC` gamma-2.2 curves/`cprt`), so no external `.icc` file is bundled. The stream is Flate-compressed by the writer
- **Composite (CJK) font embedding** (`pdfa_fonts.go`): `pdfaUsedCIDs` decodes every show operator through the font's own CMap (predefined CJK encodings yield real CIDs; Identity yields the raw code), and `embedCompositeFonts` subsets the face `fontRepo.findCJK` would render with — a collection sub-font re-wrapped via `standaloneSFNT` first — reaching glyphs as the renderer does (CID → Unicode through the ordering table or `/ToUnicode` → glyph). The rebuilt descendant carries `/FontFile2`, a `/CIDToGIDMap` stream translating the document's CIDs into the subset's glyphs, and `/W` from the subset itself. Only used glyphs are embedded (a whole CJK face is 10-16 MB; a converted corpus document grew 3 KB → 35 KB)
- **Symbol/ZapfDingbats**: no Latin clone carries their glyphs, so `symbolFaceFor` takes a face registered through `AddFontFile`/`AddFontFolder`, else the installed face of the same family (`findSystemFamily` — matching by family, since the installed Symbol's PostScript name is `SymbolMT`). Codes reach glyphs through `substituteGlyphFor`, which falls back to the `(3,0)` symbol cmap in the 0xF000 page when the character has no Unicode mapping. **Embedding permissions are honoured** everywhere: `ttfFont.embeddingAllowed` reads the OS/2 `fsType`, so a face its vendor marks non-embeddable (bit 1) or bitmap-only (bit 9) is never written into a document
- Corpus: **971 of 1,003** documents convert to a clean PDF/A-2b report (766 before this pass). What remains: ZapfDingbats with no face to embed (25), a font whose family is not installed, icon annotations (`/Text`, `/FileAttachment`) that draw no appearance of their own, and PDF/A-1 transparency flattening (use PDF/A-2, which permits transparency). Full conformance should still be confirmed with a dedicated PDF/A validator (veraPDF needs a JRE, absent here) — this library validates against its own rule set

**`validate_pdfua.go`** — PDF/UA-1 accessibility validator (epic `pdf-go-y5z`, phase 1). Mirrors the intent of Aspose.PDF for .NET's `Document.Validate(PdfFormat.PDF_UA_1)`
- `(*Document).ValidatePDFUA() *PDFUAValidationReport` — read-only diagnostic; `PDFUAValidationReport` = `Conformant bool` + `Issues []PDFUAIssue` (`Rule` + `Message`). The tagged-PDF *authoring* side (writing the structure tree + marked content) is `TaggedContent` (`tagged.go`, below)
- Rules + codes: `UA_NOT_TAGGED` (catalog `/MarkInfo /Marked true`), `UA_NO_STRUCT_TREE`/`UA_STRUCT_TREE_EMPTY`/`UA_NO_PARENT_TREE` (a `/StructTreeRoot` with structure-element kids and a `/ParentTree`), `UA_NO_LANG` (`/Catalog/Lang`), `UA_NO_TITLE` (`/Info` or XMP title) + `UA_DISPLAY_DOCTITLE` (`/ViewerPreferences /DisplayDocTitle true`), `UA_FIGURE_NO_ALT`/`UA_FORMULA_NO_ALT` (walks the structure tree resolving `/RoleMap`; every `/Figure`/`/Formula` element needs `/Alt` or `/ActualText`), `UA_INACCESSIBLE` (encryption must allow the accessibility permission). Internals: `structElemKids` follows `/K` to child structure elements (skipping MCID ints / `/MCR` / `/OBJR`); `resolveStructType` maps a type through `/RoleMap` with cycle protection. Recognises real tagged PDFs (e.g. testdata `4pages.pdf`). Scope: structural prerequisites only — not full reading-order, heading-nesting or table-scope analysis (use PAC / veraPDF-UA for final sign-off)

**`tagged.go`** — Tagged PDF authoring (epic `pdf-go-y5z`, phase 2): build a logical structure tree + marked content as the document's drawing API emits content, producing accessible (PDF/UA-conformant) output. Mirrors the intent of Aspose.PDF for .NET's `Document.TaggedContent` / `ITaggedContent`
- `(*Document).TaggedContent() *TaggedContent` — idempotent facade; on first call it creates the `/Document` root structure element, the `/StructTreeRoot` and a `/ParentTree`, and sets the catalog `/MarkInfo /Marked true` + `/ViewerPreferences /DisplayDocTitle true`. `(*TaggedContent)` — `SetTitle(s)` (→ `/Info` title), `SetLanguage(s)` (→ `/Catalog/Lang`), `Root() *StructElement`
- `(*Page).TagContent(parent *StructElement, t StructType, draw func() error) (*StructElement, error)` — the core primitive: brackets everything `draw` emits in a `/<type> <</MCID n>> BDC … EMC` marked-content sequence (via `appendToContentStream`), creates a leaf `/StructElem` (`/S`, `/P`, `/Pg`, `/K` = the MCID) as a child of `parent` (nil = root), assigns the page a `/StructParents` index and records MCID→element in the `/ParentTree` (`rebuildParentTree`). Any drawing (`AddText`, `Draw*`, `AddImage`, `AddTable`) inside the callback is tagged
- `(*StructElement)` — `AddChild(t StructType) *StructElement` (a grouping element: `/Table`→`/TR`→`/TD`, `/L`→`/LI`, …, holds children rather than content), `SetAlt(s)` / `SetActualText(s)` / `SetLanguage(s)`. `addKidRef` promotes `/K` to an array as children are appended
- `StructType` — the `/S` value (with leading slash): `StructDocument`, `StructP`, `StructH1`..`StructH6`/`StructH`, `StructSpan`, `StructFigure`, `StructFormula`, `StructTable`/`StructTR`/`StructTH`/`StructTD`/`StructTHead`/`StructTBody`, `StructList`/`StructListItem`/`StructLabel`/`StructListBody`, `StructLink`, `StructCaption`, `StructSect`/`StructDiv`/`StructPart`, …
- `(*Page).AddTaggedTable(tc, parent, t *Table, rect) (*StructElement, int, error)` (`tagged_table.go`) — renders a `*Table` like `AddTable` **and** builds its logical structure: a `/Table` element under `parent`, a `/TR` per row (reused across page breaks via `tableTagger.rows`), and a `/TH`/`/TD` per cell (the first `RepeatingRowsCount` rows → `/TH`). Each cell's content is tagged through `TagContent`; cell backgrounds, cell borders and the outer border are bracketed as `/Artifact BMC … EMC` (via `(*Page).artifact`) so they are excluded from the structure, as PDF/UA requires. Mechanism: an unexported `tagger *tableTagger` on `Table` that `drawRowRange`/`drawOuterBorder` consult — the non-tagged path is byte-identical when it is nil. Paginates and round-trips conformant
- `(*Page).AddTaggedList(tc, parent, items []string, style, rect, ordered) (*StructElement, error)` (`tagged_list.go`) — draws a bulleted (`•`) or numbered (`1.`) list and builds `/L → /LI → /Lbl`+`/LBody` (one `/LI` per item; the label and the wrapped body are each tagged via `TagContent`). Items stack top-down with `measureText` heights; any that would overflow `rect` are dropped (no list pagination yet)
- `(*Page).TagArtifact(draw func() error) error` (`tagged_list.go`) — public wrapper over the internal `artifact` helper: brackets decoration (headers/footers, page numbers, backgrounds, rules) in `/Artifact BMC … EMC` so it is excluded from the structure tree, as PDF/UA requires for non-structural content
- Bookkeeping lives on `TaggedContent` (`tagged` field on `Document`): per-page MCID counter, `/StructParents` index, and the per-page MCID→element array; the structure objects are added to `d.objects` eagerly so they serialise (and refs remap) through the normal writer. A document authored this way passes `ValidatePDFUA` and round-trips. The authoring toolkit now covers paragraphs/headings (`TagContent`), figures (`+SetAlt`), tables (`AddTaggedTable`), lists (`AddTaggedList`), artifacts (`TagArtifact`) and arbitrary grouping (`AddChild`). Remaining polish: list/heading reading-order validation, and richer auto-tagging of free `Add*` content

**`raster.go` / `raster_path.go` / `raster_stroke.go` / `render.go` / `render_device.go` / `render_image.go` / `render_glyph.go` / `render_text.go` / `render_clip.go` / `render_gstate.go` / `render_shading.go` / `render_annotations.go` / `render_document.go` / `render_tiling.go` / `render_type3.go` / `render_dingbats.go` / `cff.go` / `type1.go` / `type1_charstring.go` / `bmp.go` / `tiff.go`** — pure-Go page rasterizer (no external dependencies; stdlib `image`/`image/png`/`image/jpeg`/`image/gif`/`compress/zlib` only). Phased epic (umbrella `pdf-go-61r`); spec `docs/superpowers/specs/2026-06-04-render-to-image-design.md`. Status: **render epic complete (P1–P6 + annotations + multi-page TIFF)** (vector + images + text + clip/alpha/shadings + annotation appearances + stroke quality → PNG/JPEG/GIF/BMP/TIFF). P2 adds Image XObjects (decoded via the existing image pipeline + `/SMask` alpha, inverse-mapped onto the page) and Form XObject recursion (`render_image.go`) plus an in-house BMP encoder (`bmp.go`). P3 adds text: a TrueType glyph-outline decoder (`render_glyph.go`, simple + composite glyphs from the `glyf` table) and a text-object state machine (`render_text.go`) that fills glyphs for **embedded** TrueType fonts (Type0/CIDFontType2 + `/FontFile2`). P4 (`render_std14.go`) renders **non-embedded** fonts (Standard 14) by substituting glyph shapes from bundled **metric-compatible** open families — Arimo (Helvetica/Arial), Tinos (Times), Cousine (Courier), Carlito (Calibri — a narrower face than Arial, so without it Calibri fell back to the wider Arimo and its glyphs overran the document's Calibri advances), SIL OFL, subset to Latin and `//go:embed`ed. Because their advance widths equal the originals, word-wrapped layout is preserved and glyphs aren't distorted; serif/mono and bold/italic render distinctly. `render_fontrepo.go` adds a `FontRepository` (mirrors Aspose.PDF for .NET): `AddFontFolder`/`AddFontFile`/`AddSystemFonts` register sources used before the bundled substitute. Resolution for a non-embedded simple font: registered font → installed metric-equivalent for the Standard-14 families (`findSystemStd14`: Courier→Courier New, Helvetica→Arial, Times→Times New Roman — the same substitution Acrobat performs; bundled clones differ visibly, e.g. Cousine's low-serif design vs Courier New's slab serifs) → bundled substitute. Internals (lowercase): `flattener` adaptively flattens Bézier/arc paths to device-space polylines; `rasterizer` produces anti-aliased coverage via analytic-X + 4× supersampled-Y scanlines (nonzero + even-odd); `compositeCoverage` does src-over with an optional clip mask; `strokeToFill` (`raster_stroke.go`) builds the stroke outline with real line caps (butt/round/square), line joins (miter — with miter-limit→bevel fallback — / round / bevel) and dash patterns (`applyDash`); `renderer` interprets the content stream (q/Q/cm/w, path ops, f/S/B painting, Gray/RGB/CMYK colour, text with `Tr` rendering modes — fill/stroke, invisible mode 3 paints nothing, clip modes 4-7 accumulate the shown glyph outlines and intersect them into the graphics-state clip at `ET` (`applyTextClip`, ISO 32000-1 §9.4.3) so the common "show glyphs under `7 Tr`, then paint a full-page image/fill through them" idiom renders as glyph-shaped text, `Do` images, `BI` inline images, `gs`, `W`/`W*` clip, `sh`, `J`/`j`/`M`/`d` stroke params, `cs`/`CS` colour spaces, `BDC`/`EMC` Optional-Content visibility) and **skips the few operators not yet supported** so any page renders. Stencil image masks (`/ImageMask true`, inline `/IM true`, `render_imagemask.go`) paint the current fill colour through their 1-bit "on" samples (per `/Decode`), inverse-mapped through the CTM. A `/JBIG2Decode` image mask is JBIG2-decoded first (its globals come from `/DecodeParms`, which `decodeStream` can't reach), since otherwise the raw compressed bytes would be painted as the mask. Blend modes (`render_blend.go`) are honored: `gs` `/BM` selects a `blendMode` on the graphics state — separable (Multiply/Screen/Overlay/Darken/Lighten/ColorDodge/ColorBurn/HardLight/SoftLight/Difference/Exclusion, per channel) or non-separable (Hue/Saturation/Color/Luminosity, on the whole colour via `Lum`/`Sat`/`SetLum`/`SetSat`/`ClipColor`). The compositors funnel through `blendApply` as `C = (1−a)·Cb + a·B(Cb,Cs)` for an opaque backdrop; Normal/unsupported keep the fast src-over path. Type3 fonts (`render_type3.go`) render each glyph by executing its `/CharProcs` content stream with `FontMatrix · text-rendering matrix · text matrix · CTM`, reusing the interpreter; the code→glyph-name table comes from `/Encoding/Differences`. Optional Content (`render_oc.go`): content tagged with an OCG/OCMD that is OFF in `/Catalog/OCProperties/D` — via a `/OC` marked-content section (`BDC`…`EMC`) or an `/OC` entry on an XObject — is suppressed (OCMD policies AnyOn/AllOn/AnyOff/AllOff honored). After the content stream it paints annotation appearance streams (`render_annotations.go`): every `/Annots` entry's normal appearance (`/AP/N`, selected by `/AS` for state subdictionaries) is drawn as a Form XObject mapped into `/Rect` per ISO 32000-1 §12.5.5 — so AcroForm field widgets, stamps, highlights, free text, etc. render the way a viewer shows them; `/Popup` and Hidden/NoView (`/F` bits 2/6) annotations are skipped. Annotations are drawn in **two passes — non-widgets first, then form-field widgets on top** — mirroring how Acrobat/pdf.js render interactive form fields as a separate top layer (`/Annots` order preserved within each pass); without this a markup annotation later in `/Annots` could hide a field's value (39103.pdf white-outs its baked template text with opaque white `/Square` boxes layered over the placeholder widgets — strict `/Annots` order erased every field). A `/Tx`/`/Ch` widget first gets an opaque background (`/MK/BG`, else white) so baked page content does not show through the field (`widgetFieldBackground`). P5 (`render_clip.go`/`render_gstate.go`/`render_shading.go`): `W`/`W*` clipping intersected into a per-pixel coverage mask (`gs.clip`, applied after the next paint per ISO 32000-1 §8.5.4, saved/restored by q/Q); constant alpha via `gs` → `/ca`/`/CA`/`/LW` (translucent content blends); soft masks via `gs` `/SMask` (`render_softmask.go`): the group `/G` is rendered off-screen and reduced to a per-pixel luminosity/alpha mask that `effectiveClip` folds into every paint (saved/restored by q/Q, cleared by `/None`); axial (type 2) and radial (type 3) shadings via `sh` and shading patterns (PatternType 2), with a m-in/n-out function evaluator (`render_shading.go` + `render_function_ps.go`): types 0 sampled, 2 exponential, 3 stitching, function arrays, and 4 PostScript calculator (a real stack interpreter — arithmetic/stack/comparison/`if`/`ifelse`) and colour model inferred from output-component count. Separation and DeviceN colour spaces (`render_colorspace.go`): `cs`/`CS` resolve the space; on `sc`/`scn` the tint operands run through the tint-transform function (commonly a Type 4) to RGB. Tiling patterns (PatternType 1, `render_tiling.go`) are rendered by executing the cell content stream once per tile across the fill path's bbox (stepped by `/XStep`/`/YStep` through `/Matrix`), clipped to the path ∩ current clip; uncolored (PaintType 2) cells inherit the fill colour set with the pattern; the tile count is capped at `maxTiles`. P6 adds stroke quality (caps/joins/dash above) and the performance pass: `coverageBBox`/`compositeCoverageBBox` allocate and scan only a path's pixel bounding box instead of the whole frame (glyphs/fills are tiny), cutting the 13-page showcase from ~35 s to ~1.1 s at 150 DPI (~30×). `cff.go` adds CFF/Type2 charstring outlines (`pdf-go-l7b`): `parseCFF` reads the CFF container (INDEXes, Top/Private DICTs, charset, FDArray/FDSelect for CID-keyed) and a Type2 interpreter flattens charstrings into the same on-curve `glyphContour` the glyf decoder emits. Embedded OpenType-CFF (`/FontFile3`: Type1C / CIDFontType0C / OpenType) is resolved in `buildRenderFont` via `parseCFFProgram` (bare CFF or `OTTO` wrapper); `.otf` registered in the `FontRepository` is parsed as an sfnt whose `'CFF '` table attaches to `ttfFont.cff`, so `glyphContours` draws from it. `renderFont` carries either `prog` (TrueType) or `cff`; `gid()` maps CID→GID through the CFF charset for CIDFontType0C. **Simple** (non-Type0) CFF fonts (Type1C `/FontFile3`, e.g. MinionPro subsets) build a code→GID map (`cffFont.simpleGID`, `buildSimpleEncoding`): the CFF charset gives GID→SID and the Encoding gives code→glyph — for the predefined Standard encoding a code's SID is `code−31` across 32–126, reversed through the charset; a custom Encoding table (formats 0/1) is read directly. A **predefined** charset (Top DICT charset offset 0 = ISOAdobe) is materialised as the identity GID→SID map so subset fonts that keep the standard glyph order (e.g. NewsGothicBT) still resolve. Without this, simple CFF fonts mapped every code to GID 0 and rendered no text. When the PDF font dict carries its own `/Encoding` (WinAnsi etc.), it **overrides** the font's built-in encoding per ISO 32000-1 §9.6.6.2: the resolved `fi.encoding` rune reaches the glyph through `cffFont.runeToGID` — charset SID → glyph name (`cffStdStrings` 0–390 / String INDEX above) → AGL rune (`cffGlyphNameRune`, incl. uniXXXX/uXXXX) — with the built-in encoding as fallback; subset fonts often embed a built-in encoding covering only a few codes, which previously left pages nearly blank (56333.pdf). The render font cache is keyed by the font's **object number**, not the resource name — a page and a nested Form XObject may both define `/T1_0` pointing at different fonts. (`.otf` embedding via `LoadFont` is now supported — written as `/FontFile3` + `CIDFontType0`, see `embedCFFFont`; this render path draws it back through the same `parseCFFProgram`/`gid()` CFF machinery.) Non-embedded ZapfDingbats (`render_dingbats.go`) synthesizes vector outlines for the common marks — Acrobat's checkbox/radio "check styles": check `4`/`5`, cross `6`/`7`/`8`, circle `l`, square `m`/`n`, diamond `u`, star `H` — built in 1000-em space and painted via the shared `paintContours`; advance widths come from the ZapfDingbats AFM, so checkbox/radio widget appearances render. Embedded **classic Type1** fonts (`/FontFile`, `type1.go` / `type1_charstring.go`, `pdf-go-6w5`) are parsed in-house: the program is split into clear text / eexec / trailer by `/Length1`/`/Length2` (or the `eexec` keyword), the binary portion is eexec-decrypted (R=55665), and the Private dict's `/Subrs` + `/CharStrings` are read and charstring-decrypted (R=4330, `/lenIV`). A Type1 charstring interpreter (`t1Interp`: hsbw/sbw, r/h/v-moveto/lineto, rr/hv/vh-curveto, closepath, callsubr/return, div, seac accent composition, and callothersubr flex + hint-replacement via the PS stack) flattens each glyph into the same `glyphContour` the glyf/CFF decoders emit. `renderFont` gains a `t1` program alongside `prog`/`cff`; `gid()` resolves a simple font's code → glyph name (PDF `/Differences` → base-encoding standard name → the font's built-in `/Encoding`) → synthetic GID. PFA hex-eexec and PFB-segmented (`0x80`) containers are both accepted. Transparency groups (`render_group.go`, `pdf-go-rom`): a Form XObject with `/Group /S /Transparency` drawn under a group-level alpha, non-Normal blend or soft mask is rendered off-screen over a transparent backdrop (isolated-group semantics, the standard approximation for non-isolated too) and the flattened result is composited once via `compositeGroup` — fixing the classic per-element double-darkening of a 50%-opacity group; an opaque Normal-mode group keeps the cheap inline path (`groupNeedsComposite`). The renderer also has a `suppressText` flag (glyph fill/stroke skipped, text-clip accumulation for Tr 4-7 kept) used by the HTML exporter to produce graphics-only backgrounds. Knockout groups (`/K true`) run their sub-renderer with `renderer.knockout`, which routes the vector paint path (fills/strokes/glyphs) through `compositeCoverageKnockout` — within its coverage an element **replaces** the accumulated backdrop (`C' = Cs·a·cov + Cb·(1−cov)`) instead of compositing over it; images inside knockout groups still composite normally (documented limitation). Out of render scope: mesh shadings (ShadingType 4–7), non-embedded Symbol glyph shapes and the rarer decorative ZapfDingbats marks (`pdf-go-ylj`: code→Unicode encoding is wired so embedded/registered fonts render, but no Latin substitute is forced — non-embedded Symbol with no covering font draws nothing rather than .notdef boxes).
- `RenderOptions` struct — `DPI float64` (0 → `DefaultDPI` = 150), `Background *Color` (nil → white)
- `(*Page).RenderImage(opts) (image.Image, error)` — rasterize the page (CropBox region, Y-flip/rotation, chosen DPI) to an `*image.RGBA`; never errors on unsupported content (skips it)
- `(*Page).RenderPNG(w, opts) error` / `RenderJPEG(w, opts, quality) error` / `RenderGIF(w, opts) error` / `RenderBMP(w, opts) error` / `RenderTIFF(w, opts) error`
- `(*Document).RenderImage(pageNum, opts) (image.Image, error)`
- `(*Document).RenderTIFF(w, opts, pageNums...) error` — renders the document (or the listed 1-based pages, in order) into a **single multi-page TIFF**; pages are rendered one at a time so peak memory is one page image (`render_document.go`, `tiff.go`). The TIFF encoder is in-house (stdlib has none; `golang.org/x/image/tiff` would be an external dep): little-endian, RGB8, one strip/page, Deflate via `compress/zlib` (Compression tag 8), chained IFDs for multi-page, DPI in X/YResolution. Validated against Pillow (opens 13-page output, detects `tiff_adobe_deflate`). Mirrors Aspose.PDF for .NET's TiffDevice
- Aspose-style device wrappers (mirror Aspose.PDF for .NET): `Resolution` (`NewResolution(dpi)`), `PngDevice`/`JpegDevice`/`GifDevice`/`BmpDevice` (`NewPngDevice(res)` etc.) with `Process(page, w) error`; `TiffDevice` (`NewTiffDevice(res)`) with `Process(page, w) error` (single page) and `ProcessDocument(doc, w) error` (whole-doc multi-page)
- `AddFontFolder(path)` / `AddFontFile(path)` / `AddSystemFonts()` / `ClearFontSources()` — register fonts the renderer uses for non-embedded text before the bundled metric-compatible substitutes; mirrors Aspose.PDF for .NET's `FontRepository`. System fonts are opt-in. Indexes `.ttf` and `.ttc` (TrueType Collection — each sub-font indexed separately via `parseFontCollection`/`parseSFNTAt` in `ttf.go`; each sub-font's table directory is captured on `ttfFont.tables` so shared-table glyph reads resolve at the right offset). Also indexes `.otf` (OpenType-CFF): parsed as an sfnt whose `'CFF '` table attaches to `ttfFont.cff`, so its glyphs render through the CFF interpreter (`cff.go`). Matching is by the font's real name-table **family** (name ID 1) + style, with Standard-14 aliases (Helvetica→Arial, Times→Times New Roman, Courier→Courier New) — not a serif/mono/sans keyword guess — so a request never resolves to an unrelated face; no family match → bundled substitute

**`markdown.go` / `markdown_ast.go` / `markdown_parse_block.go` / `markdown_parse_inline.go` / `markdown_refdef.go` / `flow_runs.go`** — Markdown → PDF (epic `pdf-go-fh4l`, complete); mirrors Aspose.PDF for .NET's `MdLoadOptions` in this library's exported-func idiom. Design: `docs/superpowers/specs/2026-07-14-markdown-to-pdf-design.md`
- **Parser**: a faithful port of the CommonMark parsing strategy (open-block tree with tab-stop columns and lazy continuation; inline phase with the delimiter-stack emphasis algorithm, links/images incl. full/collapsed/shortcut references, code spans, autolinks, raw-HTML recognition, entities via stdlib `html`). **Passes the official CommonMark 0.31.2 suite 652/652** (`TestCommonMarkSpec` runs the vendored `testdata/commonmark_spec.json` through a test-only AST→HTML serializer; the floor asserts 652 so any regression fails). GFM extensions ride the same machinery: tables (header line + delimiter row transformation), `~`/`~~` strikethrough, task-list markers (detected before inline parsing, `markTaskItems`), bare URL/www/email autolinks (`gfmLinkify` — on in `parseMarkdown`, off in the strict-core spec harness)
- **Public API**: `MarkdownToDocument(path, opts...)` / `MarkdownToDocumentFromStream(r, opts...)` → new `*Document`; `(*Flow).AddMarkdown(md)` (+ `SetMarkdownOptions`) inserts blocks into an existing flow; `(*Page).AddMarkdown(md, rect, opts...)` renders into a rectangle (boxed, clips like FloatingBox). `MarkdownOptions{Format, Margin, BaseFont/BoldFont/ItalicFont/BoldItalicFont/CodeFont, FontFamily/CodeFontFamily, BaseSize, BasePath, Tagged, Title, Language}` — zero value = A4/54pt/Helvetica+Courier/11pt (Std-14 = Latin-1; set `FontFamily`/`CodeFontFamily` — resolved via `LoadFontByName`, per-style fallback to regular — or pass `LoadFont` faces for Cyrillic etc.)
- **Renderer** (`markdown.go`, one default theme): scaled bold headings (h1 ≈ 1.9×base), styled runs (bold/italic/strike, gray-backed code spans, colored underlined links carrying real `LinkAnnotation`+URI), nested bullet/ordered lists with hanging labels, GFM task checkboxes drawn as vector boxes (green check), block quotes with a left rule (`FloatingBox`-style bar), code blocks on a gray card with **preserved indentation** (drawn line-by-line because `AddText` word-wrap strips leading spaces), GFM tables via the `Table` engine (bold repeating header, `:---:` → per-column `HAlign`), thematic breaks, images (local paths against `BasePath`, `data:` URLs via temp file; **remote URLs render an alt-text placeholder — no network by design**). Raw HTML skipped (inline `<br>` honored). Pagination throughout; implemented as `fkCustom` flow-element closures over `flowState`
- **`flow_runs.go`** (`fkRuns`): styled-runs paragraph layout — greedy word wrap **across run borders** (cross-run words = unbreakable clusters), shared baseline per line under mixed sizes, same-run segments coalesce (continuous underline/link rects), inter-run spaces advance x positionally (AddText strips leading spaces). Reusable by a future HTML-lite importer
- **Tagged output**: `MarkdownOptions.Tagged` → structure tree (H1-H6/P, L→LI→Lbl, `AddTaggedTable`, Figure+Alt from `![alt]`, decorations as artifacts) + title (from opts or the first heading) + language (default "en") — output passes `ValidatePDFUA` and round-trips
- **AI synergy**: `ai.SummaryOptions.Markdown` asks the model for Markdown and renders `GetSummaryDocument`/`SaveSummary` through this pipeline (formatted summaries — LLMs emit Markdown natively)
- Known limits (v1): no syntax highlighting; footnotes/definition lists/alerts unsupported; internal `#anchor` links render styled but carry no GoTo; inter-run extraction spacing tracked as `pdf-go-bfnc`

**`docx_write.go` / `docx_parts.go`** — PDF → DOCX export (epic `pdf-go-7qiu`); mirrors Aspose.PDF for .NET's `Document.Save(path, SaveFormat.DocX)` + `DocSaveOptions`
- `(*Document).SaveDocx(path, opts...)` / `WriteDocx(w, opts...)` — reconstruct the document as an editable Word file. `DocSaveOptions{Pages, Mode DocRecognitionMode, NoImages, NoPageBreaks}`; `DocEnhancedFlow` (default) reconstructs detected ruled tables as real `w:tbl` (grid from detector column boundaries, `gridSpan`/`vMerge`, cell shading from captured fill colours, per-cell alignment, styled-run cell text; covered paragraphs/patches leave the flow); `DocFlow` keeps tables-as-pictures; `DocTextbox` (`docx_textbox.go`) is the **fixed-layout** mode mirroring Aspose's Textbox: no reconstruction heuristics — every text line-cell (lines split at wide gaps via the stream detector's `splitStreamRow`, so column-straddling lines and tab stops keep each piece at its own X/Y) becomes an absolutely positioned floating **VML text box** (`w:pict` + `v:shape` t202, page-relative, zero insets, no wrap — VML because ECMA-376 transitional lacks the 2010 `wps` namespace, and `w:pict` admits VML with `processContents="lax"`, so strict XSD stays clean), raster images and vector-graphics patches become `wp:anchor` pictures at their PDF rects (a ruled table arrives pixel-exactly as its cluster patch), and every source page is its own Word section with the page's exact size/orientation (mixed portrait+landscape preserved). v1 limitations: rotated text (watermarks) skipped, vector underline/strikethrough rules dropped, text inside a graphics cluster lives in the patch
- A pure serializer over the `flow_doc.go` core: `docSeg` classification → heading paragraphs with **named styles `Heading1-6`** (each carrying `w:outlineLvl`, so Word's navigation pane and `{TOC}` fields see the structure — direct formatting would be invisible there), list items with `w:numPr` (a **fresh `w:num` per list instance** so ordered lists restart; nesting via `listNestDepth` capped at ilvl 8), shaded monospace code paragraphs (one `w:br` per visual line), ordinary paragraphs of full-look runs (`w:rPr`: bold/italic/colour/`w:sz` half-points/`rFonts` web-safe family via `fontFamilyClass`/sub-superscript), hyperlink stretches wrapped in `w:hyperlink` (relationship `TargetMode="External"`), inline images as the minimal `wp:inline` drawing tree (media parts SHA-256-deduped, display size = PDF placement in points → EMU ×12700, capped to printable width), `w:sectPr` last in body with the first page's geometry in twips and margins derived from the content bounding box (clamped 0.5"–1"). **Source pagination preserved by default**: the first block of every source page carries `<w:pageBreakBefore/>` (blank source pages become empty paragraphs; opt out via `NoPageBreaks`), side-by-side images (overlapping vertical ranges) share one paragraph as an image row (whole-row width scaling), and default paragraph spacing tightens to 3pt in pagination mode (6pt with `NoPageBreaks`) — Word-verified page counts match the source (showcase 15/15, Binder1 8/8, marketing 2/2, 4pages 4/4; all five reference docs match exactly). **Vector-graphics recovery** (`flow_vector.go`, core option `vectorGraphics`): painted-path bboxes are collected with CTM tracking (top-level content only, no Form-XObject recursion), clustered (8pt merge gap), filtered (≥16pt per side, ≤70% page area, ≤15% text coverage — shaded text panels stay text; boxes ≥85% covered by a raster image are its frame, not graphics), rendered WITH text by the built-in rasterizer at 144 DPI and cropped into image blocks; paragraphs and raster images ≥90% inside a cluster leave the flow (chart labels live in the picture). Logos, charts, drawings and ruled tables carry over as pictures — table-as-picture matches Aspose's own Flow behaviour until EnhancedFlow builds real `w:tbl`). **Alignment + line-break reconstruction**: per-block `w:jc` inferred from line geometry against the column (single-section pages use the page frame — a sparse cover's section rect degenerates to the text bbox; relative thresholds `minSide = max(12, 0.15·colW)`), deliberate breaks preserved as `w:br` in centered/right blocks only (left-aligned ragged lines re-wrap — hard breaks + wider substitute-font metrics would double them), heading merge gated on the previous line actually filling the column, and the first block of each page gets `w:spacing w:before` from its gap to the content-area top (×0.8, ≥24pt, ≤300pt) so covers keep their vertical layout; left-aligned breaks additionally require sentence-shaped boundaries (endsSentence/startsSentence) so ragged prose still re-wraps. **Footer reconstruction**: the furniture pass keeps an exemplar per repeating line (`furnitureExemplar`: text, look, band, source page); the most frequent bottom-band line becomes `word/footer1.xml` (w:ftr, exemplar look, centered when the source was) referenced from `sectPr`, with digit runs equal to the sighting's page number → `PAGE` field and equal to the page count → `NUMPAGES` — body paragraphs stay suppressed so nothing doubles). **Inline icons**: icon-sized vector clusters (5-40pt per side, <30% text coverage, not inside a block cluster) come from a second clustering WITHOUT hairline rules (an underline otherwise fuses with the mark beside it), render as small patches and are injected into the text line whose band contains them as inline drawing runs (`docRun.icon`), positioned by counting non-space bytes left of the icon (the stable currency between fragments and merged runs))
- OPC package on `archive/zip` (`docx_parts.go`): `[Content_Types].xml`, `_rels/.rels`, `word/document.xml` + its rels (styles/numbering declared as implicit relationships), `word/styles.xml` (docDefaults sized from the body median, Normal, Heading1-6, Hyperlink), `word/numbering.xml` (bullet + decimal abstracts, 9 levels each). XML via string templating + `xmlEscape` (the `xmp.go` pattern — `encoding/xml` marshalling fights OOXML namespaces); transitional namespaces, no `mc:Ignorable`/rsid noise so parts stay XSD-valid
- **Validation**: the whole 1014-document corpus converts with zero ECMA-376 transitional XSD violations and zero python-docx failures. `tools/validate_docx.py` is the local harness (rungs: lxml XSD — schemas fetched once into gitignored `result_files/docx/xsd/` with the `xml.xsd` import patched to resolve locally; python-docx round-trip; LibreOffice headless render when `soffice` is on PATH). Go unit tests cover the package invariants (parts covered by content types, relationships resolve, `sectPr` last, `numId`s defined, unique `docPr` ids)
- v1 limitations (documented in the file header): headers/footers suppressed (not `w:hdr`/`w:ftr`), underline/strikethrough not recovered (vector rules — arrives with the path-geometry visitor), fonts referenced by family name only (no embedding), tables flow as paragraphs

**`epub_write.go`** — PDF → EPUB export (epic `pdf-go-7qiu`); mirrors Aspose.PDF for .NET's `Document.Save(path, SaveFormat.Epub)` + `EpubSaveOptions` (Flow recognition mode; PdfFlow/Fixed out of scope)
- `(*Document).SaveEpub(path, opts...)` / `WriteEpub(w, opts...)` — reflowable EPUB 3. `EpubSaveOptions{Pages, Title (dc:title; falls back to /Info title, then "Document"), NoImages}`
- A second serializer over the **DOCX block model** (`buildDocxBlocks` with pageBreaks only as page-start markers): headings → `<h1..h3>` with ids, list runs grouped into `<ul>/<ol>` by numbering instance, code blocks → `<pre>`, paragraphs of merged runs (`epubSameStyle`: bold/italic/code/link/colour/sub/super → `<b>/<i>/<code>/<a>/<span style>`), alignment via `text-align`, `<br/>` from break runs, image rows + vector patches as `<img>` (SHA-256-deduped `OEBPS/images/`, hairline rasters < 4pt skipped), inline icons as `<img class="ic">`
- **Chapters** split at level-1 headings, else every 8 source pages (`brkBefore` markers); level-1/2 headings become the `nav.xhtml` TOC (two-level tree; an h2 before any h1 is promoted). OCF container: stored-first `mimetype`, `META-INF/container.xml`, `OEBPS/content.opf` (deterministic urn:uuid from content shape, `dc:language` from `/Catalog/Lang` else "en", `dcterms:modified` from Info ModDate else a fixed epoch — builds stay reproducible)
- Validation: corpus 1014/1014 with OCF + well-formed-XML invariants; smoke files open in Calibre (`ebook-convert` → txt). Go tests assert container invariants and content reconstruction

**`xlsx_write.go` / `xlsx_parts.go`** — PDF → XLSX export (epic `pdf-go-3zgg`); mirrors Aspose.PDF for .NET's `Document.Save(SaveFormat.Excel)` + `ExcelSaveOptions`. Design: `docs/superpowers/specs/2026-08-11-pdf-to-xlsx-design.md`
- `(*Document).SaveXlsx(path, opts...)` / `WriteXlsx(w, opts...)` — Excel workbook from detected tables. `XlsxSaveOptions{Pages, Mode, NoStyles}`; `XlsxRecognitionMode`: **`XlsxTablesOnly`** (default) — every `TableAbsorber` table stacked on one "Tables" worksheet ("Page N" captions, blank-row separators), tables continuing across consecutive pages with the same column signature (boundaries ±3pt table-left-relative, `stitchKey`) are **stitched** into one with the repeated header row dropped (PdfWithTable: 41+21 rows → one 62×3 sheet); **`XlsxFullPage`** — worksheet per page, ALL text laid into cells (layout lines → rows, `splitStreamRow` gaps → cells, global columns from clustered left edges, detected tables on their exact grids) **plus page images as floating pictures** (`xdr:oneCellAnchor` drawings at the image's PDF display size, anchored near their band row, SHA-256-deduped into `xl/media`; the bomb guard `embeddableImage` applies; `NoImages` opts out)
- **Numbers become numbers**: `parseCellNumber` classifies plain numbers (`1 234,56` / `1,234.56` separators normalized), percentages (`42%` → 0.42, numFmt `0%`) and single-symbol currency amounts (`€17.00` → 17 with `#,##0.00 "€"` numFmt) into numeric cells — SUM() over an exported column works immediately (Excel-verified on the showcase bill: SUM of Totals = 133). Ambiguous text stays inline strings; dates not parsed (v1)
- Merges from `RowSpan`/`ColSpan` → `<mergeCells>`; styles (unless `NoStyles`): cell fill from `AbsorbedCell.Shading`, bold/italic/font colour from `absorbedCellRuns`, right-alignment for numerics, column widths from `colXs` (pt/5.1, clamped [4,80]), all deduplicated through `xlsxStyleRegistry` into `cellXfs`
- `xlsx_parts.go`: string-templated SpreadsheetML (the `docx_parts.go` pattern, reuses `writeDocxZip`): `[Content_Types]`, workbook(+rels), `sheetN.xml` (sparse rows, `inlineStr` cells — no sharedStrings), `styles.xml` (deduped fonts/fills/numFmts, custom numFmt ids from 164). Strict-XSD-clean (no `xml:space` on `<t>` — the transitional schema rejects it)
- Related stream-detector guard added here: a two-column candidate of long titles against bare small integers ("1. Text Capabilities Showcase | 1") is a **TOC with vector leaders** — rejected (the dot-leader guard can't see drawn rules)
- Validation: `tools/validate_xlsx.py` (ECMA-376 `sml.xsd` via lxml + openpyxl round-trip), corpus 1,014/1,014 convert panic-free; Excel COM opens all smoke workbooks without repair. Out of scope (v1): formulas, images, FullPage cross-page stitching, date recognition, sharedStrings, `tableParts`

**`svg_export.go`** — PDF → SVG export (`pdf-go-3wi0`); mirrors Aspose.PDF for .NET's `Document.Save(SaveFormat.Svg)`
- `(*Page).WriteSVG(w, opts...)` / `SaveSVG(path, opts...)`; `(*Document).SaveSVG(path, opts...)` — one selected page → path itself, several → `<stem>_pN.svg` siblings. `SVGSaveOptions{DPI (0→150; viewBox scale + raster-patch resolution), Pages, ResourceWriter (externalize images/patches; nil = data: URLs, self-contained)}`
- Rides the HTML-native vector backend: `renderPageSVGCore(p, dpi, suppressText, hideFormWidgets, sink, num)` is the shared core (HTML native passes `suppressText=true`; standalone export passes `false`, so **page text renders into the SVG as outline paths** through the `compositePath` catch-all — exact glyphs, no font dependencies, not selectable). Standalone output = xml declaration + the `xmlns`-carrying `<svg>`. Round-trip-tested against our own SVG parser (`LoadSVGFromStream` accepts the export) and verified pixel-faithful by eye in headless Chrome

**`flow_doc.go`** — the **shared flow-reconstruction core** (epic `pdf-go-7qiu` phase 1): the format-neutral analysis layer under every flow-mode exporter (Markdown, HTML flow, DOCX, EPUB). `buildFlowDoc(pages, sel, flowDocOptions)` runs pass 1 once — `Paragraphs()` per page, block+image interleaving (`flowBlock`/`insertFlowImage`), the length-weighted body-size median (`weightedMedianSize`), repeating header/footer detection (`furnitureFilter`: digit-masked text in the top/bottom 12% bands on ≥ max(3, pages/2+1) pages; both paragraph- and segment-level drops), rotated-watermark suppression (`isRotatedDecoration`), URI-link areas (`pageLinkAreas`/`linkURIAt`) and an `onParagraph` hook (HTML flow registers WOFF font usage through it). Downstream primitives: `segmentParagraph` re-splits an extracted paragraph into homogeneous `docSeg`s (list-marker starts, dominant-size jumps >1.12×, monospace flips, vertical gaps >1.6×size); `segmentRuns` flattens a segment into `docRun`s carrying the full fragment look (bold/italic/mono, font name+size, colour, sub/super, hyperlink) with **gap-synthesized spaces** (`gapIsSpace`, >0.15 em); serializers coarsen runs to their format's expressiveness via `mergeRuns(runs, samePredicate)`; `headingLevel` (1.7/1.35/1.14 body-ratio thresholds, <200 chars) and `listNestDepth` (X-indent past the list base) are the shared classifiers. Dominant-style/size selection is **deterministic** (total-order tie-breaks — map-iteration ties used to flip headings between runs)

**`markdown_export.go`** — PDF → Markdown export (epic `pdf-go-ho25`), the reverse of the Markdown renderer; a pure serializer over the `flow_doc.go` core. Design: `docs/superpowers/specs/2026-07-21-pdf-to-markdown-design.md`
- `(*Document).SaveMarkdown(path, opts...)` / `WriteMarkdown(w, opts...)` — re-assemble the document as GFM Markdown. `MarkdownSaveOptions{Pages, ImageDir (SaveMarkdown default "<stem>_files"), ImageWriter (SHA-256-deduped callback), EmbedImages (data: URLs), NoImages}`; `WriteMarkdown` without a writer skips images
- Built on `buildFlowDoc` (furniture + rotated suppression + links on) → `segmentParagraph` → `mergeRuns(segmentRuns(…), mdSameStyle)`; heading thresholds via the shared `headingLevel` → `#`/`##`/`###`; consecutive same-level heading segments merge back into one `#` line (multi-line titles)
- **Inline fidelity**: styled runs (`**bold**`/`*italic*`/`` `code` `` by mono font) with gap-synthesized spaces; `[links](…)` recovered from `LinkAnnotation`+`GoToURIAction` rects by fragment-midpoint intersection; emphasis markers hug non-space edges; Markdown specials escaped (`mdEscaper` + block-start neutralization)
- Lists: bullet glyphs (`•◦▪‣-–` etc.) → `- `, `^\d{1,3}[.)]` → `N. `, markers stripped from runs, consecutive items blank-line-free, **nesting by X-indent** (>6pt past the list base → 4-space indent). Fenced code blocks for mono segments, one line per visual line, indentation reconstructed from X offsets (0.6 em/char), fence lengthened when content contains backticks
- Round-trip tested against the Markdown renderer (headings/bold/italic/links/lists/fences/indent survive `MarkdownToDocument` → `WriteMarkdown`); verified by eye on a real 2-page report. Known limits (v1, documented): GFM task checkboxes not recovered (labels are vector graphics), heading levels cap at 3; repeating header/footer lines are auto-suppressed (digit-masked repeats in the top/bottom 12% bands, paragraph- and segment-level) and fully-rotated paragraphs (watermarks) are dropped as decoration

**`ai/` subpackage (`ai/client.go` / `ai/openai.go` / `ai/summary.go` / `ai/ocr.go` / `ai/searchable.go`)** — AI copilots (epic `pdf-go-yrnd`, phase 1); mirrors the copilot layer of Aspose.PDF for .NET's `Aspose.Pdf.AI` namespace (not its 89-class raw OpenAI Assistants binding), adapted to Go idioms (ctx + error returns, options structs instead of `WithX()` builders). Stdlib-only; the root package stays free of AI/network code. Design: `docs/superpowers/specs/2026-07-13-ai-integration-design.md`. **Privacy**: copilots send document content (extracted text and/or rendered page images) to the configured endpoint — documented on the package and both copilots; local endpoints (Ollama/LiteLLM) work with the same client
- `AIClient` interface — `Complete(ctx, CompletionRequest) (*CompletionResponse, error)`; `Message{Role, Text, Images []MessageImage}` (vision parts as base64 data: URLs), `RoleSystem/RoleUser/RoleAssistant`, `Usage`, `APIError{Status, Code, Message}`
- `NewOpenAIClient(OpenAIClientOptions{BaseURL, APIKey, Model, HTTPClient, MaxRetries})` — OpenAI-compatible `/chat/completions` client (OpenAI, LiteLLM, Ollama, OpenRouter, …); BaseURL default `api.openai.com/v1`, 120 s timeout, exponential-backoff retries on 429/5xx (default 2, negative = off), text-only messages sent as plain string content and vision messages as content-part arrays. No streaming (v1)
- `NewSummaryCopilot(client, SummaryOptions{Document/Documents/Texts, Language, MaxWords, Prompt})` — mirrors `OpenAISummaryCopilot`. `GetSummary(ctx)` extracts per-page text, chunks it page-preserving at ~24k runes (`chunkTexts`), and map-reduces (partial summaries → final); `GetSummaryDocument(ctx, format...)` renders via the flow layout (heading from the source `/Info` title); `SaveSummary(ctx, path)`. Pages with no extractable text are skipped (error hints at `MakeSearchable` when nothing remains)
- `OCREngine` interface — `Recognize(ctx, image.Image) (*OCRResult, error)`; `OCRResult{Lines []OCRLine}`, `OCRLine{Text, Box *OCRBox, Words []OCRWord}`, `OCRBox{Left, Top, Right, Bottom}` in **image pixel space (origin top-left, Y down)**. Users adapt Tesseract/cloud OCR (with real boxes) through this; the bundled engine is:
- `NewLLMOCREngine(client, LLMOCROptions{Language})` — sends the page PNG to a vision model with a strict line-per-line transcription prompt; returns line-level text, no coordinates
- `NewOcrCopilot(engine, OcrOptions{Document, Pages, All, DPI (0→300), Font})` — `GetTextRecognition(ctx) ([]TextRecognitionResult{PageNumber, Text, Lines})` mirrors `OpenAIOcrCopilot` (read-only). Scanned-page detection (`pageNeedsOCR`): < 3 extractable chars **and** an image covering ≥ 70 % of the page area (`ImageInfos` display rects); `Pages` limits, `All` forces
- `(*OcrCopilot).MakeSearchable(ctx) (int, error)` — **the beyond-Aspose differentiator** (`ai/searchable.go`): renders each target page at `DPI`, recognizes it, and writes the text back as an invisible layer (`TextStyle.Invisible`, mode 3), so the scan becomes selectable/searchable with identical pixels (asserted in tests). Box-accurate placement when the engine reports boxes for every line (pixel→user-space via `72/DPI` + Y-flip against the CropBox; font size from box height, shrunk by a ~0.5 em/rune width estimate; draw rect widened so a line never word-wraps); even-grid fallback over the dominant image rect (else CropBox) for the coordinate-less LLM engine and for rotated pages. `OcrOptions.Font` supplies a Unicode face (default Helvetica = Latin only)
- `NewChatCopilot(client, ChatOptions{Document/Documents/Texts, SystemPrompt, MaxContextRunes, Temperature})` (`ai/chat.go`, phase 2) — document Q&A with conversation memory; mirrors `OpenAIChatCopilot`. `Ask(ctx, question) (string, error)` builds `[system-with-stuffed-document-context] + history + user`, sends one completion, and appends the user/assistant turns to the history; `History() []Message` (a copy) and `Reset()`. Context stuffing only (no embeddings/vector store in v1): `buildContext` gathers the sources' extracted text once via the shared `gatherSourceTexts` (also used by SummaryCopilot), joins and truncates to `MaxContextRunes` (default 48 000 ≈ 12k tokens), and when truncated tells the model so. Default system prompt grounds answers in the document ("if not in the document, say so"); `SystemPrompt` overrides it
- `NewImageDescriptionCopilot(client, ImageDescriptionOptions{Language, MaxWords, Prompt})` (`ai/imagedesc.go`, phase 3) — mirrors `OpenAIImageDescriptionCopilot`. `Describe(ctx, image.Image) (string, error)` returns a short caption via the vision model (default prompt: one concise alt-text sentence, no "image of"). `(*ImageDescriptionCopilot).FillAltTexts(ctx, doc) (int, error)` — **the accessibility one-call fix**: for every `/Figure` in a tagged document lacking `/Alt`, it describes the figure's image and sets the description, turning `ValidatePDFUA`'s `UA_FIGURE_NO_ALT` findings into a fix (returns the count filled). The PDF-side half is `(*Document).FiguresNeedingAltText() ([]*FigureAlt, error)` (`alt_text.go`, root package): walks the structure tree via the shared `structElemKids`/`resolveStructType`/`hasAltText`, and for each un-alt-texted `/Figure` resolves the image it brackets by parsing the figure's page content and mapping its marked-content MCID (`/K`) → image XObject `Do` (tracking the BDC/EMC MCID stack, matching against `ImageInfos` by name), exposed as `FigureAlt.Image()`/`SetAltText()`. Best-effort: a figure whose image can't be located is skipped
- Tests are network-free: `httptest` servers assert the wire format (`ai/openai_test.go`); fake clients/engines drive the pipelines; the MakeSearchable e2e builds a scanned-style fixture (rendered text page re-embedded as a full-page PNG) and asserts extraction, search-box position, pixel identity before/after, and Save+Open round-trip (`ai/ocr_test.go`); `ai/chat_test.go` asserts the message shape across turns, history copy-safety, reset, truncation and the prompt override; `ai/imagedesc_test.go` builds a tagged figure with no alt, confirms MCID→image resolution, fills it, and checks `ValidatePDFUA` clears `UA_FIGURE_NO_ALT` and the alt survives a round-trip. Gated live examples: `_examples/ai_summary`, `_examples/ai_make_searchable`, `_examples/ai_chat`, `_examples/ai_alt_text` (env `AI_BASE_URL`/`AI_API_KEY`/`AI_MODEL`)
- Epic `pdf-go-yrnd` complete: client, SummaryCopilot, OcrCopilot + MakeSearchable, ChatCopilot, ImageDescriptionCopilot + FillAltTexts

### PDF parsing pipeline

1. **`io.go`** — file I/O (`readFile`, `writeFile`)
2. **`xref.go`** — locates and parses the cross-reference table or stream; handles both traditional xref tables (PDF ≤1.4) and cross-reference streams (PDF 1.5+)
3. **`lexer.go`** — byte-level tokenizer; produces tokens (int, float, name, string, keyword, etc.)
4. **`parser.go`** — builds `pdfValue` objects from tokens; handles dicts, arrays, streams with FlateDecode/LZWDecode (`lzw.go`, MSB-first 9–12-bit codes, `/EarlyChange` 0 and 1)/ASCIIHex/ASCII85/RunLength filters and PNG predictor (Predictor 12). CCITTFaxDecode (`ccitt.go` / `ccitt_tables.go`) decodes Group 4 (`/K<0`) and Group 3 1-D (`/K=0`) fax data into packed 1-bpp rows (used by 1-bit scanned images and image masks); reads `/Columns`/`/Rows`/`/BlackIs1`/`/EncodedByteAlign` from `/DecodeParms` (mixed Group 3 2-D, `/K>0`, is not yet supported)
5. **`doc.go`** — document-level logic: object lookup with caching, object streams (ObjStm), page tree traversal, dependency collection
6. **`types.go`** — type definitions: `pdfValue`, `pdfDict`, `pdfArray`, `pdfStream`, `pdfRef`, `pdfObject`, `xrefEntry`

**Malformed-input tolerance** (`openStreamCore` → `buildFromXRef`, with fallbacks):
- Stream `/Length` may be an indirect reference (`/Length N 0 R`, ISO 32000-1 §7.3.8.2), missing, or wrong — `readStreamData` (`parser.go`) takes a usable direct integer verbatim, otherwise scans for the `endstream` keyword via `streamEndIndex`, which prefers the `endstream` confirmed by a following `endobj` so a spurious `endstream` byte sequence inside binary stream data (common in CCITT/JPEG streams) doesn't truncate the stream mid-data.
- `skipToStreamData` (`lexer.go`) skips spaces/tabs before the CR/LF after the `stream` keyword (non-conformant `stream \r\n`), so the filter sees a correctly aligned data start instead of mis-decoding (which previously caused a multi-GB allocation when the resulting garbage parsed as content ops).
- `flateDecode` (`parser.go`) keeps the inflated bytes when a valid-header zlib stream ends in a bad trailing Adler-32 checksum or is truncated by a wrong `/Length` (common in the wild — otherwise the whole page blanks). A bad zlib *header* is still rejected, so an undecrypted/random stream is not inflated into garbage.
- xref entry rows are read up to any line terminator (LF, CR, or CRLF), so classic-Mac CR-only xref tables parse.
- **Encrypted streams are decrypted from the file bytes, never from a parse-time decode** (`pdfStream.raw`): the parser cannot tell ciphertext from a Flate stream, and the old assumption that encrypted bytes never survive their declared filter is false — a zlib header is only "CM == 8 and (CMF<<8|FLG) %% 31 == 0", which random ciphertext satisfies about **once in 500**, and `flateDecode`'s deliberate tolerance of a truncated tail then yields a byte or two of garbage instead of an error. The stream was marked `Decoded` and the decryption pass skipped it, silently destroying it (observed: a font's `/ToUnicode` became one byte, so a space extracted as U+FFFD — the long-standing "flaky CI text mismatch"). `parseDictOrStream` now keeps the file bytes (a subslice, so free) in `pdfStream.raw`, `encryptedStreamBytes` hands them to all three decrypt paths (RC4 / AESV2 / AESV3), and `streamExemptFromEncryption` skips what a conforming producer leaves in the clear: `/Type /XRef` streams (never encrypted, ISO 32000-1 §7.5.8.2) and `/Type /Metadata` when `/EncryptMetadata` is false (`encryptState.plainMetadata`, read by the dispatcher). Corpus: 1 009 documents encrypt→reopen→extract with byte-identical text under all three algorithms
- **Damage reporting** (`repair.go`, `pdf-go-jn8g`): the recovery above used to be silent. `(*Document).NeedsRepair() bool` and `(*Document).RepairReport() RepairReport{XRefReconstructed, ObjectsRecovered, TrailerRecovered}` report whether the cross-reference table had to be rebuilt by scanning, how many objects that scan found, and whether the trailer was synthesised from a `/Type /Catalog` object. Mirrors the intent of Aspose.PDF for .NET's `Document.IsRepairNeeded`; there is deliberately **no `Repair()` verb** — opening a damaged file already yields the recovered document and saving it writes a clean one (xref rebuilt from what was actually written, dangling refs nulled, trailer regenerated), so the method would report "done" without doing anything. Scope: structural recovery only; per-stream tolerance (a wrong `/Length`, a truncated Flate tail) happens inside the parser without a document in reach and is not counted
- `xref_reconstruct.go` — when the xref is missing/corrupt or the catalog/page tree won't resolve (e.g. an off-by-one subsection start), `reconstructXRef` rebuilds the table by scanning the file for `N G obj` headers (latest revision wins) and recovers the trailer from the last `trailer` dict or the first `/Type /Catalog` object, then `openStreamCore` retries. Limitation: objects only inside compressed object streams (ObjStm) aren't recovered by the scan.

### PDF writing (`writer.go`)

`buildDocumentPDF(d *Document)` is the sole output function:
1. Assign sequential output IDs to all objects in `d.objects`
2. Patch `/Parent` in every page dict to point to the new `/Pages` node (via `pdfDirectRef`)
3. Serialize each object; write `/Pages`, `/Catalog`, `/Info`, `/Encrypt` structural objects last
4. Write xref table + trailer

**`pdfDirectRef`** (defined in `types.go`) — like `pdfRef` but written by `writeValue` without remapping. Used for `/Parent` patches so that the new `/Pages` object number (output space) is never accidentally remapped.

**Object assembly is shared** (`linearize_assemble.go`): `(*Document).assemble()` builds the output object set + structural metadata (sequential IDs, the `/Pages` node, output `/Catalog`, `/Info`, `/Encrypt` IDs, encryption state, file header) and is consumed by **both** `buildDocumentPDF` (sequential layout) and `buildLinearizedPDF` (linearized layout), so numbering and structural-object construction live in exactly one place.

**`html_export.go` / `html_export_fonts.go` / `html_export_svg.go` / `html_export_forms.go`** — PDF → HTML export (epic `pdf-go-rfom` complete: faithful + visible-text + native modes, WOFF fonts; epic `pdf-go-dvte`: interactive AcroForm conversion)
- `(*Document).SaveHTML(path, opts ...HTMLSaveOptions)` / `WriteHTML(w, opts...)` — write the document as **one self-contained HTML file** (no external assets, no JavaScript). `HTMLSaveOptions{DPI, Title, Mode, Pages}` (DPI 0 → 144; Title default: Info title; Pages = 1-based subset in the given order, anchors keep source numbers). Mirrors the intent of Aspose.PDF for .NET's `Document.Save(SaveFormat.Html)`
- `HTMLMode` enum — `HTMLModeFaithful` (zero value): each page is a full raster (built-in renderer → PNG, base64-inlined) under a **transparent selectable text layer** positioned from `ExtractTextWithLayout` — pixel-identical to `RenderPNG` while the text is selectable/copyable/Ctrl+F-searchable. `HTMLModeText` (phase 2): the background is rendered **without glyphs** (`renderImage(opts, suppressText)`; graphics, images and Tr 4-7 text-clip effects stay) and the text layer is **visible** — real colour (`TextFragment.Color` → hex), size, bold/italic and a metric-matched family stack (Arial/Times New Roman/Courier New) — crisp at any zoom, ~half the file size
- Width fitting (`htmlWidthFit`): the fragment's natural browser width is estimated with the Standard-14 metrics of the substitute face (`substituteFontFor`; Arial ≈ Helvetica etc. — same advances as the browser defaults); |ratio−1| < 0.005 → exact, small gap (0.95–1.05, multi-rune) → `letter-spacing` only (no glyph distortion), larger → `transform: scaleX(ratio)` clamped to [0.5, 2] with any clamped residual going to letter-spacing
- Both modes: link annotations become positioned `<a>` overlays (`writeHTMLLinks`: `/URI` actions → external links, `/GoTo` actions and page-ref `/Dest` arrays → `#pageN` anchors; named destinations not chased) and page backgrounds carry `loading="lazy"`. CSS lengths are in `pt` (1:1 with PDF points); fragment Y (baseline from page bottom) maps to CSS top via `pageH − Y − 0.8·size`; text and hrefs are HTML-escaped
- `suppressText` semantics (render.go/render_text.go/render_group.go): page content only — it propagates into transparency-group sub-renderers and gates Type3 glyph char-procs, but is cleared for the annotation pass (widget/stamp appearance text is not in the extraction layer, so it must stay in the raster)
- **WOFF font embedding** (phase 3, `html_export_fonts.go`): in `HTMLModeText` the document's embedded font programs are re-wrapped as **WOFF1** (44-byte header + per-table zlib, `woffEncode`) and emitted as `@font-face` data: URLs, so visible spans render in the document's real faces; `HTMLSaveOptions.NoFontEmbedding` opts out. Coverage: `/FontFile2` TrueType (simple + CIDFontType2) and `/FontFile3 /Subtype /OpenType` full-sfnt CFF (OTTO flavor preserved via `assembleSFNTFlavor`); bare CFF (Type1C/CIDFontType0C) and Type1 stay on the phase-2 substitute+fitting fallback. A composite font's sfnt has no usable cmap, so one is **synthesized** (format 4, pure idDelta segments, `buildCmapFormat4`) from `/ToUnicode` ∘ `/CIDToGIDMap` (Identity-H/V only), restricted to the runes actually drawn; missing `name`/`OS/2`/`post` (typical in PDF subsets, required by browser sanitizers like OTS) are synthesized too. Discovery: `htmlFontSet` walks each exported page's `/Resources/Font` + nested Form-XObject resources (deduped by object number), matches fragments by `cleanFontName`, two-pass `WriteHTML` (extract+collect, then emit — `@font-face` lives in `<style>`). Embedded-face spans skip width fitting (advances match by construction; PDF `Tc` maps to `letter-spacing`) and get `class="efN"` whose rule carries the substitute stack as fallback; `@font-face` declares `font-weight`/`font-style` so span styling matches without faux-bold. No re-subsetting (PDF fonts are near-always pre-subset; for library-built documents call `SubsetFonts` before `SaveHTML`). Validated with fontTools strict parse + full cmap coverage of drawn text
- **Interactive forms** (`html_export_forms.go`, epic `pdf-go-dvte` phase 1): `HTMLSaveOptions.InteractiveForms` (Text/Native modes only) converts AcroForm fields into **real fillable HTML controls** positioned over the page — text/password `<input>` and multiline `<textarea>` (value, `maxlength`, `readonly`), checkbox (`checked`, on-state as `value`), radio groups (one `<input type=radio>` per option widget, shared `name`, cross-page safe), combo `<select>` (editable combos → `<input>` + `<datalist>`), list `<select [multiple]>` with selected options; `required` from /Ff, `FieldStyle` (/MK /BS /DA /Q) → inline CSS (border incl. dashed/bevel/inset/underline, background, text colour/size/align). The converted widgets' `/AP` appearances are **suppressed from the background render** (`renderer.hideFormWidgets` skips widgets passing `convertibleWidget` — /Tx, /Ch, non-pushbutton /Btn — in the annotation pass), so nothing draws twice; push buttons and signature widgets stay static. Fill-and-print works with zero JavaScript; writing values back into the PDF is out of scope (JSON/FDF/XFDF interchange covers the server side). Differentiator: Aspose.PDF for .NET does not convert fields to HTML controls. **Phase 2** (also shipped): `NumberField` → `<input type=number>` (step from the AFNumber decimals, value sanitized of currency/separators), `DateField` → `<input type=date>` (value converted to ISO through the format mask via `dateMaskToGoLayout`; unconvertible mask/value falls back to a text input), push buttons with a **SubmitForm/ResetForm action** → real `<button type=submit/reset>` (caption from `/MK/CA`; submit carries `formaction`/`formmethod`, the exported pages get one document-level `<form>` wrapper via `htmlFormEnvelope` — submission posts standard urlencoded data, not FDF), and every control carries a `tabindex` following the page `/Annots` order (the PDF default tab order, `pageTabOrder` by dict identity). Push buttons with other/no actions stay static
- **Outline sidebar** (`html_export_nav.go`, `pdf-go-uhfs`): `HTMLSaveOptions.OutlineNav` renders the outline (bookmark) tree as a fixed left `<nav>` sidebar of links to the `#pageN` anchors (split exports link across files via `pageHref`) — nesting collapsible with pure `<details>/<summary>` (custom chevron), zero JavaScript; first two levels start open; page resolution: `/Dest` first then GoTo `/A` (`outlineItemPage`); no-op without outlines and in flow mode. **Toggle + responsive** (still zero JS — checkbox pattern): a fixed `☰` button (`label` for the hidden `#nvt` checkbox) opens/closes the panel; ≥1141px the panel is open by default and the pages wrapper (`.pgs`) shifts right (toggle hides it and reclaims the width), <1141px (page 816px + panel doesn't fit) it's hidden by default and opens as an **overlay flyout** over the content. Modern look: white card, system-ui, rounded hover states, ellipsis on long titles. Neither Aspose nor pdf2htmlEX renders bookmarks in HTML
- **Multi-file output** (`pdf-go-uft6`): `HTMLSaveOptions.ResourceWriter func(name string, data []byte) (url string, err error)` externalizes the heavy binary parts — page background PNGs (`pageN.png`), WOFF fonts (`font_<objnum>.woff`, object-number-keyed so names stay stable across split files), native-mode SVG images/patches (`pN_imgK`/`pN_patchK`), flow images — the callback's URL is referenced instead of a base64 data: URL (nil = self-contained single file, the default). `ResourceDir` (SaveHTML only) is the convenience form: files written into the dir next to the HTML, relative URLs (`dirResourceWriter`). `SplitPages` (SaveHTML only) writes one HTML per exported page as `<stem>_pN.html`, with cross-page GoTo links rewritten to sibling files via the unexported `pageHref` hook (`saveHTMLSplit`); `WriteHTML` rejects `SplitPages` (a stream can't hold multiple files — same restriction as Aspose). Mirrors `PartsEmbeddingMode`/`SpecialFolderForAllImages`/`SplitIntoPages`/`CustomResourceSavingStrategy`. **Content dedup** (`dedupResourceSink`, `pdf-go-motm`): byte-identical assets (a logo repeated on every page, table-header images on continuation pages, identical WOFFs) are keyed by SHA-256 of the final encoded bytes — the user's ResourceWriter is called once per unique asset and later occurrences reuse the first URL; one cache spans the whole run including all files of a SplitPages save. Showcase native: 2.86 MB inline → 252 KB HTML + assets deduped 127→28 files (2029→1753 KB)
- **Flow mode** (`html_export_flow.go`, `pdf-go-ibai`): `HTMLModeFlow` produces **reflowable** HTML instead of positioned page replicas — the counterpart of Aspose's `FixedLayout=false`. Built on the `flow_doc.go` core (`buildFlowDoc` with repeating header/footer + rotated-watermark suppression, like the Markdown exporter): each `MarkupParagraph` becomes a real `<p>` or `<h1>-<h3>` (level inferred from the paragraph's dominant font size vs the document's length-weighted median body size, headings capped at 200 chars), styled with the paragraph's **dominant look** (`dominantFlowStyle`: length-weighted vote over size/bold/italic/colour/font) — WOFF `efN` classes when the font is embedded, generic family classes otherwise; small-print/lead-in size deviations kept as relative `em`. Raster images flow between paragraphs by vertical position (`insertFlowImage`, original PNG/JPEG bytes, `max-width:100%`). Output is a responsive centered column (46em, viewport meta, `overflow-wrap`) — text genuinely rewraps on mobile widths. Not applicable by design: DPI, link overlays, interactive forms; tables/vector graphics are not reconstructed (their text flows as paragraphs). Verified in headless Chrome at desktop + 390px mobile widths on the showcase
- **Native mode** (phase 4, `html_export_svg.go`): `HTMLModeNative` drops the raster background entirely — each page's graphics become one inline `<svg>` layer emitted by an `svgDevice` **riding the content-stream renderer** (`renderer.vec`): the renderer keeps doing state tracking/colour/clip/recursion, but paints are redirected — path fills/strokes → `<path>` with **true curves** (the exec path operators feed a parallel device-space recorder via `pathMove`/`pathLine`/`pathCubic`/`pathClose`) and native stroke attributes (width/caps/joins/dash, device-scaled like the raster path); images → `<image>` with the PDF's own bytes (**JPEG passthrough**, reusing `xobjectImageInfo` + `Extract`) placed by the CTM (`matrix(...) translate(0 1) scale(1 -1)`); W/W*/Tr 4-7 clips → chained `<clipPath>` defs (`gstate.vecClip`; the raster clip stays live for patches); alpha → fill/stroke-opacity; blend modes → CSS `mix-blend-mode` (`blendMode.css`). **Per-element raster patches** (`vecPatch`): content SVG can't express — shadings, tiling/shading-pattern fills, soft masks, stencil masks, transparency-group compositing — is rendered by a vec-less sub-renderer (inheriting full gs incl. raster clip + soft mask) into a transparent page-sized buffer, cropped to its alpha bbox (`rgbaAlphaBounds`) and emitted as a positioned PNG `<image>` at the correct z — degradation is local, the page stays vector. Page text is suppressed (the WOFF span layer carries it); annotation-appearance glyphs arrive through the `compositePath` catch-all as flattened outline paths. Coordinates are device px at the export DPI (viewBox spans the page), so patches align by construction. Verified in headless Chrome against the showcase (cover/images/annotations/tables/vector pages pixel-faithful); ~2.9 MB vs 5.0 MB faithful. Epic `pdf-go-rfom` is complete; possible polish later: SVG `<linearGradient>`/`<radialGradient>` for type 2/3 shadings instead of patches, `<pattern>` for tiling fills

### Linearization / fast web view (`linearize.go` / `linearize_emit.go` / `linearize_hint.go` / `linearize_assemble.go`)

`(*Document).SaveLinearized(path)` / `WriteToLinearized(w)` write a **linearized** ("fast web view") PDF per ISO 32000-1 Annex F (epic `pdf-go-h7r`): a physical layout optimised for streaming, where the first page's objects and a primary hint table are reachable from the front of the file so a viewer can render page 1 before the whole file downloads. The output is an ordinary, fully spec-compliant PDF that any reader opens normally (linearized files are also valid non-linearized files). Encryption and signing are not supported in combination with linearization (rare for web-served PDFs); such documents return an error. Mirrors the intent of Aspose.PDF for .NET's linearized save.
- Layout (classic cross-reference tables): header → linearization parameter dict (`/Linearized /L /H /O /E /N /T`, first object, within the first 1024 bytes) → first-page xref + trailer (`/Prev` → main xref, early) → catalog → open-document objects (part 4) → primary hint stream → first-page section (part 6) → body: later pages' private groups (part 7), cross-page shared objects (part 8), then page-tree/outlines/thumbnails/rest/info (part 9) → main xref + trailer → `startxref` (→ first-page xref). Objects are renumbered part 2 = 1..K2 (body, low), part 1 = K2+1.. (first-page section, high), and **each page's group is numbered consecutively starting at its page object** — qpdf's strict check recomputes every page length as the byte span of nObjects consecutively numbered objects from the page object.
- Partitioning is a verbatim port of qpdf's categorization (`QPDF_linearization.cc`): every content object is classified by its user set — the pages that reference it (parsed values only, no stream-byte scanning; a stream dict's indirect `/Length` and dict values resolving to null objects are ignored, page dict keys are walked in reverse-sorted order with a shared visited set so a `/Thumb`-shared object is attributed to the thumbnail), the catalog keys it is reachable from (`/ViewerPreferences /PageMode /Threads /OpenAction /AcroForm` → part 4 "open document"; `/Outlines` → its own class, placed in part 6 when `/PageMode /UseOutlines`, else part 9; any other root key or the `/Info` closure → "others"), and page `/Thumb` closures. Chain: outlines → openDoc → first-page-private (page 0 only, no other users) → first-page-shared (page 0 + anything) → other-page-private (exactly one later page) → other-page-shared (>1 later pages) → part 9. A direct `/Outlines` dict is materialized as a synthetic indirect object (qpdf forces the same). Dangling references are written as `null` (never aliased through the identity fallback).
- Hint tables (primary hint stream, bit-packed column-major with **every row byte-aligned** — qpdf's reader calls skipToNextByte after each column vector): the **page-offset table** (13-field header; `nobjects[0]` = the whole part 6, `nobjects[i≥1]` = 1 + that page's privates; page length = the group's byte span; content length ≡ page length per implementation note 127; shared identifiers per page ≥ 1 index into the shared table, none on page 0), the **shared-object table** (all of part 6 in order, then part 8; `nshared_first_page` = len(part 6); each entry a single-object group with its own byte length), and the **outline table** (`/O` in the hint dict, HGeneric 4×32 bits: first object/offset/count/group length; outline objects contiguous, root dict first). Two-pass-free: bit widths depend only on lengths (offset-independent), so objects are serialized once, the hint structure is computed, then absolute offsets are filled in.
- **Validation status**: qpdf's strict `check_linearization` (which recomputes every hint value from the file and compares) passes on **1008 of 1009** linearizable corpus documents — including cross-page shared fonts/resources, AcroForm, outlines and thumbnails; the one failure is a damaged file qpdf itself cannot linearize ("unable to linearize first page" on the source). CI-runnable invariants (a Go hint-stream decoder asserting the qpdf model) live in `linearize_hint_test.go`; the full pikepdf/qpdf harness in `result_files/lin/` (gitignored).

### Dependency collection (`doc.go`)

`collectPageDeps` recursively walks the object graph (dict values, array elements, stream dict, and raw stream bytes via regex `\b(\d+)\s+\d+\s+R\b`) to find all objects needed for a page. Skips `/Pages` and `/Catalog` nodes — these are rebuilt by the writer. Used by `Split` and `Extract` to build new single-document object sets.

`rewriteRefs` deep-copies a `pdfValue` tree translating all `pdfRef` IDs through an id-map. Used by `Append` to merge objects from another document without ID collisions.

### Text extraction (`text.go`, `text_layout.go`, `content_parser.go`, `font.go`, `font_metrics.go`, `encoding.go`, `cmap.go`)

1. `parseContentStream(data)` tokenizes content stream bytes into `contentOp` structs (operator + operands), reusing the existing `lexer`
2. `resolveFont(objects, fontDict)` maps font dictionaries to `fontInfo` — supports WinAnsi, MacRoman, MacExpert, Standard encodings, `/Differences`, standard 14 fonts, Symbol, ZapfDingbats, ToUnicode CMap, Type0/CIDFont with Identity-H encoding, and **predefined CJK CMaps** (see the CJK section below — `resolveType0Encoding` resolves a named `/Encoding` like `/GBK-EUC-H` to a code→CID `cidCMap` and the descendant's `/Ordering` to a CID→Unicode table); resolves glyph widths from `/Widths`, Standard 14 metrics, CID `/DW`+`/W`, or fallback
3. `parseCMap(data)` / `parseCMapFull(data)` (`cmap.go`) parse ToUnicode CMap streams — `beginbfchar`/`endbfchar` and `beginbfrange`/`endbfrange` (sequential and array forms). Destinations decode as UTF-16BE with surrogate pairs; a multi-character destination (a ligature glyph mapped to "fi") keeps its first character in `fontInfo.toUnicode` and the whole sequence in `fontInfo.toUnicodeSeq`, which the extractor's `emitMapped` emits character by character at the glyph's position
4. `textExtractor` state machine processes operators (BT/ET/Tf/Td/Tm/Tj/TJ/Tz/etc.), tracking text matrix position, font, spacing, and horizontal scaling; advances text matrix by glyph width after each character (PDF spec 9.4.4); splits into single-byte and multi-byte paths for Type0/CIDFont
5. Fragment collection: `emitRune` collects `textFragment` structs with (x, y, endX, fontName, fontSize); new fragment on font change, Y gap > fontSize×0.5, or X gap > spaceWidth×0.3
6. Visual sorting (`text_layout.go`): `groupFragmentsIntoLines` sorts fragments by Y descending then X ascending, groups by Y proximity into `TextLine` structs; `ExtractTextWithLayout` returns the structured result; `ExtractText` delegates to same pipeline
6a. **Logical order for right-to-left lines** (`bidiVisualToLogical`, `bidi.go`): glyphs are collected in visual order, which reads backwards for Arabic and Hebrew, so a line carrying strong RTL characters is reordered back into the order its characters were typed — `assembleLine` fixes `TextLine.Text` and `buildLineRuneMap` (`text_search.go`) does the same for the search haystack, keeping the per-rune fragment mapping so match rectangles stay correct. The base direction cannot come from rule P2 (it names the first strong character in *logical* order, which is the last one here), so `bidiVisualBaseLevel` decides by majority of strong characters. A glyph or `/ActualText` span standing for several characters is laid down reversed for an RTL cluster (`emitCluster`) — its text is logical while the line around it is visual, so a lam-alef ligature would otherwise come back with its two letters swapped. `TextFragment`s stay in visual order (their X positions drive the layout consumers: table detection, column splitting, the flow exporters), so a flow exporter still reconstructs an RTL paragraph's words in visual order; `TextExtractRaw` is unaffected by design
7. Form XObjects (`Do` operator) are recursively processed with inherited CTM and overridden resources
8. Marked content (`BDC`/`BMC`/`EMC`): when `BDC` carries `/ActualText` in its properties, glyph emission is suppressed and the replacement text is emitted at `EMC`; supports inline dicts, `/Properties` resource lookup, UTF-16BE strings, and nesting

### Image extraction (`image.go`, `image_decode.go`, `image_inline.go`)

1. Content stream walker tracks CTM via `cm`/`q`/`Q` and collects images on `Do` (XObject) and `BI` (inline)
2. DCTDecode images are passed through as JPEG; a chained `/Filter` array ending in the image codec (e.g. `[/FlateDecode /DCTDecode]`, common in scanned MRC PDFs) is unwrapped by applying the preceding filters first (`filterChain` + `unwrapped` in `extractXObjectImageData`); **CMYK** DCTDecode (and any with an `/SMask` or a stencil `/Mask`) are decoded to RGB and re-encoded as PNG, because Adobe/Photoshop CMYK & YCCK JPEGs store their channels inverted (APP14 "Adobe" marker) and Go's `image/jpeg` returns them raw — `jpegHasAdobeMarker` detects the marker and `decodeJPEGToPixels` re-inverts so white doesn't render as black; all non-DCT images are decoded to pixels and encoded as PNG
3. Colour-key masking (`/Mask` as a sample-range array, ISO 32000-1 §8.9.6.4) is applied as per-pixel alpha computed on the pre-conversion samples (`colourKeyAlpha`) — palette indices for Indexed images. Color spaces: DeviceRGB, DeviceGray, DeviceCMYK (→RGB via `adobeCMYKToRGB`, a 5×5×5×5 LUT baked from the MuPDF/Adobe default CMYK profile with quadrilinear interpolation — `cmyk_lut.go` — so process colours match Acrobat instead of the bluish naive `(1-C)(1-K)`; used by image decoding and the renderer's `k`/`K`/`scn` fill paths), Indexed (palette expansion), ICCBased (treated as underlying RGB/Gray/CMYK)
4. Soft masks (`/SMask`) are applied as PNG alpha channels (`decodeSoftMaskData`: JBIG2-coded masks, 1/2/4-bpc expansion, `/Decode [1 0]` inversion; `fitSoftMask` reconciles a mask whose resolution differs from the image — a higher-res bilevel text mask upscales the colour to the mask grid, MRC-style); JPEG+SMask is re-encoded as PNG
5. Inline images (BI/ID/EI) are parsed with abbreviation expansion (PDF spec Tables 4.43/4.44)
6. Form XObjects are recursed into with inherited CTM and resources
7. **JBIG2** (`/JBIG2Decode`, 1-bpp scanned bilevel): decoded in-house (`jbig2*.go`) since `decodeStream` can't reach the `/JBIG2Globals` stream — `extractXObjectImageData` detects the filter, resolves globals from `/DecodeParms` (`jbig2GlobalsData`), and calls `jbig2Decode` to a packed 1-bpp DeviceGray bitmap (0 = black; JBIG2 foreground is inverted), so extraction and the renderer (which shares `Extract`) both work. See the JBIG2 section below for scope.
8. **JPEG2000** (`/JPXDecode`, 8-bpp colour scanned layers): decoded in-house (`jpx*.go`) to interleaved 8-bit RGB/Gray pixels and encoded as PNG. `extractXObjectImageData` detects the filter and calls `jpxDecode`. **MRC stencil masks**: a colour `/JPXDecode` image with an explicit `/Mask` referencing a (usually high-res, JBIG2-coded) `/ImageMask` is composited at the mask's resolution (`decodeStencilMask` + `applyStencilMask`, ISO 32000-1 §8.9.6.3) so the bilevel text mask stays sharp over the low-res colour foreground — the standard scanned-document layering of `/bg` (background) + masked `/fg` (foreground). See the JPEG2000 section below for scope.

### JBIG2 decoding (`jbig2.go`, `jbig2_mq.go`, `jbig2_generic.go`, `jbig2_refine.go`, `jbig2_symbol.go`, `jbig2_text.go`, `jbig2_tables.go`)

Pure-Go JBIG2 decoder (ITU-T T.88) for the PDF `/JBIG2Decode` filter. Epic `pdf-go-hqr`. **Phase 1 (complete)** covers the embedded-organization segment stream on the arithmetic (MQ) coding path — the combination scanned-document PDFs almost always use:
- `jbig2_tables.go` — the MQ probability-estimation state table (Table E.1).
- `jbig2_mq.go` — the MQ arithmetic decoder (software conventions: C split into chigh/clow), the arithmetic integer decoding procedures (`decodeInt` for IADH/IADW/IAEX/IAAI/IADT/IAFS/IADS/IAIT/IARI/IARDW/IARDH/IARDX/IARDY) and `decodeIAID` for symbol IDs. Faithful port of the reference decoder; validated byte-identical to jbig2dec / PyMuPDF.
- `jbig2_generic.go` — `jbig2Bitmap` (height×width, 1 byte/pixel) and generic-region decoding (templates 0–3 with adaptive pixels and TPGDON typical prediction).
- `jbig2_refine.go` — generic refinement-region decoding (templates 0/1), used by refinement/aggregate symbol dictionaries and refining text regions.
- `jbig2_symbol.go` — `jbig2Ctx` (all IAx contexts + GB/GR context arrays + IAID), symbol-dictionary decoding by height class; the SDREFAGG path refines a single referenced symbol (REFAGGNINST==1) or aggregates several via a refining text region (>1); IAEX export run-lengths.
- `jbig2_text.go` — text-region decoding: strips of symbol instances placed by IADT/IAIT (T), IAFS/IADS (S) and IAID, honoring REFCORNER/TRANSPOSED/SBDSOFFSET and per-instance refinement; the strip loop reads the terminating OOB IADS so the bit stream stays in sync.
- `jbig2.go` — segment-header parser (embedded organization), page composition across segments (page info / generic region / symbol dict / text region), and the public `jbig2Decode(stream, globals, w, h)` returning packed 1-bpp. Unknown-length segments (T.88 §7.2.7, dataLength 0xFFFFFFFF on an immediate generic region) are delimited by scanning for the terminator (0xFF 0xAC arithmetic / 0x00 0x00 MMR) followed by the 4-byte declared height — same approach as pdf.js.

**Phase 2 (added: `jbig2_huffman.go`, `jbig2_huffsym.go`, `jbig2_hufftext.go`, `jbig2_mmr.go`, `jbig2_halftone.go`):**
- `jbig2_huffman.go` — Huffman bit reader, canonical-prefix table builder/decoder, and the 15 standard tables (Annex B); custom table segments (type 53) parse in `jbig2.go`.
- `jbig2_huffsym.go` — Huffman symbol dictionaries: height classes with a collective bitmap (MMR-coded, via `jbig2_mmr.go`, or uncompressed) split into per-symbol bitmaps. `jbig2_hufftext.go` — Huffman text regions with the run-code symbol-ID table (§7.4.3.1.7), including transposed regions with SBSTRIPS>1. Validated byte-identical (0 differing pixels) on a real two-page globals + symbol-dict + text-region scan (one page transposed, one not).
- `jbig2_mmr.go` — MMR (Group 4) regions/bitplanes reuse the `ccitt.go` G4 decoder (`pdf-go-d6n`).
- `jbig2_halftone.go` — pattern dictionaries (type 16) and halftone regions (types 20/22/23): Gray-coded bitplanes select indexed patterns stamped on a grid (`pdf-go-nkf`). Standalone refinement regions (types 40/42/43, `pdf-go-2mk`) decode in `jbig2.go` via the Phase 1 refinement decoder.

Halftone/standalone-refinement are implemented per spec but not yet corpus-validated. Unsupported constructs are skipped, so a page still renders rather than erroring.

### JPEG2000 decoding (`jpx.go`, `jpx_tier2.go`, `jpx_tagtree.go`, `jpx_tier1.go`, `jpx_dwt.go`)

Pure-Go JPEG2000 decoder (ISO/IEC 15444-1) for the PDF `/JPXDecode` filter. Epic `pdf-go-8ju`. Decodes a JP2/raw codestream to interleaved 8-bit samples; validated **pixel-exact** against pdf.js (per-component pre-transform meanAbsDiff 0.0000; final RGB within ±1 on a handful of pixels, ICT float→int rounding only) on the `/bg` and `/fg` layers of a real MRC scan.
- `jpx.go` — JP2 box walk (`jpxParseBoxes`) and codestream marker parser (`jpxParseCodestream`: SIZ/COD/COC/QCD/QCC/SOT/SOD), the public `jpxDecode(data) (pixels, comps, w, h, err)`. Unsupported COD options (selective arithmetic bypass, per-pass termination, vertically-causal context, predictable termination) surface as `jpxErr` rather than mis-decoding.
- `jpx_tier2.go` — tile/precinct/code-block geometry, packet iteration (LRCP/RLCP progression with an LRCP fallback), and packet-header parsing (inclusion + zero-bit-plane tag trees, Lblock length signalling). Subband gain `{LL:0, LH:1, HL:1, HH:2}`.
- `jpx_tagtree.go` — the inclusion and zero-bit-plane tag trees (§B.10.2). **The level count is `ceil(log2(max(w,h)))+1`** — matching pdf.js exactly. Floor-vs-ceil only diverges for a non-power-of-two code-block grid (e.g. 3 code-blocks across), where floor gives too few tree levels and silently corrupts every higher-resolution subband. This was the decisive bug: low resolutions (1–2 code-blocks) matched by coincidence while res ≥ a 3-wide grid exploded.
- `jpx_tier1.go` — EBCOT tier-1 bit-plane decoding (Annex D): the three coding passes (significance propagation / magnitude refinement / cleanup) over each code-block, driven by the shared MQ arithmetic decoder (`jbig2_mq.go` — JPEG2000's MQ coder is identical to JBIG2's). 19-entry context state packed as `(stateIndex<<1 | mps)`; context-label tables ported from pdf.js.
- `jpx_dwt.go` — inverse quantization, coefficient placement into the resolution grid, inverse DWT (5/3 reversible + 9/7 irreversible lifting), multi-component transform (RCT/ICT) and DC level shift, clamped to 8-bit.

Out of scope (not yet needed by the corpus): tile-parts beyond the first SOT per tile, packed-packet-headers (PPM/PPT), region-of-interest (RGN), and the COD options listed above. Unsupported inputs error cleanly (the image is skipped, the rest of the page still renders).

### ICC colour management (`icc.go`)

ICCBased colour spaces are converted through the embedded profile instead of being passed through as their underlying device space (epic `pdf-go-16u`). Pure-Go ICC v2 parser and transform engine: monochrome (`kTRC`) and three-component matrix/TRC profiles (`rXYZ/gXYZ/bXYZ` + `curv`/`para` tone curves, parametric types 0-4 sampled to tables), and v2 LUT profiles (`mft1`/lut8, `mft2`/lut16: input curves → multilinear CLUT interpolation → output curves) with XYZ or Lab PCS (legacy 16-bit Lab encoding handled); PCS → sRGB via the combined Bradford-adapted D50→D65 matrix. **sRGB-likeness detection** (primaries + TRC probes) skips the per-pixel work for the overwhelmingly common embedded-sRGB case. Wire-in: image extraction (`extractXObjectImageData` generic path + DCT branch — a wide-gamut JPEG is decoded, transformed and re-encoded as PNG since bare JPEG bytes would be shown as sRGB) with a memoised per-colour cache, and renderer `sc`/`scn` fills via `csTintConverter` (an `/ICCBased` array yields a profile-backed tintFunc). Corpus survey: 156 profiles across 131 docs — 106 RGB matrix/TRC (93 sRGB-like → skipped), 49 gray TRC, 1 CMYK LUT; all parse. Out of scope: ICC v4 `mAB`/`mBA` pipelines (none in the corpus), rendering intents beyond the default, Indexed-over-ICCBased bases; parse failures keep the previous pass-through behaviour.

### CJK / CID-keyed font support (`cmap_cid.go`, `cmap_cid_data.go`, `cjk_cmaps/`)

Non-embedded composite (Type0) CJK fonts — e.g. SimSun with `/Encoding /GBK-EUC-H`, Adobe-GB1 ordering, no `/FontFile2` and no `/ToUnicode` — previously extracted as U+FFFD and rendered blank. Epic `pdf-go-eaw`. Decisions: **system fonts** (no bundled CJK font — those are ~10–16 MB) and **all CJK orderings** (Adobe GB1 / CNS1 / Japan1 / Korea1 / KR).
- `cmap_cid.go` — `cidCMap`: parses the CMap text format (`begincodespacerange` / `begincidrange` / `begincidchar` / `usecmap`) into a variable-width codespace (1–4 bytes) and a code→CID lookup (sorted ranges + singles, binary-searched). `next(s)` consumes one code per the codespace (byte-wise containment, §9.7.6.2), so a single string mixes 1-byte Latin and 2-byte CJK. The same parser serves both the predefined Adobe CMaps and a PDF's own embedded `/Encoding` CMap stream.
- `cjk_cmaps/` + `cmap_cid_data.go` — `//go:embed`ed, gzip'd data generated from Adobe's BSD-licensed cmap-resources: the predefined encoding CMaps (code→CID, e.g. `GBK-EUC-H`, `90ms-RKSJ-H`, `B5pc-H`, `KSC-EUC-H`, the `Uni*-UCS2/UTF16` families) and a compact per-ordering CID→Unicode table (from each `cid2code.txt`'s UTF-16 column, preferring CJK-unified codepoints over radicals/compat). `predefinedCMap(name)` loads + caches a CMap (resolving `usecmap` against the bundle); `cidToUnicodeForOrdering(ordering)` loads the CID→Unicode map. ~1.3 MB total.
- `resolveType0Encoding` (`font.go`) wires this into `fontInfo`: `/Encoding` name → `cidCMap`, `/CIDSystemInfo /Ordering` → `cidToUni`. Extraction (`showStringCMap` in `text.go`) decodes codes → CIDs → Unicode (and `/W` widths keyed by CID); Latin codes the CMap maps to proportional-Latin CIDs that Adobe leaves Unicode-less fall back to the ASCII code itself.
- Rendering (`render_text.go`): a non-embedded CJK font resolves to a real outline font via `fontRepo.findCJK` — the caller-registered fonts first, then a **separate, lazily-built index of the OS font directories** (kept apart from the opt-in `find()` so Latin substitution stays opt-in), matched by the document's family (SimSun → `simsun.ttc`) or a well-known default family for the ordering (`cjkOrderingFamilies`). Glyphs map CID → Unicode (`cjkUni`) → GID through the system font's cmap; the per-font `cjkASCII` override covers Latin-in-CJK-font runs. With no covering font installed, CJK glyphs are skipped (blank) rather than erroring. A non-embedded *simple* font that is really a CJK face (e.g. SimSun with WinAnsiEncoding for its Latin glyphs) is likewise resolved to the installed CJK font (`isCJKFamily` → `findCJK`) instead of a bundled Latin substitute, so its Latin renders in the document's actual face and stays consistent with its composite (Type0) sibling. Font matching uses an exact PostScript-or-family name and a **compact** (separator-stripped) name (`findSystemExact` / `sysByCompact`), so a BaseFont `YuGothicMedium` resolves to the installed `YuGothic-Medium` / `Yu Gothic Medium` — the exact weight, with the correct units-per-em — rather than a coarse family default; `findCJK` tries this exact match first, then the ordering-default families (exact, then a word-boundary-prefix fallback `sysFamilyPrefix` so a candidate like `microsoft jhenghei` resolves to the installed `Microsoft JhengHei UI`, while `-ExtB`/`-ExtA` extension faces — which carry only rare supplementary ideographs — are skipped so common characters don't render blank; e.g. `MSung-Light`/CNS1 → `msjh.ttc`). The CID→Unicode table also drives **Identity-H** composite fonts whose descendant is a real Adobe-ordered CIDFont (`/Ordering (Japan1)`, no `/FontFile2` — e.g. a non-embedded Yu Gothic): the 2-byte code is the Adobe CID, so CID → Unicode → GID renders the installed face. A non-embedded Identity-H font with no Adobe ordering still falls back to the GID-as-CID path against the real installed font (`findSystemExact`) when present. When that font has an **explicit (non-Identity) `/CIDToGIDMap`** — whose GIDs only match the absent embedded program, so GID-as-CID is wrong — and a **`/ToUnicode`** CMap with a Latin BaseFont (e.g. Arial in a WinZip/Acrobat visible signature appearance, `pdf-go-5ws`), the renderer substitutes a metric-compatible face (`find` → `findSystemStd14` → bundled `fallbackFontFor`) and reaches glyphs through Unicode: code(=CID) → `/ToUnicode` rune → substitute glyph (reusing the `cjkUni` map, which `gid()` consults before the unusable `/CIDToGIDMap`). The exact installed font still wins for an Identity `/CIDToGIDMap`. Validated: 59015.pdf (GBK-EUC-H / SimSun), plus Japan1 (`90ms-RKSJ-H`, `UniJIS-UCS2-H`) and mixed GB1/CNS1 documents render via the installed CJK fonts; 50466_signed.pdf's visible signature text renders via the ToUnicode-substitute path.

## Output conventions

- All files produced by examples and manual runs are saved to `result_files/` in the project root.
- Generated Markdown reports (API comparisons, release notes, and similar `.md` write-ups) go in `reports/` at the project root instead — kept out of `result_files/` so they're easy to find among the binary output.
- Both `result_files/` and `reports/` are not committed to the repository (gitignored).
- Exception: `_examples/feature_showcase/main.go` writes `docs/feature_showcase.pdf` (committed and linked from README). Regenerate only on meaningful changes to keep git history lean.
- Exception: `_examples/showcase_preview/main.go` writes `docs/feature_showcase-preview.png` (committed; the clickable preview in the README) — a 2×2 montage of showcase pages rasterized by this library's own `Document.RenderImage`. Regenerate after the showcase changes: `go run ./_examples/feature_showcase` then `go run ./_examples/showcase_preview`.

## Examples

- Standalone runnable example programs live in `_examples/<name>/main.go`. The leading `_` makes the Go toolchain skip these when matching `./...`, so `go build ./...` and `go test ./...` do not touch them. Run individually with e.g. `go run ./_examples/feature_showcase`.
- Short, focused API examples live as `ExampleXxx` functions in `examples_test.go` (package `asposepdf_test`). These appear under "Examples" on pkg.go.dev next to each documented function and are validated by `go test` via their `// Output:` comments.

## Testing conventions

- Test PDF files are stored flat in `testdata/` (`4pages.pdf`, `Binder1.pdf`, `PdfWithLinks.pdf`, `PdfWithTable.pdf`, `alfa.pdf`, `marketing.pdf`, `Hello world.pdf`, `PdfWithAcroForm.pdf`).
- Which files each test uses is declared in `testdata/testfiles.json` — keyed by test function name; value is `[][]string` (array of groups, each group is an array of file names). One group = one test run; multiple groups = the test is run once per group.
- When writing tests that use real PDF files, use the `testFile(t)`, `testFiles(t)`, or `testGroups(t)` helpers from `helpers_test.go`, and add the corresponding entry to `testdata/testfiles.json`. Ask the user which file to use before adding a new entry.
- Each feature gets its own `*_test.go` file (e.g. `splitter_test.go`, `metadata_test.go`).
- `TestSplitFiles` in `splitter_test.go` iterates files listed in `testdata/testfiles.json` under `"TestSplitFiles"`, splits each into `result_files/TestSplitFiles/<stem>/`, and validates every output page with `Validate`.

## Task tracking (beads)

This project uses [beads](https://github.com/gastownhall/beads) for issue/task tracking via the `bd` CLI.

```bash
# Status overview
bd status

# Create an issue
bd create "title" --body "description"

# List issues
bd list

# Update issue status
bd update <issue-id> --status <open|in-progress|closed>

# View an issue
bd show <issue-id>
```
